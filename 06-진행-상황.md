# 구현 현황 — autotuner/

> **이 문서는 무엇인가.** **무엇이 되고 무엇이 안 되는지**의 목록이다. 남은 작업과, 보류하기로 결정한 것들이 이유와 함께 적혀 있다.
>
> **§4 의 보류 항목을 먼저 보라.** 이미 조사하고 접은 것들이다. 모르고 다시 파면 시간을 버린다.
>
> **언제 읽나.** 작업을 시작하기 직전. 가장 자주 갱신되는 문서다.

**최종 갱신:** 2026-09-02
**목적:** 신규 다계층 병렬 I/O 오토튜닝 프레임워크를 개발하기 위한 **베이스라인 확보.** 같은 파라미터 공간·같은 목적함수·같은 측정 방식 위에서 H5Tuner와 TunIO를 비교할 수 있어야 한다.
**함께 볼 문서:** [`07-후속-연구-계획.md`](07-후속-연구-계획.md) (설계 근거), [`03-원본-코드-분석.md`](03-원본-코드-분석.md) (저장소·논문 분석)
**추적 상태:** 2026-09-14 부터 공개 저장소(`h5tuner-ext`)에 함께 올린다.

---

## 한 줄 현황

**세 프레임워크가 모두 end-to-end로 돌아간다.** TunIO의 세 컴포넌트 중 탐색 루프에 들어가는 둘(부분집합 선택, 조기 종료)은 구현됐고, 소스 분석(I/O Discovery)은 미착수다.

**조기 종료기에 알려진 한계가 하나 있고 보류 결정했다** — 합성 곡선에서는 휴리스틱 베이스라인이 더 좋다. 실기 검증 후 재검토한다. §4.1

**h5bench를 PBS Pro(누리온)에서 튜닝할 수 있다 (2026-08-25).** 후보마다 잡을 제출하는 런처와 h5bench 어댑터를 넣었다. 가짜 스케줄러·가짜 벤치마크로 세 프레임워크 전 경로를 확인했다. 실기는 미확인이다. 상세는 `02-설계-결정.md` §12.

**그 과정에서 기존 결함 하나를 고쳤다.** 반복 측정이 대역폭에 반영되지 않고 있었다. `--reduction`이 벽시계에만 걸려서 TunIO의 "후보당 3회"가 사실상 1회였다.

**TunIO 세 컴포넌트가 모두 구현됐다 (2026-08-25).** I/O Discovery와 Darshan을 넣었다. 논문 재독으로 §7 #22(반복 실행 시간을 튜닝 비용에 넣지 않는다)를 새로 찾아 맞췄다. 상세는 `02-설계-결정.md` §13.

**논문 충실도로 남은 것 셋.** PCA(#9), 2단 신경망(#11), 세대 50(#16). 전부 의도적 편차이고 근거가 §7에 있다.

---

**누리온에 올라갔고 shim이 빌드됐다 (2026-08-28).** 이게 이 저장소에서 가장 오래된 미확인 항목이었다. 진짜 HDF5 1.14.1.2 헤더로 컴파일했고 경고가 하나도 없다.

**툴체인이 예상과 달랐다.** h5bench와 MACSio가 Intel이 아니라 GCC 10.2.0 + OpenMPI 3.1.0 + `hdf5-gcc`로 빌드되어 있다. Intel HDF5로 shim을 만들었다면 심볼이 맞지 않아 `LD_PRELOAD`가 조용히 아무 일도 하지 않았을 것이다.

**`configure.ac`의 기존 버그를 고쳤다.** `-lmpich`가 하드코딩되어 있어 OpenMPI나 Intel MPI 사이트에서는 항상 링크가 깨진다.

**잡 개수를 15배 줄였다.** 세대 전체를 잡 하나에서 평가한다. 40세대 캠페인이 601잡에서 41잡이 된다. 누리온 `normal` 큐가 88% 혼잡하고 380개가 대기 중이라 이것이 실질적 제약이었다.

**MACSio 어댑터를 넣었고 h5bench write보다 나은 튜닝 대상임을 확인했다.** SIF 모드가 데이터셋을 single chunk로 만들어 `chunk_cache`가 살아나고, 메시 메타데이터가 많아 메타데이터 파라미터 넷도 실제로 눌린다.

---

**실기 튜닝이 성공했다 (2026-09-01).** H5Tuner 가 h5bench write 를 260 → 575 MB/s 로 **121% 개선**했다. TunIO 는 sweep 순위를 받아 거의 같은 성능(568)을 **32% 적은 시간**에 얻고 7세대에서 조기 종료했다. RoTI 가 137 대 173 으로 TunIO 가 26% 높다. **논문의 주장이 재현됐다.**

**여기까지 오는 데 조용한 실패 다섯을 걷어냈다.** 전부 에러 없이 잘못된 결과를 내던 것들이다. 상세는 `04-누리온-환경.md` §7b~§7e 와 스킬 `autotuner-context`.

`NUM_PARTICLES` 가 쓰기에서 무시되던 것, 크기 접미사가 공백을 요구하던 것, shim 이 `libmxml.so.1` 을 못 찾아 로드조차 안 되던 것, HDF5 심볼이 rpath 없이 미해결이던 것, 그리고 **h5bench 가 async API 를 부르는데 shim 이 동기 API 만 후킹하던 것**이다.

**마지막이 가장 컸고 그 뒤에 툴체인 문제가 드러났다.** OpenMPI 3.1.0 의 ROMIO 에는 Lustre 드라이버가 없어 MPI-IO 힌트가 전부 무시된다. Intel MPI 로 옮기고 `I_MPI_EXTRA_FILESYSTEM=1` 을 켜야 `cb_nodes` 와 `striping_factor` 가 산다. shim 과 h5bench 를 Intel 툴체인으로 다시 빌드했다.

**영향도 순위를 171점 실측으로 얻었다.** `striping_factor`(88.7) > `cb_nodes`(52.2) > `striping_unit`(34.8) > `chunk_cache`(25.9) 순이고 죽은 파라미터는 없다.

**뉴론 이식 조사를 마쳤다 (2026-09-02).** `05-뉴론-이식.md` 참조. 시스템 HDF5 가 전부 serial 이라 병렬 빌드가 필요하고, 공유 노드 정책 때문에 측정 노이즈 대응이 필요하다. Slurm 런처도 새로 만들어야 한다. 사흘 규모로 본다.

누리온 환경 상세는 `04-누리온-환경.md`, 뉴론은 `05-뉴론-이식.md` 에 있다.

| 구성요소 | 상태 |
|---|---|
| 공용 코어 (공간·설정파일·평가·목적함수·기록/재생·지표) | **완료** |
| 유전 알고리즘 (룰렛휠 / 토너먼트, 부분집합 마스킹) | **완료** |
| H5Tuner (공정 비교판) | **완료** |
| H5Tuner (원본 재현판) | **완료** |
| 조기 종료 — 휴리스틱 (논문의 비교 대상) | **완료** |
| 조기 종료 — 강화학습 (DQN) | **동작함. 합성 환경에서는 휴리스틱이 우세 — 보류** → §4.1 |
| 부분집합 선택 — 정적 순위 | **완료** |
| 부분집합 선택 — 강화학습 (문맥적 밴딧) | **완료** |
| PyTorch 없는 환경용 폴백 (random feature + 선형 헤드) | **완료** |
| **C shim 파라미터 5개 추가 (HDF5 1.14.1-2)** | **완료 (2026-08-13)** |
| **shim 버그 7건 수정** | **완료 (2026-08-13)** |
| **PBS Pro 런처 (`launchers.py`)** | **완료 (2026-08-25). 실기 미확인** |
| **h5bench 어댑터 (`apps.py`)** | **완료 (2026-08-25). 실기 미확인** |
| **반복 측정의 대역폭 집계 수정** | **완료 (2026-08-25)** |
| **I/O 커널 추출 (libclang) — TunIO 3번째 컴포넌트** | **완료 (2026-08-25). 실기 미확인** |
| **세대 단위 잡 배치 (`launch_many`)** | **완료 (2026-08-28)** |
| **MACSio 어댑터** | **완료 (2026-08-28). 실기 미확인** |
| **`configure.ac` MPI 라이브러리 자동 감지** | **완료 (2026-08-28)** |
| **shim 실기 빌드 (누리온, GCC/OpenMPI)** | **완료 (2026-08-28)** |
| **힌트가 Lustre에 반영되는지 확인** | **완료 (2026-09-01). Intel MPI 필요** |
| **shim async 훅 (`H5Fcreate_async` 등)** | **완료 (2026-09-01)** |
| **Intel 툴체인 빌드 (shim, mxml, h5bench)** | **완료 (2026-09-01)** |
| **실기 캠페인 — H5Tuner 121% 개선** | **완료 (2026-09-01)** |
| **실기 비교 — TunIO RoTI 26% 우위** | **완료 (2026-09-01)** |
| **뉴론(Slurm) 이식 조사** | **완료 (2026-09-02). 미착수** |
| **Slurm 런처** | **미착수** |
| **`test/` 프로그램 링크** | **깨짐. libtool `.la` 체인** → §4.2 |
| 대역폭 실측 (Darshan) | **완료 (2026-08-25). 실기 미확인** |

**결정된 것:** 세대 40 고정. 부분집합 표현은 파라미터별 점수 + 상위 k (후보 B). 조기 종료기는 단일 신경망 end-to-end (후보 A). RL은 PyTorch, 없으면 폴백.

---

## 1. 지금 돌아가는 명령

전부 실행 확인했다. MPI와 HDF5가 필요 없다.

```bash
# 프레임워크와 파라미터 공간 목록
python3 -m autotuner info

# H5Tuner — 합성 모델로 탐색만 확인
python3 -m autotuner run --framework h5tuner --dry-run --seed 1 \
    --generations 8 --population 10

# H5Tuner 원본 재현 — 벽시계 시간 최소화, 4차원, 13,440 조합
python3 -m autotuner run --framework h5tuner-original --dry-run --seed 1

# 격자를 쓸어 트레이스 기록 (실기에서는 --dry-run 대신 --app-cmd)
python3 -m autotuner sweep --space minimal --levels 3 --dry-run \
    --record traces/grid3.jsonl

# 트레이스에서 파라미터 영향도 순위 뽑기
python3 -m autotuner rank traces/grid3.jsonl --space minimal --out ranking.txt

# 기록된 격자 위에서 재생 — 앱 실행 0회
python3 -m autotuner run --framework h5tuner --space minimal --levels 3 \
    --replay traces/grid3.jsonl --seed 1 --generations 10

# TunIO 전체 (강화학습 정지기 + 강화학습 부분집합 선택기)
python3 -m autotuner run --framework tunio --space minimal --levels 3 \
    --replay traces/grid3.jsonl --ranking-from-trace traces/grid3.jsonl \
    --subset-size 3 --seed 1 --rl-seed 7

# ablation: 정지기만 휴리스틱으로 바꿔 강화학습의 기여를 분리
python3 -m autotuner run --framework tunio --space minimal --levels 3 \
    --replay traces/grid3.jsonl --ranking-from-trace traces/grid3.jsonl \
    --stopper heuristic --subset rl --subset-size 3 --seed 1 --rl-seed 7

# ablation: 부분집합만 정적 순위로 바꿔 학습의 기여를 분리
python3 -m autotuner run --framework tunio --space minimal --levels 3 \
    --replay traces/grid3.jsonl --ranking-from-trace traces/grid3.jsonl \
    --stopper rl --subset static --subset-size 3 --seed 1 --rl-seed 7

# 실기 실행
export LD_PRELOAD=/path/to/H5Tuner/lib/libautotuner.so
python3 -m autotuner run --framework h5tuner \
    --app-cmd 'mpiexec -n 32 ./my_app' \
    --perf-regex 'Write bandwidth: ([0-9.]+) MB/s'
```

`--subset rl`을 오프라인 prior 없이 실행하면 경고가 나온다. 논문은 파라미터 스윕으로 사전학습하므로, prior 없이 돌린 결과는 다른 실험이다.

조기 종료기는 실행 시작 시 합성 곡선으로 오프라인 학습을 한다. 폴백 백엔드로 약 30초 걸린다. `--rl-verbose`로 학습 추이를 볼 수 있다.

---

## 2. 완료된 파일

**공용 코어.** 세 프레임워크가 전부 이걸 통과한다. 측정 방식이 다르면 비교가 성립하지 않기 때문이다.

| 파일 | 하는 일 |
|---|---|
| [`autotuner/space.py`](autotuner/space.py) | 파라미터 공간. `paper12`(12개), `minimal`(현 shim이 주입 가능한 7개), `original`(원본 5유전자). `coarsen()`으로 격자를 조밀도 낮춤 |
| [`autotuner/config_writer.py`](autotuner/config_writer.py) | config.xml 생성. **원자적 쓰기** — 임시 파일에 쓰고 rename |
| [`autotuner/objective.py`](autotuner/objective.py) | `walltime`(최소화) / `bandwidth`(최대화). 앱 출력에서 대역폭 긁어내는 정규식 스크레이퍼 |
| [`autotuner/evaluate.py`](autotuner/evaluate.py) | 앱 실행·측정·캐시. live / replay / dry-run 네 모드. 합성 모델 포함 |
| [`autotuner/trace.py`](autotuner/trace.py) | JSONL 기록·재생 |
| [`autotuner/metrics.py`](autotuner/metrics.py) | RoTI, 세대별 이력, 요약 출력 |
| [`autotuner/ga.py`](autotuner/ga.py) | 유전 알고리즘. 룰렛휠(H5Tuner) / 토너먼트(TunIO), 부분집합 마스킹 |
| [`autotuner/early_stopping.py`](autotuner/early_stopping.py) | 정지 정책 + **합성 로그 곡선 생성기**(강화학습 학습 데이터) |
| [`autotuner/subset.py`](autotuner/subset.py) | 부분집합 선택 + 트레이스 기반 영향도 순위 |
| [`autotuner/rl/nets.py`](autotuner/rl/nets.py) | 함수근사기 두 개. PyTorch MLP / 의존성 없는 random feature + 선형 헤드 |
| [`autotuner/rl/features.py`](autotuner/rl/features.py) | 상태 인코딩. 전부 상대값이라 스케일과 목적 방향에 무관하다 |
| [`autotuner/rl/stopper.py`](autotuner/rl/stopper.py) | DQN 조기 종료기. 합성 곡선으로 오프라인 학습, 타깃 네트워크 + fitted-Q |
| [`autotuner/rl/picker.py`](autotuner/rl/picker.py) | 문맥적 밴딧 부분집합 선택기. 파라미터별 점수 + 상위 k |
| [`autotuner/frameworks/`](autotuner/frameworks/) | 프레임워크별 설정. `base.py`가 세대 루프를 공유한다 |
| [`autotuner/cli.py`](autotuner/cli.py) | `run` / `sweep` / `rank` / `info` |

**동작 확인한 것.**

유전 알고리즘이 수렴한다. 합성 지형에서 8세대에 299 → 1013 MB/s.

원본 재현판이 정확히 13,440 조합을 탐색한다. 원본의 14개 alignment 쌍을 그대로 쓴다.

config.xml이 `xml/config_lustre.xml`과 같은 구조로 나온다.

휴리스틱 정지기가 작동한다. 21세대에서 "5세대 동안 5% 개선 없음"으로 멈춘다.

기록 → 순위 → 재생 왕복이 된다. 2,187점 격자를 쓸고, 영향도 순위를 뽑고, 그 위에서 앱 실행 0회로 탐색한다.

영향도 순위가 온전성 검사를 통과한다. 합성 모델이 쓰지 않는 `alignment_threshold`가 효과 0.000으로 마지막에 온다.

정적 부분집합이 논문 방향대로 동작한다. 상위 3개만 튜닝하면 6세대에 1639 MB/s, 전부 튜닝하면 10세대에 1265 MB/s.

트레이스에 없는 설정을 만나면 명확한 오류를 낸다. 조용히 보간하지 않는다.

---

## 3. 구현 중 내린 결정 기록

논문이 명시하지 않은 지점들이다. **"TunIO를 재현했다"고 말할 때 근거가 되어야 하므로 전부 기록한다.** 뒤집을 수 있다.

### 3.1 파라미터가 12개인 근거

논문 §III-C는 이름을 11개만 열거하는데 §IV는 12개를 튜닝한다고 쓴다.

**HDF5 alignment의 threshold와 값을 따로 세어 12개를 맞췄다.** 이건 해석이지 사실이 아니다. 부수 효과가 하나 있다. 두 개로 나누면 독립적으로 변이할 수 있다. 원본처럼 `"threshold,alignment"` 문자열 하나면 불가능하다.

원본 재현판(`original` 공간)은 원래대로 문자열 쌍 하나를 쓴다.

### 3.2 부분집합에서 빠진 파라미터의 값

**개체가 지금 들고 있는 값을 그대로 유지한다.** 기본값으로 되돌리지 않는다.

근거는 논문 자신의 서술이다. §IV-D는 엘리티즘이 이전 세대의 최적 설정을 그대로 넘긴다고 말한다. 빠진 파라미터를 기본값으로 되돌리면 엘리티즘이 보존하려는 것을 바로 버리는 셈이다.

### 3.3 교차 연산의 범위

**부분집합 안에서만 교차한다.** 활성 위치들만 모아 그 안에서 단일점 교차를 한다.

전체 유전자를 교차하면 비활성 파라미터도 섞이므로 "이 부분집합만 튜닝한다"는 말과 모순된다.

### 3.4 반복 측정의 축약

**기본은 `best`다.** 최소화 목적이면 최소값, 최대화면 최대값을 쓴다.

`evolve.py`가 `min(times)`를 쓴 이유와 같다. 가장 빠른 실행이 콜드 캐시와 이웃 부하에 가장 덜 오염된다.

**논문은 3회 평균을 쓴다.** `--reduction mean`으로 재현할 수 있고, TunIO의 기본값을 `mean`으로 잡아뒀다.

### 3.5 변이율

**논문대로 유전자당 0.15 고정이다.**

파라미터가 21개면 자손 하나에 평균 3.15개가 변한다. 좋은 조합이 매번 깨지는 수준이다. 관례는 유전자 수의 역수다. 그래서 `--scale-mutation` 옵션을 뒀지만 **기본은 꺼져 있다.** 논문과 다른 동작을 기본값으로 두지 않기 위해서다.

### 3.6 영향도 순위 계산법

**일원 효과 크기를 쓴다.** 파라미터 값별로 평균 성능을 내고 그 평균들의 표준편차를 효과로 본다.

**논문의 PCA와 다르다.** §III-C는 파라미터에 대해 perf 기준 PCA를 한다고 하지만, 성분 로딩을 파라미터별 순위로 바꾸는 방법을 밝히지 않는다. 그리고 입력만의 PCA는 목표에 대한 정보를 담지 않는다. 일원 효과는 "어느 knob이 목적함수를 움직이는가"라는 질문에 직접 답하고 의존성이 없다.

**한계:** 상호작용을 못 본다. 둘이 함께일 때만 의미 있는 파라미터 쌍은 둘 다 무력해 보인다.

### 3.7 부분집합 크기 일정

**기본은 자유 파라미터의 1/3을 올림한 고정 크기다.** `--subset-grow-every`로 점진 확대도 된다.

논문은 부분집합을 고르고 영향도 순으로 정렬한다고만 말한다. 크기나 일정을 밝히지 않는다. **둘 다 우리 선택이다.**

### 3.8 `chunk_cache`와 `mdc_conf`의 축소

`chunk_cache`는 `rdcc_nbytes` 한 차원만 튜닝한다. `H5Pset_cache`는 인자가 4개인데 나머지는 HDF5 기본값에 둔다.

`mdc_conf`는 `default` / `aggressive` / `conservative` 세 프리셋으로 만들었다. `H5AC_cache_config_t`는 필드가 20개가 넘는다.

**논문은 이 파라미터들이 어떤 형태로 튜닝됐는지 밝히지 않는다.**

### 3.9 후보 값 목록 전체

**논문은 파라미터 이름만 주고 값 목록을 주지 않는다. 아래 목록은 전부 우리 것이다.**

`evolve.py`에 있던 것은 그대로 가져왔다. 그래서 일부가 깔끔한 2의 거듭제곱이 아니다. 바꾼 것은 셋이다.

`sieve_buf_size`는 512MB 단일값에서 64KB~2MB 6개로 되살렸다. 원본의 주석 처리된 목록은 `65535` 계열, 즉 2¹⁶−1의 배수였다. 정렬용 버퍼로는 의미가 없다. 원본의 `NEW, NOT FOR IPDPS` 줄이 고친 2의 거듭제곱 값을 썼다.

`alignment`는 상한을 256KB에서 4MB로 올렸다. `striping_unit` 하한이 1MB이므로 원래 범위로는 스트라이프 경계 정렬이 불가능했다.

`cb_buffer_size`는 `striping_unit`에 묶인 상태에서 독립 유전자로 되살렸다. 값은 원본의 주석 처리된 목록을 썼다.

**결과 공간 크기는 4.48억이다.** 논문의 21.8억보다 작다. 파라미터당 평균 후보가 5.26개 대 5.9개다. 억지로 맞추면 가짜 정밀도가 되므로 실제 값을 그대로 보고한다.

### 3.10 재생하지 않는 원본 버그

원본 재현판에서도 **일부러 재현하지 않은 것**이 둘 있다.

`config.xml` 경로 결합 누락이다. `home_dir + 'config.xml'`로 구분자가 없어서 홈 디렉터리 옆에 파일이 떨어졌고 shim은 그걸 본 적이 없다. 재현하면 모든 후보가 같은 노이즈를 측정한다.

정리가 실행되지 않던 버그다. `Popen(["cmd", "rm SDS.h5"], shell=True)`는 첫 원소만 실행하므로 출력 파일이 후보 사이에 남았다.

둘 다 설계가 아니라 결함이므로 재현 대상이 아니다.

---

### 3.11 부분집합 표현 — 파라미터별 점수 + 상위 k

**신경망은 파라미터마다 "지금 얼마나 중요한가" 점수 하나를 내고, 상위 k개를 튜닝한다.** 2¹²=4,096개 부분집합 중 하나를 고르는 방식이 아니다.

이유는 세 가지다. 첫째, 논문 §III-C의 "부분집합을 반환하고 영향도 내림차순으로 정렬한다"가 정확히 점수 매기고 정렬하는 구조다. 둘째, 논문의 오프라인 사전학습(스윕으로 파라미터별 영향도 추출)이 곧 이 점수를 만드는 것이므로 사전학습 결과가 갈 곳이 있다. 셋째, `--subset static`과 `--subset rl`이 같은 메커니즘을 공유하고 순위의 출처만 달라지므로 **강화학습의 기여를 분리 측정할 수 있다.**

**잃는 것:** 부분집합 크기 k를 학습하지 못한다. 논문도 크기를 밝히지 않으므로 설정값으로 뒀다.

**확장성:** 파라미터를 30개로 늘려도 학습 대상이 30개다. 평면 2ⁿ 표현이면 10억이 된다.

### 3.12 신경망은 하나로 합쳤다

**논문의 State Observer와 Action Decider를 한 신경망으로 합쳐 end-to-end 학습한다.** 은닉층이 상태 표현이고 출력층이 결정이다. 표준 DQN이다.

논문은 두 컴포넌트를 분리해 서술하지만 각각의 구조를 밝히지 않는다. 분리해서 만들려면 Observer의 학습 목표를 우리가 또 정해야 하므로, **충실하려다 추측을 하나 더 하는 결과**가 된다. 이 규모(상태 8차원, 행동 2개)에서는 구조가 정책을 바꾸지 않는다.

### 3.13 부분집합 선택기의 학습을 밴딧으로 한 이유

**한 세대가 끝나면 그 세대에 튜닝했던 파라미터들의 점수를 그 세대의 보상 쪽으로 당긴다.** 1스텝 값 갱신이므로 문맥적 밴딧이다.

논문은 Subset Picker를 Q-learning이라 하고 State Observer를 문맥적 밴딧이라 한다. 밴딧으로 구현하면 논문이 이름 붙인 두 조각이 하나의 일관된 알고리즘이 된다. 다단계 Q-learning으로 만들면 Observer의 자리가 사라진다.

보상은 논문의 형태를 따른다. `상대 개선 − size_penalty × (부분집합 크기 / 전체 파라미터 수)`, 5회차 지연. 두 항의 가중치는 논문에 없어 `--size-penalty` 기본값 1.0(논문의 단순 차이)으로 뒀다.

**다만 두 군데가 논문과 다르고, 2026-08-12 재독에서 확인했다.**

**(1) 논문은 성능의 "수준"을 쓰고 우리는 "개선폭"을 쓴다.** §III-C의 원문은 `normperf(perf) − normparam(...)`이고 `perf`는 절대 대역폭이다. 우리는 baseline 대비 상대 개선을 쓴다.

의도적 선택이다. 수준을 쓰면 **나중 회차가 그냥 나중이라는 이유로 높은 보상을 받는다** — best-so-far가 단조 증가하므로 후반 회차는 어떤 부분집합을 골랐든 높은 perf를 본다. 부분집합의 좋고 나쁨을 배우려면 그 교란이 없어야 한다. 개선폭은 이 문제가 없다.

논문에 충실한 arm이 필요하면 플래그 하나로 추가할 수 있다. 필요한 것은 `BW_single` 값 하나다.

**(2) `BW_single`은 "정의되지 않았다"고 적었는데, 그건 과했다.** §III-C 원문은 "The perf is normalized using the constant 1/(BW_single × num_nodes)"다. `BW_single × num_nodes`는 **완전 선형 확장을 가정한 이상적 총 대역폭**이다. 그러니 `norm_perf`는 *이상적 최대치의 몇 %를 달성했는가*이고 대략 [0,1]에 든다.

의미는 명확하다. 없는 것은 **값**이고, 단일 노드 측정 한 번으로 얻는다. 앞선 기록(§7의 미명시 목록)에서 이 항목을 완화해야 한다.

### 3.14 오프라인 사전학습이 정책에 도달하는 경로

**스윕에서 뽑은 영향도 순위를 두 가지로 쓴다.** 하나는 신경망 입력 특징이다. 다른 하나는 warm-start 회귀 목표다 — 캠페인이 시작될 때 신경망의 순위가 스윕의 순위와 같아지도록 미리 맞춘다.

논문 §III-C는 PCA로 영향도를 뽑는다고 하지만 **그 결과를 초기 정책으로 옮기는 방법을 밝히지 않는다.** 회귀로 맞추는 것이 가장 직접적이다. 그리고 prior를 입력 특징으로도 넣었으므로, 캠페인의 실제 관측이 prior와 어긋나면 신경망이 prior를 깎아내릴 수 있다.

### 3.15 PyTorch 없는 환경용 폴백

**은닉층을 무작위로 고정하고 선형 헤드만 학습한다.** random feature 근사다.

이 맥에는 torch도 numpy도 없어서 **실제로 이 경로가 돌아가는 경로다.** 형식적 stub이 아니라 같은 데이터·같은 보상·같은 루프로 학습한다. 다만 MLP보다 약한 근사기이므로 `BACKEND`를 보고에 넣었다.

학습률이 백엔드마다 다르다. Adam+MLP는 1e-3, 선형 헤드+SGD는 5e-2다. 1e-3으로 폴백을 돌리면 수백 라운드가 필요하다.

### 3.16 fitted-Q에 타깃 네트워크가 필요했다

처음에는 타깃 네트워크 없이 만들었더니 **손실이 단조 증가하고 정책이 "항상 계속"으로 붕괴했다.** 계속의 가치가 자기 자신의 목표로 되먹임되기 때문이다.

이제 매 라운드 신경망을 복사해 얼려두고 그 사본으로 목표를 계산한다. 교과서적 처방이지만, 안 넣으면 조용히 실패하는 종류라 기록해둔다.

## 4. 막힌 것 — 결정이 필요하다

### 4.2 `test/` 프로그램이 누리온에서 링크되지 않는다 (2026-08-28)

**증상.** `make`가 `src/`까지는 정상이고 `test/`에서 멈춘다.

```
libtool: link: cannot find the library `/opt/pbs/lib/libpbs.la'
```

**원인.** 누리온 OpenMPI의 `libmpi.la`가 의존성으로 `/opt/pbs/lib/libpbs.la`를 적어놨는데 그 파일이 없다. `libpbs.so`는 있다. libtool이 `.la` 체인을 따라가다 막힌다.

**시도했지만 안 된 것.** `CC=gcc`로 바꾸기, `AM_MPI_LIB`를 비우기, HDF5/MPI/mxml을 절대 경로 `.so`로 주기. 셋 다 우회되지 않았다.

**영향은 제한적이다.** `libautotuner.so`는 정상적으로 빌드된다. 막히는 것은 파라미터 주입을 확인하는 15개 테스트다.

**대안이 오히려 강하다.** MACSio를 직접 돌리고 `lfs getstripe`로 보면 "HDF5가 값을 받았다"가 아니라 "파일시스템이 적용했다"까지 확인된다.

**다시 볼 때의 해법 후보.** libtool 없이 테스트를 수동 컴파일하거나, `test/Makefile.am`이 libtool을 거치지 않도록 바꾸거나, 사이트에 `libpbs.la` 생성을 요청한다.

### 4.1 조기 종료기의 보상 설계 — **보류 결정 (2026-08-12)**

**결정: 지금은 고치지 않고 넘어간다.** 근거는 현재 시험장이 합성 환경이라는 것, 그리고 실기 HPC에서는 조기 종료가 유리하게 나올 개연성이 있다는 것이다.

아래는 무엇을 보류했는지의 기록이다. 실기 실행 후 다시 볼 항목이다.

#### 관측된 것

합성 곡선 200개에서 두 정지기를 비교했다. 점수는 "최적 정지 시점 대비 달성 return 비율"이다.

| 곡선 종류 | 강화학습 | 휴리스틱 | 안 멈춤 |
|---|---|---|---|
| 매끄러운 로그 곡선 | 0.939 | **0.987** | — |
| 중간에 정체 구간 있음 | 0.871 | **0.920** | — |
| 혼합 (기본 분포) | 0.909 | **0.953** | 0.839 |

강화학습이 휴리스틱보다 나은 곡선은 200개 중 72개다.

**학습은 정상이다.** 평균 정지 시점이 39/40에서 25/40으로 내려가고, 안 멈추는 정책(0.839)보다 낫다. 논문의 정지 지점(50세대 중 35, 70%)과 비슷한 62% 지점에서 멈춘다.

#### 논문 충실도는 어디까지인가

논문이 **명시한** 것은 전부 구현되어 있다. 로그 곡선, 아래로 향하는 노이즈, 가상 앱마다 다른 초기값과 성장률, 5회차 지연 보상, 정체 시 학습 종료. 워밍업 하한(§3.16 옆)만 우리가 추가했고 그 이유도 기록되어 있다.

**보상 함수는 논문에 없다.** §III-D는 "최근 회차에 측정 가능한 개선이 없을 때 멈춘다", "성능 이득과 튜닝 시간의 균형을 맞춘다"까지만 말하고 함수를 주지 않는다. 그래서 `상대 개선 − step_cost`는 **우리 것이고, 충실/불충실을 따질 대상이 아니다.** 결과를 만들어내는 부분이 바로 이 미명시 지점이다.

**회차당 비용이 일정하다는 가정도 논문에 없다.** 우리 가정이다.

#### 남는 위험

**합성 곡선은 평가장이 아니라 학습 데이터다.** 정지기는 이 곡선들로 학습한 정책을 그대로 들고 실기에 나간다. 즉 "합성 환경이라 괜찮다"는 판단은 *평가*에는 맞지만 *학습*에는 그대로 적용되지 않는다. 균등 비용 가정이 정책에 새겨진 상태로 HPC에 올라간다.

실기에서 이게 어떻게 드러날지: 실제로는 나쁜 설정이 느리게 실행되므로 초반 회차가 비싸다. 그러면 계속하는 것의 실제 이득이 학습 때 본 것보다 크다. **정지기가 필요 이상으로 일찍 멈출 방향으로 편향될 것으로 예상한다.**

#### 재독 확인 (2026-08-12)

**논문에 조기 종료기의 보상 함수는 없다.** 원문과 사용자 하이라이트 30개를 모두 확인했다. §III-D가 주는 것은 셋뿐이다. "Smart Configuration Generation과 유사한 RL 기법으로 구현했다", "5회차 지연 보상을 쓴다", 그리고 절 제목이 **"Balancing Tuning Cost and Benefit"**이라는 것.

**이 재독은 우리 방향을 뒤집지 않고 오히려 뒷받침한다.** §III-D 본문이 "성능 이득과 튜닝에 쓴 **시간**의 균형을 맞춘다"고 명시한다. 즉 `이득 − 시간 비용`이 논문의 서술된 의도다. 우리 구조(`개선 − step_cost`)가 그 의도와 맞고, 빠진 것은 **step_cost가 고정 상수가 아니라 실제 소요 시간에 비례해야 한다**는 점이다. 아래 해법이 정확히 그것이다.

부수적으로, 부분집합 선택기의 보상은 `이득 − 정규화된 부분집합 크기`다. 크기가 그쪽의 비용 대리 지표다. 정지기의 대응물이 시간이라는 읽기는 두 컴포넌트를 같은 형태로 놓는다.

#### 다시 볼 때의 해법

합성 곡선의 회차 비용을 성능의 역수에 비례하게 두고 보상을 ΔRoTI로 되돌린다. 나쁜 설정은 느리다는 물리적 사실이고, 논문이 실제로 보고하는 지표를 그대로 목적함수로 쓸 수 있다. 휴리스틱은 비용을 전혀 안 보므로 더 이상 최적이 아니다.

논문 §IV-C가 "완벽한 RoTI는 35회차에 멈추면 달성된다"고 쓰는데, 회차 비용이 일정하면 RoTI는 아주 이른 회차에서 최대가 된다. **비용이 일정하지 않다는 것이 이 모순을 설명하는 유일한 후보다.** 우리 평가기(`evaluator.tuning_seconds`)와 dry-run 합성 앱은 이미 비용을 성능에 연동해 재고 있다. 균등 비용을 가정한 곳은 정지기의 합성 곡선뿐이다.

반나절 규모의 수정이다. **실기 트레이스가 확보되면, 합성 곡선 대신 그 트레이스에서 관측된 실제 비용 분포로 학습하는 편이 더 낫다.**

## 5. 남은 작업

**의존 관계가 있는 축이 하나 있고, 나머지는 독립이다.**

```
[A] C shim 파라미터 5개 (+ HDF5 1.10 상향)
        ↓  없으면 paper12 스윕이 5개를 "효과 없음"으로 잘못 학습시킨다
[B] 승격된 기존 버그 4건
        ↓  없으면 수천 회 재실행에서 측정이 오염된다
[C] 실기 스윕 → 트레이스 기록
        ↓  이게 병목을 푼다. 이후 작업은 다시 이 맥에서 된다
[D] picker prior 교체 (무료) + stopper 재학습 (보상 비용 항 수정)
        ↓
[E] H5Tuner vs TunIO 비교 실험

독립 작업: [F] I/O Discovery  [G] Darshan  [H] 단위 테스트  [I] 구현 매뉴얼
```

### 완료

~~**강화학습 두 컴포넌트 구현.**~~ **완료 (2026-08-11).** `rl/picker.py`, `rl/stopper.py`. 폴백 포함. 설계 결정은 §3.11~§3.16.

~~**Fig 10(a) 실패 사례 재현.**~~ **부분 완료.** 정체 구간이 있는 곡선을 생성하고 두 정지기를 비교했다. **결과는 예상과 반대였다** — 휴리스틱이 이겼다. 원인과 보류 결정은 §4.1. 남은 것은 **실기 트레이스에서 같은 비교를 하는 것**이다.

### ~~[A] C shim에 파라미터 5개 추가~~ — **완료 (2026-08-13)**

**HDF5 1.14.1-2 기준으로 구현했다.** 대상 클러스터에 설치된 버전이다(parallel ON, Fortran/C++ 바인딩 OFF).

**`src/autotuner_hdf5.c`.** `set_hdf5_parameter()`의 분기 체인에 5개를 추가하고 `H5Fcreate` 훅에 호출 5줄을 추가했다.

| 파라미터 | 호출 | 비고 |
|---|---|---|
| `meta_block_size` | `H5Pset_meta_block_size` | `strtoull`로 파싱. 기존 `atoi`와 달리 오버플로 없음 |
| `chunk_cache` | `H5Pget_cache` → `H5Pset_cache` | `rdcc_nbytes`만 바꾸고 슬롯 수·축출 정책은 property list의 기존 값을 유지 |
| `coll_metadata_write` | `H5Pset_coll_metadata_write` | **HDF5 1.10+.** 미만이면 `#if`로 컴파일에서 빠지고 런타임 경고 |
| `col_meta_ops` | `H5Pset_all_coll_metadata_ops` | 동일 |
| `mdc_conf` | `H5Pget_mdc_config` → 수정 → `H5Pset_mdc_config` | 프리셋 3종. **get 후 크기 필드만 덮는다** — 구조체 필드가 20개 넘고 상호 제약(min ≤ initial ≤ max)이 있어서, 안 건드린 필드는 HDF5가 이미 유효하다고 본 값을 유지한다 |

**함께 고친 것.**

**빈 엘리먼트 가드.** `<sieve_buf_size/>`처럼 텍스트 자식이 없는 노드는 모든 분기에서 `node->child->value.text.string`을 역참조해 **앱을 죽였다.** 정적 shim에는 체크가 있었고 동적 shim에만 없었다. 이제 경고 후 건너뛴다.

**`.misc/config.sh`.** HDF5를 1.8.12 → 1.14.1-2로 올렸다. 다운로드 URL이 죽은 경로(`hdfgroup.org/ftp`, `msweet.org/files/project3`)여서 GitHub 릴리스를 1순위, 기존 경로를 폴백으로 두고 **검증 필요 주석**을 달았다.

그리고 이 스크립트는 **원래부터 `bash -n`을 통과하지 못했다.** `if` 7개 `fi` 6개로, NetCDF4 블록의 `fi`가 빠져 있어 스크립트 끝의 `else`가 엉뚱한 `if`에 붙었다. 제가 건드린 파일이므로 함께 고쳤다.

**XML 템플릿 4개** (`xml/config.xml`, `config_lustre.xml`, `config_chunking.xml`, `config_chunking_IBM.xml`)에 신규 파라미터 예시를 넣었다. `config_gpfs.xml`은 HDF5 섹션이 없어 건드리지 않았다.

**새 테스트 `test/test_hdf5_metadata_params.c`** (약 200줄). 기존 방식을 따르지 않았다 — 저장소의 14개 테스트는 `ph5example.c`를 복제한 것이고 16,800줄에 고유 로직이 600줄이다. **5개 파라미터가 모두 `H5Fcreate` 한 지점에서 주입되므로 한 프로그램이 전부 검증할 수 있고, 실제 병렬 쓰기 워크로드가 필요하지 않다.** 1,300줄 대신 200줄이다. `test/Makefile.am`의 `TEST_PROG`에 등록했다.

`test/config.xml.in`에 기대값을 넣었다. `mdc_conf`는 일부러 `aggressive`로 두어 **프리셋 경로가 실행되게** 했다(`default`는 아무것도 안 하는 분기다).

**파이썬 쪽.** `space.py`에서 5개를 `shim=True`로 바꿨다. 그러면 `minimal` 공간이 `shim` 필터로는 `paper12`와 같아지므로, **7개를 명시 목록으로 재정의**했다(빠른 실행과 격자 스윕용). `--hdf5-version` 옵션을 추가해 실행 전에 버전 부족을 경고한다.

**검증한 것.** 이 맥에 HDF5·MPI가 없어 실제 컴파일은 못 했다. 스텁 헤더로 문법 검증했고 **1.14.1과 1.8.12 양쪽 경로 모두 통과**한다. XML 5개 파싱 확인, `bash -n` 통과 확인, 파이썬 회귀 확인.

**남은 것.** 실기에서 `./autogen.sh && ./configure && make && make check`. 특히 `H5AC_cache_config_t`의 필드 이름과 `H5Pget_all_coll_metadata_ops` 시그니처는 1.14.1 헤더로 실제 컴파일해야 확정된다.

### ~~[A-old] 원래 계획~~

`meta_block_size`, `chunk_cache`, `coll_metadata_write`, `col_meta_ops`, `mdc_conf`.

고칠 곳은 두 군데다. [`src/autotuner_hdf5.c:123-176`](src/autotuner_hdf5.c#L123-L176)의 `strcmp` 분기 체인에 항목을 추가하고, [`:264-265`](src/autotuner_hdf5.c#L264-L265)에 호출을 한 줄씩 추가한다.

**선행 조건.** [`.misc/config.sh:24`](.misc/config.sh#L24)가 `hdf5-1.8.12`를 고정한다. `coll_metadata_write`와 `col_meta_ops`는 HDF5 1.10.0에서 도입되었으므로 1.8.12에서는 컴파일되지 않는다. 버전을 올리고 새 호출을 `#if H5_VERSION_GE(1,10,0)`으로 감싼다. 같은 파일의 다운로드 URL이 2013년경 경로라 살아 있는지도 확인해야 한다.

**왜 먼저인가.** 이게 안 되면 `paper12` 공간을 실기에서 돌려도 12개 중 5개가 조용히 무시된다. 그 트레이스로 영향도 순위를 뽑으면 **5개를 "효과 없음"으로 잘못 배운다.** 즉 [C]와 [D]가 오염된다. CLI가 경고는 하지만 경고로 해결되는 문제가 아니다.

**함께 할 일.** `xml/` 템플릿에 신규 파라미터 예시를 넣고, `test/config.xml.in`에 기대값을 추가하고, §7.2 스타일의 되읽기 테스트를 파라미터별로 만든다.

### ~~[B] 승격된 기존 버그 4건~~ — **완료 (2026-08-13)**

네 건 모두 고쳤고, 고치는 과정에서 **더 심각한 것 세 개를 추가로 찾았다.**

#### 원래 목록

**1. `fopen` 결과를 검사하지 않았다.** 스트림이 그대로 XML 파서로 갔다. `config.xml`이 없으면 앱이 죽었고, **튜너가 다시 쓰는 중인 파일을 읽어도 죽었다** — 캠페인이 수천 번 하는 일이다. 이제 실패 시 **경고 후 튜닝 없이 파일을 만든다.** 앱을 깨뜨리는 shim이 아무것도 튜닝하지 않는 shim보다 나쁘기 때문이다.

**2. `AT_CONFIG_FILE`을 되살렸다.** 경로가 프로세스 작업 디렉터리에 하드코딩되어 있었다. 그래서 랭크마다 자기 cwd에 `config.xml` 사본이 필요했고, **후보를 동시에 평가하는 것이 원천적으로 불가능했다.** 이제 환경변수로 지정할 수 있다.

**3. 비-MPIO 드라이버에서 원본을 호출하지 않았다.** 메시지를 찍고 `-1`을 반환했다. 즉 직렬 HDF5 앱에 `LD_PRELOAD`를 걸면 **튜닝이 안 되는 게 아니라 파일 생성이 실패했다.** 슬라이서가 `H5Pset_fapl_mpio`를 떨어뜨렸을 때 I/O 커널이 걸리는 함정도 이것이다.

이제 **HDF5 계층 파라미터는 양쪽 다 적용한다.** property list 설정이라 MPI가 필요 없기 때문이다. MPI-IO 힌트·파일시스템 힌트·collective 메타데이터 스위치만 건너뛴다.

**4. `bglockless:` 프리픽스가 무효였다 — 그리고 무효보다 나빴다.** `set_gpfs_parameter`가 프리픽스된 이름을 지역 버퍼에 만든 뒤 **`strcpy(filename, new_filename)`으로 호출자의 문자열에 썼다.** 그 문자열은 `const`이고 쓰려는 내용보다 11바이트 짧다. **버퍼 오버플로다.** 게다가 `H5Fcreate`는 자기 `filename`을 그대로 실제 호출에 넘기므로 결과가 반영되지도 않았고, malloc한 버퍼는 누수됐다.

`IBM_lockless_io`가 거의 `true`가 아니라서 터지지 않았을 뿐이다. 이제 `gpfs_lockless_filename()`이 호출자가 준 버퍼에 쓰고 크기를 검사하며, `H5Fcreate`가 그 이름으로 파일을 만든다.

#### 고치면서 추가로 찾은 것

**5. `H5Dcreate1`과 `H5Dcreate2`에 1·2번 버그가 그대로 있었다.** 경로 하드코딩, `fopen` 미검사, `fclose` 없음, `mxmlDelete` 없음. **데이터셋마다 호출되므로 누수가 `H5Fcreate`보다 심하다.**

세 훅이 각자 복사본을 갖고 있었으므로 **`at_load_config()` 헬퍼로 뽑았다.** 한 구현으로 세 곳이 고쳐진다.

**6. `MPI_Info` 객체가 매 `H5Fcreate`마다 누수됐다.** `H5Pget_fapl_mpio`는 호출자가 소유하는 **사본**을 돌려주고 `H5Pset_fapl_mpio`는 자기 사본을 만든다. 어느 쪽도 해제되지 않았다. `MPI_Info_free`를 추가했다.

**7. `sieve_buf_size`와 `alignment`가 `H5Pset_*` 반환값을 버렸다.** `int ierr = H5Pset_...()` 후 `ierr`를 보지 않았다(컴파일러 경고로 드러났다). 주입 실패가 조용히 지나가는 것은 이 실험에서 정확히 피해야 하는 실패 모드다. 이제 확인하고 경고한다. 파싱도 `atoi`에서 `strtoull`로 바꿨다.

#### 검증

스텁 헤더로 문법 검증, **HDF5 1.14.1과 1.8.12 양쪽 경로 모두 `-Wall` 경고 없이 통과.** 실기 컴파일은 여전히 남아 있다.

**행동 변화 주의.** 3번은 동작이 바뀌는 수정이다. 이전에는 비-MPIO 앱에서 파일 생성이 실패했고 이제는 성공한다(HDF5 파라미터만 적용). 의도한 것이지만 기존 동작에 의존한 스크립트가 있다면 영향을 받는다.

### [C] 실기 스윕 → 트레이스 기록

**이 하나가 병목을 푼다.** 클러스터에서 한 번 돌리면 이후 작업 대부분이 이 맥으로 내려온다.

```bash
export LD_PRELOAD=/path/to/lib/libautotuner.so
python3 -m autotuner sweep --space paper12 --levels 2 \
    --app-cmd 'mpiexec -n 128 ./kernel' \
    --perf-regex '...' --record traces/paper12-L2.jsonl
```

`--levels 2`면 격자가 4,096점이다. 후보당 1분이면 약 68시간이므로 배치 잡으로 쪼개거나 `--samples`로 무작위 표본을 쓴다. **CAOS 랩 서버나 HPC 클러스터가 필요하다.**

### [D] picker prior 교체 + stopper 재학습

**picker는 코드 변경 0이다.** `--ranking-from-trace traces/paper12-L2.jsonl`로 실기 순위를 넣으면 된다.

**stopper는 손봐야 한다.** 보상의 `step_cost`를 고정 상수에서 회차별 실제 소요 시간에 비례하게 바꾼다. 그리고 합성 곡선 대신 실기 트레이스에서 관측된 곡선·비용 분포로 학습한다. 근거와 상세는 §4.1. 반나절 규모.

### [E] H5Tuner vs TunIO 비교 실험

세 프레임워크 + ablation arm들을 같은 트레이스 위에서 재생으로 돌린다. 지표는 RoTI, 목표 대역폭 도달 회차, 총 예산.

**여기서 세대 수는 40으로 통일되어 있다.** 논문의 50을 쓰면 TunIO에 25% 많은 평가를 주게 되므로 쓰지 않는다.

### [F] I/O Discovery — **범위 외로 둘지 판단 필요**

계획서 §4.4다. 파이썬 800~1500줄로 남은 작업 중 가장 크다.

**빼고 가는 것도 정당하다.** 탐색을 개선하는 기법이 아니라 평가를 싸게 만드는 기법이고, 세 프레임워크 모두 전체 앱으로 평가하면 탐색 비교는 오히려 깔끔하다. "TunIO의 평가 비용 절감은 범위 외"라고 명시하면 된다.

**하기로 하면 루프 축약을 먼저 떼어낸다.** 논문 수치로 RoTI 2.47 → 23.30이 축약에서 나오고 AST 슬라이싱 단독은 2.87뿐이다. 축약은 앱에 반복 인자를 노출하는 것으로 시작할 수 있다.

### [G] Darshan 기반 대역폭 측정

지금은 정규식 스크레이핑만 된다. 논문은 Darshan을 쓴다. `LD_PRELOAD`에 Darshan과 `libautotuner.so`를 함께 넣을 때 후킹이 충돌하는지 확인이 필요하다. 읽기가 섞인 워크로드에서 α를 얻으려면 필요하다.

### [H] 단위 테스트

지금은 CLI를 직접 돌려 확인했다. 최소한 이 셋은 필요하다.

부분집합 마스킹이 비활성 유전자를 정말 건드리지 않는지. 재생이 결정론적인지(같은 seed → 같은 탐색 경로). config.xml 원자적 쓰기.

### [I] 구현 매뉴얼

**요청받은 것.** 설계 포인트와 선택 이유를 PLAN 문서 형식으로 한데 모은다. 지금은 §3의 16건과 각 모듈 docstring에 흩어져 있다.

**[D]가 끝난 뒤에 쓰는 것이 맞다.** 보상 설계가 바뀌면 매뉴얼의 핵심 절이 바뀌기 때문이다.

## 6. 열린 질문 (결정 대기는 아니지만 기록)

`--levels`로 격자를 조밀도 낮추는 방식이 최선인지. 재생이 성립하려면 탐색이 기록된 격자 위에 있어야 한다. 지금은 파라미터마다 균등하게 값을 골라낸다. 영향 큰 파라미터에 더 많은 레벨을 주는 편이 나을 수 있다.

`bandwidth` 목적함수에서 α를 어떻게 얻을지. 지금은 바이트 수를 모르면 1.0(순수 쓰기)으로 둔다. 이 저장소의 모든 워크로드가 쓰기이므로 맞지만, 읽기가 섞이면 Darshan이 필요하다.

`h5tuner-original`의 `striping_factor` 목록에 `4`를 남겨둔 것. 원본 주석은 "1과 4는 매우 나쁘므로 제외"라고 쓰고 목록에 4를 넣었다. 원본에 충실하게 그대로 뒀다. 주석이 틀렸는지 목록이 틀렸는지 알 수 없다.

~~TunIO의 세대 수.~~ **결정됨 (2026-08-11): 40세대.** 논문 §IV-C의 HACC 실험은 50세대지만, 50을 쓰면 TunIO에 베이스라인보다 25% 많은 평가 기회를 주게 되어 비교가 오염된다. H5Tuner와 같은 40으로 고정했다.
