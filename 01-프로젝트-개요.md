# 01 — 프로젝트 개요

> **이 문서는 무엇인가.** 처음 오신 분이 **가장 먼저 읽는 문서**다. 이 프로젝트가 무엇을 하는지, 지금 어디까지 왔는지, 나머지 문서를 어떤 순서로 읽어야 하는지를 담았다.
>
> **읽는 데 15분.** 여기까지만 읽어도 대화는 된다.

---

## 1. 한 줄로

**HPC 응용의 병렬 I/O 설정값을 자동으로 찾아 주는 도구를 만들고 있고, 그 비교 기준선을 확보하는 단계다.**

---

## 2. 무슨 문제인가

슈퍼컴퓨터에서 큰 데이터를 파일로 쓸 때, 성능은 **설정값 몇 개에 크게 좌우된다.** 코드를 바꾸지 않아도 그렇다.

예를 들어 이런 것들이다.

- 파일을 **몇 개의 디스크에 나눠 쓸지** (`striping_factor`)
- 한 조각을 **얼마나 크게 자를지** (`striping_unit`)
- 쓰기를 **몇 개 프로세스가 대표로 모아서 할지** (`cb_nodes`)

우리가 실제로 측정한 예를 하나 들면, 같은 프로그램·같은 데이터인데 **설정만 바꿔서 260 MB/s 가 570 MB/s 가 됐다. 2.2 배다.** 코드는 한 줄도 안 고쳤다.

**문제는 좋은 설정값을 아무도 모른다는 것이다.** 세 가지 이유 때문이다.

**첫째, 경우의 수가 너무 많다.** 우리가 다루는 파라미터는 12 개고 조합은 **4억 4 천만 가지**다. 하나씩 다 돌려볼 수 없다.

**둘째, 파라미터끼리 얽혀 있다.** 하나씩 따로 최적값을 찾아 합치면 최적이 안 된다. 실제로 겪었다. `striping_factor` 를 혼자 재면 8 이 제일 좋은데, `striping_unit` 을 같이 탐색하면 **4** 가 이긴다.

**셋째, 정답이 상황마다 다르다.** 응용이 바뀌면, 프로세스 수가 바뀌면, 데이터 모양이 바뀌면 최적값이 바뀐다. 우리 측정에서 최적 스트라이프가 1 차원 배열에서는 `4MiB × 4`, 3 차원에서는 `1MiB × 1` 이었다. **한 번 찾아서 문서에 적어 두는 방식이 통하지 않는다.**

그래서 **자동으로 탐색하는 도구**가 필요하다. 그게 오토튜너다.

---

## 3. 이 저장소가 하는 일

**기존 연구 두 개를 같은 바닥 위에 다시 구현했다.**

| | 무엇인가 | 방법 |
|---|---|---|
| **H5Tuner** | HDF Group 이 만든 원조 도구 | 유전 알고리즘으로 12 개를 전부 탐색 |
| **TunIO** | 2024 년 IPDPS 논문 | 영향 큰 파라미터 4 개만 골라 탐색하고, 더 나아질 게 없어 보이면 조기 종료 |

**왜 논문 숫자를 인용하지 않고 다시 만들었나.** 두 논문은 다른 기계에서 다른 응용을 다른 방식으로 쟀다. 그 숫자를 나란히 놓으면 차이가 **알고리즘 때문인지 환경 때문인지 구분되지 않는다.**

그래서 **같은 파라미터 공간, 같은 목적함수, 같은 측정 방법, 같은 예산**으로 맞춰서 다시 구현했다. 그래야 차이를 알고리즘 탓으로 설명할 수 있다.

**최종 목표는 이 둘보다 나은 새 프레임워크를 만드는 것이다.** 지금은 그 비교 기준선을 만드는 단계다.

---

## 4. 어떻게 동작하나

두 덩어리다.

### (1) 파라미터를 주입하는 C 라이브러리 — `src/`

**응용을 다시 컴파일하지 않고 설정값을 바꿔치기한다.** `LD_PRELOAD` 로 끼어들어 HDF5 함수 호출을 가로챈다.

```
평소:      응용 ──H5Fcreate──▶ libhdf5.so
shim 적용: 응용 ──H5Fcreate──▶ libautotuner.so ──▶ libhdf5.so
                                      ↑ 여기서 설정값 주입
```

가로챈 뒤 `config.xml` 을 읽어 값을 넣고, 원래 함수를 부른다. **응용 입장에서는 아무것도 달라지지 않는다.** 이게 HDF Group 이 만든 원본의 핵심 아이디어이고, 우리는 여기에 버그 수정과 비동기 API 지원을 더했다.

### (2) 탐색을 돌리는 파이썬 — `autotuner/`

**설정값을 하나 정해서 응용을 돌리고, 성능을 재고, 더 나은 값을 찾는 루프다.**

```
설정 생성 → config.xml 쓰기 → 응용 실행 → 대역폭 측정 → 다음 설정 결정
   ↑                                                          │
   └──────────────────────────────────────────────────────────┘
```

프레임워크는 `--framework h5tuner` / `--framework tunio` 로 갈아 끼운다. **나머지는 전부 공유한다** — 그래야 비교가 성립한다.

---

## 5. 지금까지 확인된 것

전부 KISTI **누리온**에서 실제로 측정한 값이다. 상세는 `04-누리온-환경.md` 에 있다.

**오토튜닝이 실제로 작동한다.** h5bench 쓰기 성능이 **260 → 575 MB/s (+121%)** 로 올랐다. 8 세대, 후보 41 개, 5.6 분이 들었다.

**TunIO 의 논문 주장이 재현됐다.** 거의 같은 성능(568 대 575)을 **32% 적은 시간**에 얻고 스스로 멈췄다.

**단, 중요한 조건이 하나 붙는다.** TunIO 에 사전 지식(파라미터 영향도 순위)을 주지 않으면 **3.3% 밖에 못 올린다.** 측정 노이즈가 3.4% 이므로 사실상 실패다. 그 순위를 만들려면 응용을 **171 번** 더 돌려야 하는데, 그 비용은 아무 예산에도 잡혀 있지 않다.

**그리고 그 사전 지식은 같은 응용에서 뽑은 것이다.** 처음 보는 응용에도 통하는지는 **아직 모른다.** 이게 지금 가장 큰 미해결 질문이다.

**파라미터 영향도 순위** (실측 171 점, 단위 MB/s)

| 순위 | 파라미터 | 효과 |
|---:|---|---:|
| 1 | `striping_factor` | 88.7 |
| 2 | `cb_nodes` | 52.2 |
| 3 | `striping_unit` | 34.8 |
| 4 | `chunk_cache` | 25.9 |
| … | | |
| 12 | `coll_metadata_write` | 2.2 |

**측정 변동폭은 3.4%** 다(같은 설정 30 회 반복). 3 회 반복 평균을 쓰면 0.9% 로 떨어진다.

---

## 6. 저장소 구조

```
├── src/                    C 라이브러리 (LD_PRELOAD shim)
│   └── autotuner_hdf5.c    HDF5 함수를 가로채는 본체
├── autotuner/              파이썬 오토튜너 (우리가 새로 만든 부분)
│   ├── space.py            12 개 파라미터의 탐색 공간
│   ├── objective.py        성능을 어떻게 재는가
│   ├── ga.py               유전 알고리즘
│   ├── frameworks/         h5tuner / tunio 구현
│   ├── rl/                 TunIO 의 강화학습 두 개
│   ├── discovery/          I/O 코드만 잘라내는 도구
│   ├── launchers.py        PBS / 로컬 실행
│   └── apps.py             벤치마크 어댑터
├── traces/                 실측 데이터
├── test/                   C 테스트 (현재 링크 실패, 06 문서 §4.2)
└── 01~07-*.md              이 문서들
```

**`autotuner/` 가 우리가 새로 쓴 부분이다.** `src/`, `xml/`, `examples/` 는 HDF Group 원본에 수정을 가한 것이다.

---

## 7. 문서 읽는 순서

| # | 문서 | 누가 읽나 | 분량 |
|---|---|---|---:|
| **01** | **프로젝트 개요** ← 지금 이것 | 전원 | 15 분 |
| **02** | 설계 결정 | 코드를 고칠 사람 | 김 |
| **03** | 원본 코드 분석 | C shim 을 건드릴 사람 | 중간 |
| **04** | 누리온 환경 | **클러스터에서 뭔가 돌릴 사람 전원** | 중간 |
| **05** | 뉴론 이식 | 다른 시스템으로 옮길 사람 | 짧음 |
| **06** | 진행 상황 | 작업을 시작하기 직전 전원 | 중간 |
| **07** | 후속 연구 계획 | 연구 방향을 정할 사람 | 김 |

**바쁘면 01 → 06 → 04 만 읽어도 된다.** 무엇이 되는지, 무엇이 남았는지, 어떤 함정이 있는지가 거기 있다.

---

## 8. 5 분 만에 돌려 보기

**MPI 도 HDF5 도 클러스터도 필요 없다.** 파이썬 3.6 이상만 있으면 된다.

```bash
# 파라미터 공간이 어떻게 생겼는지 본다
python3 -m autotuner info

# 가짜 성능 모델로 탐색을 한 번 돌려 본다 (실제 실행 없음)
python3 -m autotuner run --framework h5tuner --dry-run --seed 1 \
    --generations 8 --population 10

# TunIO 로 바꿔서 같은 것을 돌린다 — 조기 종료가 보인다
python3 -m autotuner run --framework tunio --dry-run --seed 1 \
    --generations 8 --population 10
```

`--dry-run` 은 응용을 실행하지 않고 합성 성능 모델로 탐색만 돌린다. **탐색 로직을 이해하는 데는 이것으로 충분하다.**

실기 실행은 `02-설계-결정.md` §14 에 확정된 경로와 함께 있다.

---

## 9. 도움이 필요한 지점

**이 저장소를 공유하는 이유가 여기 있다.** 우선순위 순이다.

### (1) 사전 지식이 다른 응용에 전이되는가 — 가장 중요

TunIO 의 성능은 "어느 파라미터가 중요한가"를 미리 아는 데서 나온다. 그런데 우리가 그 순위를 뽑은 응용과 튜닝한 응용이 **같다.** 처음 보는 응용에서도 통하는지 확인해야 한다.

**설계는 해 뒀다.** 네 팔로 비교한다 — 사전 지식 없음 / 다른 응용의 순위 / 자기 순위 / H5Tuner. 자세한 것은 `07-후속-연구-계획.md` §8.6 과 `04-누리온-환경.md` §7h 에 있다.

### (2) 사전 지식의 비용을 어떻게 회계할 것인가

순위를 뽑는 데 응용 실행 **171 회**가 든다. 튜닝 자체는 41 회다. 논문은 "오프라인이라 여러 번에 걸쳐 나눠 갚는다"고 하는데, **그 논리는 (1) 이 참일 때만 성립한다.**

### (3) 다른 시스템에서의 재현

지금 결과는 누리온 하나에서 나왔다. **다른 클러스터에서 같은 경향이 나오는지**가 필요하다. 특히 노드를 공유하는 시스템에서 측정 변동폭이 얼마나 커지는지가 관심사다(`07` §8.2a).

### (4) 벤치마크 확대

지금은 h5bench 쓰기 하나로 결론을 냈다. exerciser 어댑터는 붙여 뒀고, MACSio 는 이 환경에서 깨져 보류 상태다.

---

## 10. 용어

| 용어 | 뜻 |
|---|---|
| **shim** | 함수 호출을 가로채는 얇은 라이브러리. 여기서는 `libautotuner.so` |
| **`LD_PRELOAD`** | 리눅스에서 "이 라이브러리를 제일 먼저 보라"고 지정하는 환경변수 |
| **Lustre** | 슈퍼컴퓨터에서 쓰는 병렬 파일시스템. 파일을 여러 디스크에 쪼개 저장 |
| **striping** | 파일을 몇 개 디스크에 어떤 크기로 쪼갤지 |
| **MPI-IO 힌트** | MPI 에게 "이렇게 써 달라"고 주는 키/값. 무시돼도 에러가 안 난다 |
| **collective I/O** | 프로세스들이 협력해 한 번에 크게 쓰는 방식. 반대는 independent |
| **ROMIO** | MPI-IO 의 실제 구현체. 파일시스템별 드라이버가 필요하다 |
| **Darshan** | I/O 를 관찰해 기록하는 프로파일러 |
| **세대 / 개체** | 유전 알고리즘 용어. 한 세대에 여러 설정(개체)을 평가한다 |
| **RoTI** | Rate of Tuning Improvement. (성능 향상) ÷ (튜닝에 든 분). TunIO 논문의 지표 |
| **PBS Pro / Slurm** | 작업 스케줄러. 누리온은 PBS, 뉴론은 Slurm |

---

## 11. 알아 두어야 할 함정 셋

**전부 에러를 내지 않고 조용히 틀린 결과를 낸다.** 실기에서만 드러났고, 전체 목록은 `04-누리온-환경.md` 에 있다.

**shim 이 로드조차 안 될 수 있다.** `LD_PRELOAD` 는 의존 라이브러리를 못 찾으면 조용히 무시한다. 그러면 모든 실험이 튜닝 없이 돌아가는데 겉보기엔 정상이다. **없는 설정 파일을 주고 경고 메시지가 나오는지 세어서 확인한다.**

**MPI-IO 힌트는 Intel MPI 에서만 작동한다.** OpenMPI 3.1.0 의 ROMIO 에는 Lustre 드라이버가 없어 영향도 1·2·3·9 위 파라미터가 전부 무시된다. `I_MPI_EXTRA_FILESYSTEM=1` 이 필요하다.

**출력을 자르지 않는다.** `tail -20` 때문에 결정적인 메시지를 놓쳐 원인 규명이 하루 늦어진 적이 있다. 경고가 스크롤아웃돼서 한 캠페인을 통째로 버린 적도 있다.

---

## 12. 라이선스와 출처

**원본은 HDF Group 의 H5Tuner 다** (`COPYING`, BSD 계열, 2012–2016). `src/`, `xml/`, `examples/`, `test/` 가 그 계보다. 재배포는 저작권 고지를 유지하면 허용된다.

**`autotuner/` 는 이 프로젝트에서 새로 작성했다.**

**TunIO 는 논문만 참고했다.** 저자 코드는 공개되어 있지 않아 논문 기술만 보고 재구현했다. 해석이 갈린 지점은 `02-설계-결정.md` §7 에 22 건으로 정리해 두었다.

> Rajesh 외, *TunIO: An AI-powered Framework for Optimizing HPC I/O*, IPDPS 2024, pp. 494–504. DOI `10.1109/IPDPS57955.2024.00050`
