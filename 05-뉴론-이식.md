# 뉴론 환경 조사 — 이식 가능성

> **이 문서는 무엇인가.** KISTI **뉴론**(Slurm 스케줄러, 이종 노드)으로 옮길 수 있는지 조사한 기록이다.
>
> **아직 실행하지 않았다.** 조사만 한 상태이고, 가장 큰 걸림돌은 시스템 HDF5 가 전부 serial 이라는 점이다(§1).
>
> **언제 읽나.** 누리온 외의 시스템으로 옮길 때. 다른 클러스터라도 §8(그대로 쓰는 것)과 §9(작업 순서)는 그대로 참고가 된다.

**작성일:** 2026-09-02
**계정:** 아래 경로의 `$USER` 는 각자의 KISTI 계정으로 바꿔 읽는다.
**접속:** `ssh neuron` (맥의 `~/.ssh/config`, 로그인 노드 `glogin01`)
**추적 상태:** 2026-09-14 부터 공개 저장소(`h5tuner-ext`)에 함께 올린다.

---

## 0. 한 줄 결론

**옮길 수 있지만 준비 작업이 사흘 규모다.** 가장 큰 걸림돌은 **시스템 HDF5가 전부 serial**이라는 것이다. 병렬 HDF5를 직접 빌드해야 한다.

**두 번째 걸림돌은 공유 노드 정책**이다. 코드 문제가 아니라 측정 신뢰도 문제다.

**좋은 소식은 `/scratch`가 Lustre이고 Intel MPI 19.1.2가 있다는 것**이다. 누리온에서 MPI-IO 힌트가 작동하는 것을 검증한 그 버전이다.

---

## 1. 치명적: 시스템 HDF5가 전부 serial이다

세 빌드를 모두 확인했다.

| 빌드 | Parallel HDF5 |
|---|---|
| `/apps/libraries/hdf5/2.0.0/gcc/11.5.0` | **OFF** |
| `/apps/libraries/hdf5/2.0.0/nvhpc/25.11` | **OFF** |
| `/apps/libraries/hdf5/2.0.0/intel/25.3.0` | **OFF** |

**`H5Pset_fapl_mpio`를 쓸 수 없다.** MPI-IO 드라이버가 없으므로 우리 shim의 `parallel` 분기가 통째로 꺼지고, 12개 파라미터 중 MPI-IO와 파일시스템 계층 4개가 무의미해진다.

**병렬 HDF5를 직접 빌드해야 한다.** 누리온에서 `00-dependency`에 직접 빌드해 둔 것과 같은 작업이다.

**버전을 1.14.1.2로 맞추기를 권한다.** 뉴론 시스템 모듈은 2.0.0인데 메이저 버전이 올라 API가 바뀌었을 수 있다. 누리온과 같은 버전으로 맞추면 shim을 손대지 않아도 되고 두 시스템의 결과를 비교할 수 있다.

**모듈 이름이 헷갈린다.** `hdf5/1.12.0`을 로드하면 실제로는 `hdf5/2.0.0`이 로드된다(모듈 파일이 `load("hdf5/2.0.0")`을 호출한다). `hdf5/1.10.2`도 마찬가지일 가능성이 있다.

**2.0.0에도 async API는 있다.** `H5Fcreate_async`, `H5Dcreate_async`, `H5Dwrite_async` 세 심볼을 확인했다. 2.0으로 가더라도 shim의 async 훅은 유효하다.

---

## 2. 치명적: 공유 노드 정책

문서의 문장이다.

> 모든 큐에서 작업 할당은 공유 노드 정책(하나의 노드에 복수개 작업 동시 수행 가능)이 적용됩니다

**누리온은 exclusive였다.** 뉴론에서는 같은 노드에서 남의 작업이 돌고, 그 작업의 I/O가 우리 대역폭 측정에 섞인다.

**규모를 보면 심각하다.** 누리온에서 잰 `cb_buffer_size`의 효과가 13.5 MB/s였다. 이웃 작업 하나가 그보다 큰 변동을 만든다. 오토튜닝은 설정 간 차이를 보는 일이므로 노이즈가 신호를 덮으면 탐색이 무의미해진다.

**대응 셋.**

반복 측정을 늘린다. `--reps 5`로 두고 축약을 `max`로 하면 간섭이 없던 관측이 채택된다. 누리온에서는 자원 절약으로 `--reps 1`을 썼는데 뉴론에서는 그럴 수 없다.

`--exclusive`가 허용되는지 확인한다. 허용되면 문제가 사라진다.

그래도 안 되면 **측정 노이즈를 결과에 명시한다.** 프레임워크 간 비교는 같은 노이즈를 겪으므로 여전히 유효하지만, 절대 대역폭은 신뢰 구간을 붙여야 한다.

---

## 3. 좋은 소식

### `/scratch`가 Lustre다

```
10.159.210.3@o2ib2:10.159.210.4@o2ib2:/lustre1/scratch  lustre  3.5P  2.6P  804T  77%
lustre1-MDT0000, lustre1-MDT0001   (MDT 2개)
```

`striping_factor`와 `striping_unit`이 그대로 의미를 가진다.

### Intel MPI 19.1.2가 있다

```
mpi/impi-19.1.2      <- 누리온에서 검증한 버전
mpi/impi-21.17
mpi/openmpi-3.1.5 / 4.1.1 / 4.1.8
```

**누리온에서 MPI-IO 힌트가 작동한 것이 이 버전이다.** `I_MPI_EXTRA_FILESYSTEM=1`과 `I_MPI_EXTRA_FILESYSTEM_LIST=lustre`가 같은 방식으로 통할 가능성이 높다.

**OpenMPI는 기대하지 않는 편이 낫다.** 누리온 3.1.0에서 ROMIO에 Lustre 드라이버가 없어 힌트가 전부 무시됐다. 4.1은 다를 수 있지만 확인 전까지는 Intel MPI로 간다.

### 쿼터가 넉넉하다

`/scratch`에 100TB, 파일 400만 개이고 현재 28KB, 7개만 쓰고 있다. 홈은 64GB에 132KB다.

---

## 4. 스케줄러가 Slurm이다

`SlurmLauncher`를 새로 만들어야 한다. `PBSLauncher`를 본떠 200줄 정도이고, sentinel 방식과 세대 배치는 그대로 재사용된다.

```sh
#SBATCH -J at-gen-000
#SBATCH -p cpu
#SBATCH --nodes=2
#SBATCH --ntasks-per-node=48
#SBATCH --time=00:30:00
#SBATCH --comment="field=math;appl=in_house"
```

**`--comment`가 필수다.** 누리온의 `-A etc`에 해당하는데 형식이 `field=<분야>;appl=<프로그램>`이다. `showappl`로 조회한 결과 우리 작업에는 `field=math`에 `appl=in_house` 또는 `misc`가 맞다.

명령어는 `sbatch` / `squeue` / `scancel`이다. 잡 실행은 `srun` 또는 `mpirun` 둘 다 된다.

---

## 5. 잡 제한이 100배 빡빡하다

| | 누리온 normal | 뉴론 (파티션별) |
|---|---:|---:|
| 동시 제출 | 1,000 | 8~20 |
| 동시 실행 | 700 | 2~10 |

**세대 단위 배치가 선택이 아니라 필수가 된다.** 40세대 캠페인이 41잡이므로 `Max Active 20`에도 순차로 들어간다. 후보마다 잡을 냈다면 601잡이라 아예 불가능했다.

이미 구현해 둔 배치가 여기서 값어치를 한다.

---

## 6. 노드 구성

| 파티션 | 노드 | 코어 | 메모리 | 용도 |
|---|---:|---:|---:|---|
| `cpu` | 7 | 48 | 500GB | **I/O 튜닝에 적합** |
| `bigmem` | 4 | 24+ | 770GB+ | 대용량 메모리 |
| `cas_v100*`, `amd_a100*`, `amd_h200*` 등 | — | — | — | GPU |
| `gh200_1` | 1 | — | — | **ARM** |

**`cpu` 파티션을 쓴다.** GPU를 쓰지 않으므로 GPU 파티션을 점유할 이유가 없고 경쟁도 심하다.

**노드당 48코어다.** 누리온 KNL 64와 다르므로 `--ntasks-per-node=48`로 바뀐다.

**`gh200_1`은 ARM이다.** shim, mxml, HDF5, h5bench를 전부 ARM으로 다시 빌드해야 한다. 갈 이유가 없다.

**한 캠페인은 한 파티션에서 끝나야 한다.** 파티션이 섞이면 성능 차이가 파라미터 때문인지 하드웨어 때문인지 구분되지 않는다.

최대 walltime은 모든 파티션이 2일이다.

---

## 7. Darshan이 없다

모듈에도 `/apps`에도 없다.

**치명적이지 않다.** 우리 코드가 정규식 스크레이핑으로 폴백하고 h5bench는 자기 대역폭을 출력한다. `--darshan-lib`를 안 주면 자동으로 그 경로다.

**단 I/O Discovery로 만든 커널을 쓸 때는 문제가 된다.** 커널은 print 문이 제거되므로 스크레이핑할 대상이 없다. 그때는 Darshan을 직접 빌드하거나 커널에 출력을 남겨야 한다.

---

## 8. 그대로 쓰는 것

**shim의 C 코드**가 그대로다. async 훅을 포함해 HDF5 API 수준이므로 스케줄러와 무관하다. 재빌드만 하면 된다.

**파이썬 오토튜너 코어 전체**가 그대로다. 공간, 목적함수, GA, RL, 기록·재생, 지표가 전부 스케줄러 독립이다.

**h5bench 어댑터**도 그대로다. `--mpi-cmd`를 `srun`으로 바꾸면 된다.

**세대 배치, 반복 배치, Darshan 통합**도 그대로다.

---

## 9. 작업 순서

**첫째, 병렬 HDF5를 직접 빌드한다.** 1.14.1.2로 맞춘다. 반나절.

**둘째, MPI 힌트를 검증한다.** 누리온에서 쓴 `mpi_hints.c`를 그대로 돌린다(`/scratch/$USER/12-autotuner/probe2/`에 있다). `MPI_File_get_info`가 5개를 돌려주면 무력, 18개면 정상이다. `striping_factor`를 바꿔가며 `lfs getstripe`를 본다.

**이 둘이 통과해야 나머지가 의미를 가진다.**

**셋째, `SlurmLauncher`를 구현한다.** 하루.

**넷째, h5bench를 뉴론 툴체인으로 빌드한다.** 누리온에서 한 대로 `mpiicc -std=gnu99`로 직접 컴파일한다. CMake는 json-cwx를 받으려다 멈춘다.

**다섯째, 공유 노드 노이즈를 측정한다.** 같은 설정을 10회 반복해 변동 폭을 본 뒤 `--reps`를 정한다.

**전체 사흘 규모다.**

---

## 10. 확인하지 않은 것

**MPI-IO 힌트가 실제로 통하는지.** 가장 중요한 미확인 항목이다.

**`--exclusive`가 허용되는지.** 공유 노드 노이즈를 없앨 수 있는지가 여기 달렸다.

**병렬 HDF5 빌드가 순조로울지.** 누리온에서는 이미 되어 있어서 겪지 않은 단계다.

**`cpu` 파티션의 Lustre 대역폭.** 누리온과 다를 수 있고, 후보당 시간 추정이 달라진다.
