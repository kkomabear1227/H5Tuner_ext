# 누리온 환경 현황

> **이 문서는 무엇인가.** KISTI **누리온**(PBS Pro 스케줄러)에서 실제로 돌린 기록이다. **실측 숫자가 거의 다 여기 있다.**
>
> **가장 중요한 절 넷.** §7c(MPI-IO 힌트가 Intel MPI 에서만 작동한다), §7d(첫 튜닝 결과와 파라미터 영향도 순위), §7e(H5Tuner 대 TunIO 비교), §7f(측정 변동폭).
>
> **언제 읽나.** 클러스터에서 무언가를 돌릴 계획이면 통째로 읽는다. 함정이 전부 여기 적혀 있고, 전부 **에러 없이 조용히 틀린 결과를 내는** 종류다.

**작성일:** 2026-08-28
**계정:** 아래 경로의 `$USER` 는 각자의 KISTI 계정으로 바꿔 읽는다.
**접속:** `ssh nurion` (맥의 `~/.ssh/config`에 있음, `ControlPersist 12h`)
**추적 상태:** 2026-09-14 부터 공개 저장소(`h5tuner-ext`)에 함께 올린다.

---

## 0. 한 줄 결론

**실험에 필요한 것이 전부 이미 설치되어 있다.** HDF5 1.14.1.2(parallel ON), Darshan, h5bench 1.4.0, MACSio가 홈에 있다.

**`libautotuner.so`는 2026-08-28에 빌드했다.** 진짜 HDF5 헤더로 경고 없이 컴파일된다. 상세는 §6.

**디스크는 걱정할 필요가 없다.** `/scratch`에 99TB가 남는다.

**큐 개수 제한도 걱정할 필요가 없다.** `normal`에 잡 1000개를 걸 수 있다. 문제는 개수가 아니라 대기 시간이다.

---

## 1. 파일시스템 현황

두 곳을 쓴다. 홈은 소프트웨어, 스크래치는 데이터다.

**`/home01/$USER`에 5.3GB를 썼고 한도는 64GB다.** 파일 수는 22,085개이고 한도는 200,000개다. 여유가 충분하지만 대용량 출력을 여기 두면 안 된다.

**`/scratch/$USER`에 690GB를 썼고 한도는 100TB다.** 파일 수는 44,421개이고 한도는 1,000,000개다.

`/scratch`의 690GB 중 631GB가 `h5bench_exerciser`다. 이전 실험 데이터로 보인다. 19GB가 `11-analysis`에 있다.

**전체 파일시스템은 `/scratch`가 19PB 중 62% 사용, `/home01`이 691TB 중 29% 사용이다.**

### 파일 개수에 주의할 점 하나

튜닝 캠페인은 후보마다 디렉터리를 만든다. 후보 하나당 `config.xml`, `h5bench.cfg`, `job.sh`, 반복별 출력, sentinel, Darshan 로그가 생기므로 대략 10개다.

**800점 스윕이면 8,000개 정도다.** 한도 100만 개에 비하면 미미하다. 다만 캠페인을 여러 번 돌리면 쌓이므로 주기적으로 지우는 편이 좋다.

---

## 2. 큐 제한

**우리가 쓸 큐는 `normal`이다.** 제한은 이렇다.

| 항목 | 값 |
|---|---|
| 동시 대기 (max_queued) | 1,000 |
| 동시 실행 (max_run) | 700 |
| 최대 walltime | 48:00:00 |
| 기본 KNL 모드 | quadrant / cache |

**`debug` 큐는 제한이 훨씬 빡빡하다.** 대기 2개, 실행 2개, **최대 2노드**, walltime 12시간이다. 짧은 확인에는 쓸 수 있지만 4노드 실험은 못 돌린다.

`flat` 큐는 대기 50개, 실행 40개다. Flat 메모리 모드가 필요할 때만 쓴다.

`long`은 대기 30개, 실행 30개, walltime 120시간이다.

### 진짜 제약은 혼잡도다

**2026-08-28 15시 기준 `normal`이 88% 사용 중이고 380개 잡이 대기하고 있다.** 6,946노드 중 829노드가 비어 있다.

`debug`는 90%, `flat`은 94%다.

**그래서 잡 개수 제한(1000개)은 실질적 제약이 아니다.** 제약은 잡 하나가 큐에서 기다리는 시간이다. 후보마다 잡을 내면 그 대기가 후보 수만큼 곱해진다.

---

## 3. 설치된 소프트웨어

전부 홈 디렉터리에 있고 module로 로드하는 것이 아니다.

### HDF5 1.14.1.2

```
~/00-dependency/installation-home/hdf5        (Intel MPI 빌드)
~/00-dependency/installation-home/hdf5-gcc    (GCC 빌드)
```

**Parallel HDF5가 ON이다.** Fortran과 C++ 바인딩은 OFF다.

컴파일러는 `/apps/compiler/intel/19.1.2/impi/2019.8.254/bin64/mpicc`다. **Intel 19.1.2 + Intel MPI 2019.8이다.**

소스는 `~/00-dependency/hdf5`에 1.4GB로 있다.

### Darshan

```
~/00-dependency/installation-home/darshan-runtime/lib/libdarshan.so
~/00-dependency/installation-home/darshan-util/bin/darshan-parser
```

GCC 빌드도 나란히 있다(`darshan-runtime-gcc`, `darshan-util-gcc`).

**`darshan-parser`가 있으므로 우리 `autotuner/darshan.py`가 바로 동작한다.** 로그 디렉터리로 쓰던 `~/10-log/darshan-log`가 있다.

### h5bench 1.4.0

```
~/01-benchmark/h5bench/build/
```

빌드된 바이너리가 전부 있다.

`h5bench_write`, `h5bench_read`, `h5bench_overwrite`, `h5bench_append`, `h5bench_write_unlimited`, `h5bench_exerciser`, `h5bench_hdf5_iotest`, `h5bench_amrex_sync`, `h5bench_openpmd_write`, `h5bench_openpmd_read`, 그리고 **`h5bench_macsio`와 `macsio`**다.

**MACSio가 이미 빌드되어 있다는 것이 중요하다.** 논문이 I/O Discovery 평가에 쓴 응용이다.

---

## 4. 잡 스크립트 관례

기존 스크립트(`~/03-job-script/h5bench-exerciser-characterization/`)가 쓰는 형식이다.

```sh
#PBS -V
#PBS -N <이름>
#PBS -q normal
#PBS -A etc
#PBS -l select=1:ncpus=64:mpiprocs=64:ompthreads=1
#PBS -l walltime=04:00:00
#PBS -m n
```

**`-A etc`를 쓴다.** 누리온은 application code가 필수인데 우리 작업에 맞는 것이 없으므로 `etc`가 맞다.

**`-m n`으로 메일을 끈다.** 후보마다 잡을 내면 메일이 수백 통 오므로 반드시 필요하다. 우리 런처 기본값에 넣어야 한다.

---

## 5. 우리 작업에 대한 함의

### 빌드할 것은 하나뿐이다

`libautotuner.so`만 만들면 된다. 필요한 것이 전부 있다.

**빌드는 §6에서 실제로 마쳤다.** 아래는 확정된 명령이다.

```bash
module load gcc/10.2.0 openmpi/3.1.0
export HDF5ROOT=$HOME/00-dependency/installation-home/hdf5-gcc
export MXMLROOT=/scratch/$USER/12-autotuner/deps/mxml-install
export MPIROOT=/apps/compiler/gcc/10.2.0/openmpi/3.1.0
./autogen.sh
./configure --with-hdf5=$HDF5ROOT --with-mxml=$MXMLROOT --with-mpi=$MPIROOT CC=mpicc
make
```

**HDF5는 Intel 빌드가 아니라 `hdf5-gcc`를 쓴다.** 이유는 §6에 있다.

### 작업 디렉터리는 `/scratch`다

누리온은 계산 작업을 `/scratch`에서 하도록 요구한다. 그리고 Lustre 스트라이핑 튜닝이 목적이므로 `/scratch`여야 의미가 있다.

```
/scratch/$USER/12-autotuner/     새로 만든다
```

홈은 소스와 바이너리만 둔다.

### Python 3.9.5

`/apps/applications/PYTHON/3.9.5/bin/python3`이 시스템 기본이다.

**`autotuner`가 3.9.5에서 정상 동작한다.** `info`와 `dry-run`을 실행해 확인했다.

PyTorch는 확인하지 않았다. 없으면 폴백 백엔드가 쓰인다.

### 잡 개수를 줄이는 것이 실질적으로 중요하다

큐가 88% 혼잡하고 380개가 대기 중이다. 잡 하나의 대기 시간이 길다.

**세대 단위 배치를 구현했다(2026-08-28).** 40세대에 개체 15면 601잡이 41잡이 된다. 세대 안의 후보는 어차피 순차로 평가하므로 한 allocation 안에서 돌려도 결과가 같다. 상세는 `02-설계-결정.md` §12.

---

## 6. 실제로 확인한 것 (2026-08-28 작업 결과)

### 툴체인이 확정됐다

**h5bench와 MACSio는 Intel이 아니라 GCC로 빌드되어 있다.** `ldd`로 확인했다.

```
libhdf5.so.310 -> 00-dependency/installation-home/hdf5-gcc/lib
libmpi.so.40   -> /apps/compiler/gcc/10.2.0/openmpi/3.1.0/lib
```

**그래서 shim도 같은 툴체인으로 빌드해야 한다.** Intel 빌드 HDF5로 만들면 심볼이 맞지 않는다.

```bash
module load gcc/10.2.0 openmpi/3.1.0
HDF5ROOT=$HOME/00-dependency/installation-home/hdf5-gcc
DARSHAN=$HOME/00-dependency/installation-home/darshan-runtime-gcc/lib/libdarshan.so
PARSER=$HOME/00-dependency/installation-home/darshan-util-gcc/bin/darshan-parser
```

`hdf5-gcc`도 1.14.1.2이고 Parallel HDF5가 ON이다.

### shim이 빌드됐다

**진짜 HDF5 1.14.1.2 헤더로 컴파일했고 경고가 하나도 없다.** 이 저장소에서 가장 오래된 미확인 항목이 해소됐다.

```
/scratch/$USER/12-autotuner/lib/libautotuner.so
```

mxml 2.9는 `deps/mxml-install`에 직접 빌드했다. 로그인 노드에서 GitHub 다운로드가 된다.

### 고쳐야 했던 것 하나

**`configure.ac`가 `-lmpich`를 하드코딩하고 있었다.** OpenMPI 사이트에서는 항상 링크가 깨진다. `libmpi`와 `libmpich` 중 있는 것을 고르도록 고쳤다.

### 아직 깨지는 것 하나

**`test/` 프로그램이 링크되지 않는다.** OpenMPI의 `libmpi.la`가 `/opt/pbs/lib/libpbs.la`를 의존성으로 적어놨는데 그 파일이 없다. `libpbs.so`는 있다.

libtool이 `.la` 체인을 따라가다 막히는 문제다. `CC=gcc`로 바꾸고 절대 경로 `.so`를 줘도 우회되지 않았다.

**shim 자체는 영향받지 않는다.** `src/`는 정상적으로 빌드된다. `make check`로 하려던 파라미터 주입 확인은 MACSio를 직접 돌려 `lfs getstripe`로 보는 편이 오히려 강한 증거다.

### MACSio가 h5bench write보다 나은 튜닝 대상이다

MACSio의 HDF5 플러그인 옵션을 확인한 결과다.

`--parallel_file_mode SIF`가 단일 공유 파일에 collective I/O를 쓴다. **데이터셋을 single chunk로 만들므로 `chunk_cache`가 살아난다.** h5bench write는 `COMPRESS=NO`면 contiguous라 그 파라미터가 죽는다.

메시 메타데이터가 많아 `meta_block_size`, `coll_metadata_write`, `col_meta_ops`, `mdc_conf`도 h5bench write보다 훨씬 잘 눌린다.

**MACSio에 `--sieve_buf_size`, `--meta_block_size`, `--alignment` 옵션을 주면 안 된다.** 셋 다 우리가 튜닝하는 파라미터다. 비워두면 shim이 제어한다.

**MACSio는 `libjson-cwx.so.2`를 필요로 한다.** h5bench 빌드 트리 안에 있고 기본 라이브러리 경로에 없다.

```
LD_LIBRARY_PATH=$HOME/01-benchmark/h5bench/build/json-cwx/lib
```

### 큐 대기가 실질적 제약이다

`debug` 큐에 2노드 잡을 냈더니 바로 실행되지 않고 대기했다. 큐가 90% 사용 중이다.

**그래서 잡 개수를 줄이는 최적화를 넣었다.** 세대 전체를 잡 하나에서 평가한다. 40세대 캠페인이 601잡에서 41잡이 된다.

---

## 7. MACSio는 이 환경에서 깨진다 — 보류 (2026-08-31)

**MACSio의 SIF 모드가 HDF5 assertion으로 죽는다. 우리 코드와 무관하다.**

```
macsio: H5Fio.c:179: H5F_shared_block_write:
  Assertion `((addr)!=(18446744073709551615UL))' failed.
```

`18446744073709551615`은 `HADDR_UNDEF`다. 주소가 정해지지 않은 상태로 블록 쓰기를 시도한다.

**shim 없이 돌린 대조군도 똑같이 죽는다.** 세 조건(shim 없음 / shim + 빈 config / shim + striping)을 한 잡에서 비교했고 전부 `exit=134`였다. 그래서 shim은 원인이 아니다.

**2026-08-31에 보류하기로 했다.** 고치려면 MACSio나 HDF5 쪽을 파야 하는데 이 프로젝트의 범위가 아니다. 튜닝 대상은 h5bench write로 간다.

**다시 볼 때의 후보.** MIF 모드로 우회되는지 본다. 다만 논문이 쓰는 단일 공유 파일 설정과 달라지므로, 되더라도 그 차이를 기록해야 한다.

### 부수적으로 알게 된 것

**HDF5가 최적화 없이 빌드되어 있다.**

```
Build Mode: Release
Optimization Level: OFF
```

그리고 C `assert()`가 살아 있다. 위 assertion이 터진 것이 그 증거다.

**절대 성능 수치가 낮게 나온다.** 프레임워크 간 상대 비교는 여전히 유효하지만, 논문 수치와 직접 비교할 때는 이 점을 밝혀야 한다.

---

## 7b. 실기에서만 드러난 함정 셋 (2026-08-31)

**계산 노드의 python은 3.6이다. 로그인 노드는 3.9다.**

```
File "/usr/lib64/python3.6/argparse.py"
TypeError: __init__() got an unexpected keyword argument 'required'
```

`add_subparsers(required=True)`가 3.7부터라 캠페인이 시작조차 못 했다. 코드를 3.6 호환으로 고쳤고, 잡 스크립트에서는 `/apps/applications/PYTHON/3.9.5/bin/python3`을 절대 경로로 쓴다.

**h5bench의 크기 접미사는 앞에 공백이 필요하다.**

`parse_unit()`이 `strtok(str, " ")`으로 숫자와 단위를 나눈다. `512K`는 512로 읽히고 단위가 무시된다. `512 K`라야 1024배가 곱해진다. 문서의 "16M, 8K" 주석이 오해를 부른다.

**어댑터가 접미사를 정수로 전개하도록 고쳤다.** `512K` → `DIM_1=524288`이다. 공백 규칙에 의존하지 않는다.

**쓰기 연산에서 `NUM_PARTICLES`는 무시된다.** `read_config()`가 `num_particles = dim_1 * dim_2 * dim_3`로 덮어쓰고 기본값이 전부 1이다. `NUM_PARTICLES`만 설정하면 랭크당 입자 하나를 쓴다. `NUM_DIMS`와 `DIM_1`을 써야 한다.

**세 함정 모두 조용히 잘못된 결과를 낸다.** 에러가 아니라 4KB짜리 실험이 성공한 것처럼 끝난다. 대역폭이 KB/s로 나오는 것이 유일한 단서였다.

---

## 7c. MPI-IO 힌트는 Intel MPI 에서만 작동한다 (2026-09-01)

**결론부터. OpenMPI 3.1.0 에서는 `striping_factor`, `striping_unit`, `cb_nodes`, `cb_buffer_size` 가 전부 무시된다. Intel MPI 에서는 환경변수 두 개를 켜면 작동한다.**

파라미터 12개 중 4개가 여기 달려 있고, 그 4개가 대역폭에 가장 큰 영향을 준다. 툴체인 선택이 실험의 성립 여부를 가른다.

### 증거

`MPI_File_get_info()` 가 돌려주는 힌트 개수가 결정적 단서였다.

| | OpenMPI 3.1.0 | Intel MPI 2019.8 |
|---|---|---|
| 채택 보고 힌트 수 | 5개 (우리가 준 것만) | **18개** |
| ROMIO Lustre ADIO | **없음** (심볼 0) | 있음 (`ADIOI_LUSTRE` 21개) |
| `striping_factor` 실제 적용 | 안 됨 | **됨** |

정상적인 ROMIO 는 `romio_cb_read`, `cb_config_list`, `romio_ds_write` 같은 시스템 힌트를 함께 보고한다. OpenMPI 가 5개만 돌려준 것은 **힌트를 저장만 하고 해석하지 않는다**는 뜻이었다.

`OMPI_MCA_io=romio314` 로 ROMIO 를 강제하는 것은 실제로 적용되지만(컴포넌트 로딩 확인) 소용이 없다. 그 ROMIO 에 Lustre 드라이버가 없어 UFS 로 폴백하고 힌트를 버린다.

OMPIO 에는 Lustre 지원이 있다(`MCA fs: lustre`, 심볼 16개). 그래서 MCA 파라미터로 시도했지만 **그것도 무력이었다.**

| OMPIO MCA 파라미터 | 값 변화 | 결과 |
|---|---|---|
| `fs_lustre_stripe_width` | 1 / 8 / 32 | stripe_count 계속 1, 대역폭 446/445/384 (노이즈) |
| `io_ompio_num_aggregators` | 2 / 16 / 64 | 561.9 / 557.7 / 559.7 MB/s (차이 1% 미만) |

### Intel MPI 에서 켜는 방법

```bash
export I_MPI_EXTRA_FILESYSTEM=1
export I_MPI_EXTRA_FILESYSTEM_LIST=lustre
```

**이것 없이는 ROMIO 가 `/scratch` 를 NFS 로 오인한다.**

```
romio_filesystem_type = NFS:      <- 스위치를 끈 상태
```

그러면 Lustre ADIO 를 쓰지 않고 `striping_factor` 를 버린다. `romio_filesystem_type=LUSTRE:` 를 MPI_Info 힌트로 직접 주는 것만으로는 **해결되지 않는다.** Intel MPI 쪽 스위치를 켜야 한다.

스위치를 켜면 `striping_factor=8` 이 채택 목록에 남고 파일이 `lmm_stripe_count: 8` 로 생성된다.

### `lfs setstripe` 우회 (striping 전용)

MPI 힌트와 무관하게 동작하는 유일한 경로다. 디렉터리에 걸면 그 안에 만든 파일이 상속한다.

2노드 128랭크, 4GB 쓰기에서 확인한 값이다.

| stripe_count | 실제 반영 | 대역폭 |
|---:|---|---:|
| 1 | 1 | 424.963 MB/s |
| 8 | 8 | 527.791 MB/s |
| 32 | 32 | 564.910 MB/s |

**33% 향상이고 튜닝이 실제로 작동한다는 첫 실증이다.** 다만 `cb_nodes` 와 `cb_buffer_size` 는 이 방법으로 다룰 수 없다. 파일 레이아웃이 아니라 MPI-IO 런타임 동작이기 때문이다.

### 그래서 결정

**Intel 툴체인으로 간다.** 병렬 I/O 오토튜닝에 `cb_nodes` 와 `cb_buffer_size` 가 필요하고, Intel MPI 가 유일한 경로다.

`~/01-benchmark/h5bench-icc-impi/` 에 `h5bench_write` 와 `h5bench_read` 를 빌드했다. CMake 는 쓸 수 없었다 -- `CMakeLists.txt:288` 이 json-cwx 를 GitHub 에서 받으려다 52분간 멈춘다. 소스 세 개를 `mpiicc -std=gnu99` 로 직접 컴파일하는 편이 확실하다.

shim 도 Intel 로 다시 빌드해야 한다. GCC+OpenMPI 빌드는 Intel MPI 앱에 붙지 않는다. mxml 도 같은 컴파일러로 맞춘다(`deps/mxml-install-icc`).

**h5bench 의 다른 벤치마크(MACSio, AMReX, OpenPMD 등)는 Intel 로 빌드되지 않는다.** 사용자가 GCC+OpenMPI 를 택한 이유가 그것이었다. write/read 만 Intel 로 쓰고 나머지는 범위 밖으로 둔다.

---

## 7d. 첫 실기 튜닝 결과 (2026-09-01)

**H5Tuner 가 h5bench write 를 261 → 570 MB/s 로 118% 개선했다.** 오토튜닝이 실기에서 작동한다는 첫 증명이다.

2노드 128랭크, 8세대 × 개체 5, 후보 41개, 튜닝 5.4분이다. RoTI 최고 136.2 MB/s per tuning minute.

찾아낸 설정은 `striping_factor=4`, `striping_unit=8Mi`, `cb_nodes=8`, `cb_buffer_size=8Mi` 다.

**단독 실험과 다른 값이 나온 것이 중요하다.** 개별 실험에서는 `striping_factor=8` 이 최적이었는데 캠페인은 4를 골랐다. `striping_unit` 이 8Mi 로 다르기 때문이다. **파라미터 간 상호작용이 있고, 하나씩 훑는 방식으로는 최적을 찾지 못한다.** 오토튜닝이 필요한 이유가 실측으로 드러났다.

### 같은 잡의 TunIO 는 3.3% 에 그쳤다 — 사전 순위를 주지 않았기 때문이다

**이것이 사실상 ablation 이다.** `c1` 잡(`23813664`)은 H5Tuner 와 TunIO 를 한 잡에서 연달아 돌렸고, TunIO 에는 `--rl-seed 7 --rl-curves 40 --rl-rounds 8` 만 주고 **`--ranking-from-trace` 를 주지 않았다.** 즉 조기 종료기의 합성 곡선 사전학습은 있었지만 **부분집합 선택기의 오프라인 영향도 순위가 없었다.**

| | H5Tuner | TunIO (순위 없음) |
|---|---:|---:|
| baseline | 261.146 MB/s | 264.345 MB/s |
| best | 570.600 MB/s | **273.097 MB/s** |
| 향상 | **+118.5%** | **+3.3%** |
| gain | 309.454 MB/s | 8.752 MB/s |
| 튜닝 시간 | 5.4 분 | 4.8 분 |
| RoTI peak | 136.225 | **3.058** |
| RoTI final | 57.165 | 1.823 |

**성능은 올랐지만 의미 없는 수준이다.** 3.3% 는 §7f 에서 잰 측정 변동폭 3.4% 와 같은 크기다. **노이즈와 구별되지 않는다.** RoTI 는 45배 차이가 난다.

**원인은 부분집합 선택기다.** 사전 순위 없이 시작하면 picker 가 영향 없는 파라미터만 고르다 예산을 소진한다. 8세대 40후보는 picker 가 스스로 순위를 학습하기에 턱없이 부족하다. §7d 의 영향도 순위에서 보듯 상위 4개(`striping_factor`, `cb_nodes`, `striping_unit`, `chunk_cache`)를 놓치면 탐색이 갈 곳이 없다.

**TunIO 의 결함이 아니다. 논문이 요구하는 입력을 빠뜨린 것이다.** 논문은 파라미터 스윕으로 사전학습한다고 명시한다. 순위를 넣은 뒤 같은 예산에서 TunIO 가 568.460 (+119%) 에 도달하고 RoTI 가 H5Tuner 보다 26% 높아졌다(§7e).

**따라서 세 캠페인이 하나의 논증을 만든다.**

`c1` 이 "순위 없는 TunIO = 3.3%", `s3` 이 순위를 만든 sweep(171점), `cmp` 이 "순위 있는 TunIO = 119% + 조기 종료" 다. **Impact-First Tuning 이 TunIO 성능의 원천이라는 것을 실측으로 보인다.** 논문에는 이 ablation 이 없으므로 우리 기여로 쓸 수 있다.

**주의할 점 하나.** `c1` 의 두 baseline 이 261.146 과 264.345 로 1.2% 다르다. 같은 설정인데 다른 값이 나온 것이고 §7f 의 3.4% 변동폭 안에 있다. 최종 논문 수치는 `--reps 3` 으로 다시 재야 한다.

### 영향도 순위 (171점 실측)

| 순위 | 파라미터 | 효과 (MB/s) |
|---:|---|---:|
| 1 | striping_factor | 88.678 |
| 2 | **cb_nodes** | 52.180 |
| 3 | striping_unit | 34.809 |
| 4 | chunk_cache | 25.912 |
| 5 | col_meta_ops | 20.098 |
| 6 | alignment_threshold | 19.756 |
| 7 | meta_block_size | 15.732 |
| 8 | alignment | 15.116 |
| 9 | cb_buffer_size | 13.519 |
| 10 | sieve_buf_size | 11.873 |
| 11 | mdc_conf | 3.348 |
| 12 | coll_metadata_write | 2.167 |

**`cb_nodes` 가 2위다.** Intel MPI 로 전환한 판단이 옳았음을 보여준다. OpenMPI 였다면 1·2·3·9위가 모두 죽은 채로 탐색했을 것이다.

**소스 독해로 세운 예측 둘이 틀렸다.** `chunk_cache` 는 "COMPRESS=NO 면 contiguous 라 무효" 로 봤는데 4위(25.9)로 살아 있다. `sieve_buf_size` 도 10위(11.9)로 0 이 아니다. **소스만 읽고 죽었다고 단정한 것이 성급했다.** §12 의 그 예측은 실측으로 정정한다.

죽은 파라미터는 없다. 가장 낮은 `coll_metadata_write` 도 2.167 이다. 논문이 "12개 중 5개는 영향이 없었다" 고 쓴 것과는 다른 결과이고, 워크로드가 다르니 당연하다.

### sweep 은 후보당 21초다

200점 sweep 이 walltime 60분을 1분 초과해 죽었다(171점 기록 후). **무작위 설정은 느린 조합을 많이 포함하므로 GA 캠페인의 후보당 10초보다 두 배 걸린다.** 다음에는 후보당 25초로 잡는다.

**Recorder 가 한 줄씩 flush 한 덕에 171점이 살아남았고 순위를 뽑기에 충분했다.** 그 설계가 실제로 값어치를 했다.

---

## 7e. 첫 공정 비교 — H5Tuner vs TunIO (2026-09-01)

**둘 다 성능을 121% 올렸고, TunIO 가 26% 더 좋은 RoTI 로 더 빨리 도달했다.**

| | H5Tuner | TunIO |
|---|---:|---:|
| baseline | 260.343 MB/s | 260.013 MB/s |
| best | 575.140 MB/s | 568.460 MB/s |
| 향상 | +121% | +119% |
| 튜닝 시간 | 5.6 분 | **3.8 분** |
| **RoTI peak** | 137.008 | **173.149** |
| RoTI final | 56.061 | **81.942** |
| 정지 | 없음 (8세대 소진) | **7세대에서 조기 종료** |

**논문의 주장이 재현됐다.** TunIO 는 거의 같은 성능(568 vs 575, 1.2% 차이)을 **32% 적은 시간**에 얻고 조기에 멈춘다. RoTI 가 26% 높다.

### TunIO 의 부분집합 선택기가 작동한다

warm start 가 sweep 순위를 그대로 재현했다.

```
picker: warm-started from the offline ranking;
        initial top 4: striping_factor, cb_nodes, striping_unit, chunk_cache
```

**영향도 순위 1~4위와 정확히 일치한다.** 그리고 세대마다 부분집합을 바꾼다. 0세대에 `cb_nodes, chunk_cache, col_meta_ops, striping_unit` 을 고르고, 1세대에 `col_meta_ops` 를 빼고 `striping_factor` 를 넣는 식이다.

**0세대에서 이미 568.460 에 도달한다.** H5Tuner 는 같은 세대에 566.535 다. 고영향 파라미터만 튜닝하니 첫 세대부터 좋은 값을 찾는다. 이것이 논문의 Impact-First Tuning 이다.

### 조기 종료기도 작동한다

```
stopped at generation 7: predicted improvement over the next 5 iterations
is below the 1.0%-per-iteration threshold (Q_continue=-0.0265)
```

3세대부터 best 가 568.460 에서 고정이다. 정지기가 그것을 읽고 7세대에서 멈췄다.

**합성 곡선으로 학습한 정책이 실기에서 합리적으로 동작했다.** TODO §4.1 의 우려 -- 균등 비용 가정 때문에 필요 이상으로 일찍 멈출 것이라는 예상 -- 는 이 규모에서는 나타나지 않았다. 8세대라는 짧은 예산에서는 판단하기 이르다.

### 주의: 8세대는 짧다

**H5Tuner 도 4세대 이후 개선이 없었다.** 575.140 에서 고정이다. 두 프레임워크 모두 탐색이 일찍 수렴했고, 이 규모에서는 조기 종료의 이득이 과대평가될 수 있다.

**논문의 50세대로 늘리면 그림이 달라질 수 있다.** H5Tuner 가 더 오래 탐색해 더 좋은 값을 찾을 여지가 있고, 그러면 TunIO 의 조기 종료가 손해가 된다. 이 비교는 "8세대 예산에서" 라는 조건을 달아야 한다.

---

## 7f. 측정 변동폭 — `--reps` 를 정하는 근거 (2026-09-02)

**설정을 고정하고 30회 반복했더니 변동계수가 3.4% 다.** 2노드 128랭크, 캠페인이 찾은 최적 설정 그대로다.

```
평균 552.4 MB/s · 표준편차 18.8 · 최소~최대 511.5~571.8 (폭 10.9%)
```

**`--reps 3` 이면 변동계수가 0.9% 로 떨어진다.** TunIO 논문 설정이 3회이고 그 값이 충분함이 실측으로 확인됐다.

| `--reps` | 1 | 2 | **3** | 5 |
|---|---:|---:|---:|---:|
| 변동계수 | 3.4% | 1.7% | **0.9%** | 0.5% |

**상위 파라미터는 노이즈보다 충분히 크다.** `striping_factor` 88.7 은 표준편차의 4.7배, `cb_nodes` 52.2 는 2.8배다.

**하위 파라미터는 묻힌다.** `cb_buffer_size` 13.5 는 0.7배, `coll_metadata_write` 2.2 는 0.12배다. **`--reps 1` 로 뽑은 sweep 순위에서 10~12위의 상대 순서는 신뢰하지 않는 편이 안전하다.**

**앞으로 실기 캠페인은 `--reps 3` 으로 간다.** 지금까지 자원 절약으로 1회를 썼고 상위 파라미터 판정에는 문제가 없었지만, 결론을 낼 실험에서는 3회를 쓴다.

자세한 분석과 뉴론 비교 계획은 `07-후속-연구-계획.md` §8.2a 에 있다.

---

## 7g. 2026-03 exerciser 캠페인이 그대로 남아 있다 (2026-09-02)

**누리온에 30잡짜리 파라미터 민감도 캠페인 결과가 온전히 보존되어 있다.** 오토튜너 작업보다 다섯 달 앞선 2026-03-30 실험이고, 우리가 지금 튜닝하는 파라미터 중 셋을 이미 격자로 훑어 놓았다.

**어디에 있는지부터 적는다.** 벤치마크 출력은 `/scratch/$USER/h5bench_exerciser/exerciser-*/​*.pbs/ToBeDelete_stdout` 30개다. Darshan 원본은 `/home01/$USER/10-log/darshan-log/h5bench-exerciser-characterization/` 에 30개(총 1.4GB)가 남아 있다. 잡 스크립트는 `/home01/$USER/03-job-script/`, JSON 설정은 `/home01/$USER/02-config/` 에 있고 h5bench 드라이버 로그도 같은 곳에 있다.

**격자는 3×2×5다.** 배열 차원 1D/2D/3D, I/O 모드 collective/independent, 그리고 np256에서 스트라이프 셋(1MiB×1, 4MiB×4, 64MiB×16)과 4MiB×4에서 np 셋(8/64/256)이다. 벤치마크는 h5bench **exerciser**이고 랭크당 128MiB를 10회 반복해서 쓴다.

### 대역폭은 벤치마크 자신의 값을 쓴다

**Darshan DXT로 계산한 대역폭은 independent 모드에서 신뢰할 수 없다.** 3월 분석 노트북(`~/Documents/home01/11-analysis/.../analysis.ipynb`)은 DXT_MPIIO 레코드의 바이트 합을 시간 창으로 나눠 대역폭을 구했다. collective 실행에서는 맞지만 independent 실행에서는 틀린다.

**이유는 DXT가 랭크당 4MiB에서 조용히 잘리기 때문이다.** 3D independent 세 실행 모두 랭크당 정확히 52,426 레코드에서 멈췄다. independent 모드는 2KiB(3D) 또는 32KiB(2D) 단위로 쪼개 쓰므로 연산 수가 폭발하고 버퍼 한도에 먼저 닿는다. 그 결과 3D independent np256은 전체 320GiB 중 8%만 추적됐다. `darshan-dxt-parser` 는 이때 경고를 내지 않는다. 노트북이 붙인 `[WARNING]` 헤더는 DXT_POSIX 오버플로에 대한 것이고 DXT_MPIIO 유실과는 다른 사건이다.

**그래서 exerciser 가 stdout 에 직접 출력하는 `RawWrBDWTH` 를 쓴다.** 이 값은 전체 데이터를 자기 시간으로 나눈 것이라 랭크별 값의 최솟값이 곧 최저 랭크 기준 집계 대역폭이다. `Min`/`Med`/`Max`/`Avg`/`Std` 는 랭크가 아니라 **10회 반복에 대한 모멘트**다(소스의 `NUM_ITERATIONS 10`). 즉 공짜로 반복 측정 오차를 얻는다. 단위는 MiB/s다.

**복원한 표는 `traces/exerciser-characterization-2026-03-30.csv` 에 있다.** 그림 스크립트는 `traces/plot-exerciser-characterization.py` 다.

### 로그에서 복원되는 파라미터와 안 되는 파라미터

**Lustre 스트라이프는 실측값으로 복원된다.** Darshan 의 LUSTRE 모듈이 `LUSTRE_COMP1_STRIPE_SIZE`, `LUSTRE_COMP1_STRIPE_COUNT`, `LUSTRE_NUM_COMPONENTS`, 그리고 배정된 OST ID 목록까지 남긴다. 20개 로그에서 뽑아 디렉터리 이름과 전부 일치하는 것을 확인했다. **선언이 아니라 파일시스템이 실제로 적용한 값이므로 검증에 쓸 수 있다.**

**collective 여부도 복원된다.** `MPIIO_COLL_WRITES` 가 collective 실행에서 정확히 10×np 이고 independent 실행에서 0이다. `H5F_USE_MPIIO` 는 30건 전부 1이다. 흥미로운 점은 collective 실행에도 `MPIIO_INDEP_WRITES` 가 125건 남는다는 것이다. 메타데이터 쓰기는 독립으로 나간다.

**그러나 MPI-IO 힌트 값은 어디에도 없다.** `MPIIO_HINTS` 는 힌트를 설정한 **호출 횟수**일 뿐이고 키-값을 저장하지 않는다. Darshan 에 `MPIIO_HINT_*` 같은 카운터는 존재하지 않는다. **`cb_nodes`, `cb_buffer_size`, `romio_cb_write`, `striping_factor`, `striping_unit` 을 이 로그에서 되살릴 방법은 없다.**

**다만 이 캠페인에서는 그것들이 애초에 설정되지 않았다.** 스트라이프는 MPI 힌트가 아니라 잡 스크립트의 `lfs setstripe -S 4M -c 4` 로 디렉터리에 걸었다. 그리고 툴체인이 GCC 10.2 + OpenMPI 3.1.0 이다. §7c 에서 이 조합의 ROMIO 에 Lustre 드라이버가 없어 힌트가 전부 무시된다는 것을 확인했으므로, **설정했더라도 통하지 않았을 것이다.** 로그에 없는 이유와 효과가 없었을 이유가 같다.

### 오토튜너에 쓸 수 있는 것

**30개의 라벨된 측정점이 생겼다.** 좌표는 (스트라이프 크기, 스트라이프 개수, collective 여부)이고 문맥은 (배열 차원, 랭크 수)다. 우리 12차원 공간의 3차원 부분공간이지만 §7d 에서 실측 1·2위였던 `striping_factor` 와 `cb_nodes` 계열을 포함한다.

**곧바로 확인되는 사실 둘이 있다.** 스트라이프만 바꿔 2D independent np256 에서 **31.2배** 차이가 났다. 그리고 최적 스트라이프가 문맥마다 다르다 — 1D 는 4MiB×4, 2D independent 는 64MiB×16, 3D collective 는 1MiB×1 이다. **단일 정답이 없다는 것이 오토튜닝의 전제이므로 이 캠페인이 그 전제의 실측 근거가 된다.**

**주의할 점은 이 값들이 Intel MPI 가 아니라는 것이다.** §7c 이후 우리 실험은 icc+impi 로 옮겼다. 스트라이프 효과는 `lfs setstripe` 로 걸었으므로 툴체인과 무관하게 유효하지만, MPI 힌트 계층의 효과는 여기 없다. **두 캠페인의 절대 대역폭을 직접 비교하면 안 된다.**

---

## 7h. 벤치마크 빌드 행렬과 exerciser 어댑터 (2026-09-09)

**결론부터. GCC+OpenMPI 는 11개 벤치마크를 전부 빌드하지만 MPI-IO 힌트가 죽어 쓸 수 없다. icc+impi 는 힌트가 살지만 지금까지 패턴 둘만 빌드했다. exerciser 가 그 긴장을 푼다 — 외부 의존이 전혀 없는 단일 소스 파일이다.**

### GCC+OpenMPI 는 전부 빌드된다

2026-03 빌드(`~/01-benchmark/installation-home/h5bench-gcc/bin`)에 열한 개가 있다.

```
h5bench_exerciser          h5bench_write               h5bench_read
h5bench_append             h5bench_overwrite           h5bench_write_unlimited
h5bench_write_var_normal_dist
h5bench_hdf5_iotest        h5bench_amrex_sync
h5bench_macsio             h5bench_openpmd_read / _write
```

**그래서 처음에 GCC+OpenMPI 로 시작한 것이다.** 그런데 §7c 에서 OpenMPI 3.1.0 의 ROMIO 에 Lustre 드라이버가 없어 `striping_factor`, `striping_unit`, `cb_nodes`, `cb_buffer_size` 가 전부 무시되는 것을 확인했다. §7d 의 영향도 순위에서 이 넷은 1·3·2·9 위다.

**따라서 OpenMPI 빌드로 전이 실험을 하면 안 된다.** 상위 파라미터가 죽은 공간에서 전이를 재는 셈이고, 그 결과는 어떻게 나와도 해석할 수 없다. **"OpenMPI 로 컴파일되는가" 는 우리에게 유용한 질문이 아니다.**

### icc+impi 에서 무엇이 막히는지

**막히는 것은 컴파일러가 아니라 CMake 의 외부 의존 내려받기다.** MACSio 는 json-cwx 를, AMReX 와 openPMD 는 각자의 저장소를 CMake 가 빌드 중에 가져온다. 계산 노드에서 외부망이 막혀 있고 로그인 노드에서도 52분 멈춘 적이 있다(§7b). h5bench_write 를 `mpiicc -std=gnu99` 로 직접 컴파일해 우회한 것이 그래서였다.

**exerciser 는 그 목록에 없다.** `exerciser/h5bench_exerciser.c` 하나가 전부이고 include 는 표준 헤더와 `hdf5.h`, `mpi.h` 뿐이다. `commons/h5bench_util.c` 도 쓰지 않는다.

```sh
mpiicc -std=gnu99 -O2 -o h5bench_exerciser exerciser/h5bench_exerciser.c \
    -I$HDF5/include -L$HDF5/lib -lhdf5 -lz -lm -Wl,-rpath,$HDF5/lib
```

**metadata_stress 도 후보다.** `hdf5_iotest.c` + `dataset.c` + `ini.c` + `configuration.c` 네 파일이고 외부 의존이 없다. 메타데이터 경로를 때리므로 `meta_block_size`, `col_meta_ops`, `mdc_conf` 가 살 가능성이 높다.

**아직 실기에서 컴파일해 보지 않았다.** 위 명령이 통하는지가 다음 세션의 첫 확인 항목이다.

### exerciser 가 전이 실험에 맞는 이유

**한 바이너리 안에서 분포 이동이 나온다.** `--numdims` 와 `--dimranks` 가 하이퍼슬랩 모양을 바꾸고, §7g 에서 최적 스트라이프가 1D 는 4MiB×4, 2D independent 는 64MiB×16, 3D collective 는 1MiB×1 로 갈렸다. **응용을 새로 빌드하지 않고 prior 를 벗어난 조건을 만들 수 있다.**

**동기 HDF5 API 를 쓴다.** `H5Fcreate`, `H5Dcreate`, `H5Dwrite` 다. h5bench_write 때문에 넣은 async 훅이 여기서는 필요 없고, shim 의 원래 훅이 바로 발동한다.

**3월 데이터 30점이 대조군으로 이미 있다.** 어댑터가 옳게 붙었는지 검증할 기준값이 있다는 뜻이다.

**`--indepio` 는 튜닝 대상이 아니다.** paper12 공간에 dxpl collective 플래그가 없으므로 이것은 문맥 변수로 남는다. 전이 실험에서 조건을 가르는 데 쓸 수 있다.

### 어댑터에 박아 넣은 함정 셋

**`--numdims` 가 `--minels`, `--dimranks`, `--bufmult` 보다 먼저 와야 한다.** 커널이 확인하고 `exit(-1)` 한다. 각 플래그가 인자 목록에서 numDims 개의 정수를 읽기 때문이다. 어댑터가 순서를 보장한다.

**대역폭 단위가 MiB/s 다.** 커널이 `1048576.0` 으로 나눈다(`h5bench_exerciser.c:600`). 패턴 벤치마크의 `Raw write rate` 는 human-readable 로 접미사가 붙는데 이쪽은 접미사가 없다. **MB/s 로 착각하면 4.9% 높게 읽는다.** 비율만 쓰는 목적함수에는 영향이 없지만 두 응용의 절대값을 나란히 놓을 때는 환산해야 한다.

**`--metacoll` 을 주지 않는다.** 커널이 스스로 `H5Pset_coll_metadata_write` 와 `H5Pset_all_coll_metadata_ops` 를 부르고, 둘 다 튜닝 대상이다. shim 이 나중에 덮으므로 결과는 우리 값이 이기지만 명령줄이 실험 기록으로서 거짓이 된다. MACSio 의 `--sieve_buf_size` 와 같은 이유다.

**부수적으로 하나 더.** 출력 파일 이름이 `hdf5TestFile-<rand()>` 이고 `srand()` 가 시계로 초기화되므로 후보마다 이름이 다르다. `--keepfile` 을 주지 않으면 커널이 `unlink` 하지만, 중간에 죽으면 남는다. 어댑터 preamble 이 `rm -f hdf5TestFile-*` 를 돌린다 — Lustre 는 생성 시점에 레이아웃을 고정하므로 남은 파일이 다음 후보에게 이전 스트라이프를 물려준다.

### 검증한 것

`Avg` 행 4열을 긁는 정규식이 3월 실측과 일치한다. 1D collective np256 stripe4M 에서 3322.659 MiB/s 를 뽑아 3.4841 GB/s 이고, `traces/exerciser-characterization-2026-03-30.csv` 의 3.484 와 같다. 5행 중 `Avg` 하나만 매칭된다.

```sh
python3 -m autotuner run --framework h5tuner --dry-run \
    --app exerciser --exerciser-bin $EX \
    --exerciser-numdims 1 --exerciser-minels 16777216 --exerciser-dimranks 128
```

3D 도 통한다: `--exerciser-numdims 3 --exerciser-minels 256,256,256 --exerciser-dimranks 8,4,4`. 개수가 안 맞으면 거부한다.

---

## 8. 계산 자원 예산

**진단 잡은 소모가 미미하다.** probe와 diag를 합쳐 2노드로 약 3분이다.

**진짜 소모는 캠페인이다.** 원래 계획(4노드, sweep 800점, 40세대 × 15개체, 반복 5회)이면 sweep 53 노드·시간, 캠페인 200 노드·시간으로 합계 250 노드·시간이 넘는다.

**줄이는 방법 넷.** 사용자가 2026-08-31에 자원 절약을 요청해 아래를 적용하기로 했다.

노드를 4개에서 2개로 줄인다. 절반이 된다. `cb_nodes`와 striping의 효과는 2노드에서도 나타난다.

sweep 표본을 800에서 300으로 줄인다. 파라미터당 각 값에 100개가 붙어 순위를 뽑기에 충분하다.

반복을 5회에서 1회로 줄인다(`--reps 1`). 탐색 단계에서만 그렇게 하고, 최종 비교에서는 늘린다.

세대를 40에서 20으로, 개체를 15에서 8로 줄인다. 후보가 600개에서 160개가 된다.

**합치면 250 노드·시간이 15 정도가 된다.**

**논문 설정에서 벗어나는 것은 기록해야 한다.** 세대 수(#16)와 반복 축약(#14)은 `02-설계-결정.md` §7에 이미 있는 항목이므로 값만 갱신하면 된다.

---

## 9. 확인하지 않은 것

**`lfs getstripe`로 스트라이핑이 실제로 적용되는지는 아직 확인 중이다.** h5bench write로 확인 잡을 냈다(`23798674`). 이것이 튜닝 전체의 전제다.

**h5bench write가 이 환경에서 도는지도 같은 잡에서 확인한다.** MACSio가 깨졌으므로 대조군을 함께 넣었다.

**PyTorch 유무를 확인하지 않았다.** 없으면 폴백 백엔드가 쓰인다.
