# H5Tuner를 TunIO처럼 확장하기 — 작업 계획

> **이 문서는 무엇인가.** TunIO 논문을 읽고 도출한 **후속 연구 후보 10건**(§8)과 그 근거다.
>
> **이것은 연구 어젠다다.** 이 저장소에서 유일하게 "결과"가 아니라 "앞으로 할 일"을 담은 문서다. 2026-09-14 에 공개하기로 결정했다. 나머지 문서는 이 파일 없이도 완결된다.
>
> **언제 읽나.** 연구 방향을 정할 때. 구현이나 실행에는 필요 없다.

**작성일:** 2026-08-11
**기준 커밋:** `f8ead15` (브랜치 `main`)
**근거 논문:** Rajesh 외, *TunIO: An AI-powered Framework for Optimizing HPC I/O*, IPDPS 2024, pp.494–504. DOI `10.1109/IPDPS57955.2024.00050`. Zotero 항목 키 `UCN6VL82`
**함께 볼 문서:** [`03-원본-코드-분석.md`](03-원본-코드-분석.md) — 저장소 구조와 기존 버그 분석
**추적 상태:** 2026-09-14 부터 공개 저장소(`h5tuner-ext`)에 함께 올린다.

---

## 이 문서의 결론부터

**할 일은 세 덩어리다. 그중 하나는 지금 당장 고쳐야 하는 결함이고, 하나는 논문을 따라가는 구현이고, 하나는 연구 주제다.**

첫째, **지금 우리 코드의 파라미터 설정에 결함이 있다.** 원래 설계는 6개 차원을 탐색하게 되어 있었다. 그런데 두 차원이 사람 손으로 제거되어 지금은 4개만 탐색한다. 게다가 남은 차원 중 하나는 값의 범위가 잘못되어 있다. 이건 논문과 무관하게 고쳐야 한다. §2에서 설명한다.

둘째, **논문의 세 컴포넌트를 구현하는 순서를 논문 순서대로 하면 안 된다.** 논문은 소스 분석 → 파라미터 선택 → 조기 종료 순으로 설명한다. 하지만 이건 실행 순서일 뿐이다. 구현 난이도와 효과를 따지면 조기 종료를 먼저 만들어야 한다. §4에서 설명한다.

셋째, **파라미터를 마음대로 늘릴 수 없다.** 늘리는 방법이 두 가지 있는데, 하나는 안전하고 하나는 위험하다. 위험한 쪽은 강화학습을 망가뜨린다. §5에서 설명한다. 이 절이 가장 중요하다.

---

## 1. TunIO는 무엇이고 우리 코드와 어떤 관계인가

### 1.1 H5Tuner가 하는 일

**H5Tuner는 애플리케이션을 다시 컴파일하지 않고 HDF5의 I/O 설정을 바꿔주는 도구다.**

원리는 함수 가로채기다. 리눅스에서 `LD_PRELOAD` 환경변수에 라이브러리를 지정하면, 그 라이브러리가 원래 함수보다 먼저 호출된다. H5Tuner는 이 방식으로 `H5Fcreate` 같은 HDF5 함수를 가로챈다.

가로챈 다음에는 이렇게 동작한다. 먼저 현재 디렉터리에서 `config.xml`을 읽는다. 그 파일에 적힌 파라미터 값을 HDF5와 MPI에 주입한다. 그리고 원래의 `H5Fcreate`를 호출한다. 애플리케이션은 자기 설정이 바뀐 것을 모른다.

주입 코드는 [`src/autotuner_hdf5.c`](src/autotuner_hdf5.c)에 있다. 전체 저장소는 2만 3천 줄이지만 실제 로직은 400줄 정도다. 나머지 88%는 복제된 테스트와 예제다.

### 1.2 자동 튜닝은 별도 프로그램이 한다

**설정 파일을 바꿔가며 최적값을 찾는 일은 라이브러리가 아니라 외부 스크립트가 한다.**

이유가 있다. `config.xml`은 `H5Fcreate`가 호출될 때 한 번만 읽힌다. 실행 중에 파일을 바꿔도 이미 만들어진 파일에는 영향이 없다. 그래서 설정을 바꾸려면 애플리케이션을 처음부터 다시 실행해야 한다.

그 재실행을 반복하는 것이 [`evo/evolve.py`](evo/evolve.py)다. 유전 알고리즘으로 후보 설정을 만들고, 매 후보마다 `config.xml`을 새로 쓰고, 앱을 실행하고, 걸린 시간을 재고, 좋은 후보를 골라 다음 세대를 만든다.

기본 설정으로 앱을 몇 번 실행하는지 계산해보자. 개체 15개, 세대 40번, 후보마다 5번 반복 측정이다. 곱하면 3,000번이다. 앱 한 번이 1분이면 50시간이 걸린다. HPC 배치 잡을 전제로 만든 것이다.

### 1.3 TunIO가 바꾼 것

**TunIO는 H5Tuner의 주입 라이브러리를 그대로 쓴다. 바꾼 것은 그 위의 탐색 스크립트다.**

논문 §III-A에 이렇게 적혀 있다. "It builds off of the existing H5Tuner library, using its mechanisms to override the configuration parameters of HDF5 applications via an XML file."

즉 우리 저장소의 C 코드가 TunIO의 주입 계층 그대로다. TunIO는 `evolve.py`가 하던 일을 DEAP 기반 파이프라인으로 바꾸고, 거기에 세 가지를 추가했다.

첫 번째는 **애플리케이션 I/O 추출**이다. 소스 코드를 분석해서 I/O에 필요한 부분만 남긴 작은 프로그램을 만든다. 튜닝할 때 전체 앱 대신 이걸 실행하면 한 번 평가하는 시간이 줄어든다.

두 번째는 **똑똑한 설정 생성**이다. 파라미터를 전부 튜닝하지 않고 매 회차에 일부만 고른다. 어느 것을 고를지는 강화학습이 결정한다.

세 번째는 **조기 종료**다. 더 튜닝해도 이득이 없다고 판단하면 멈춘다. 멈출 시점도 강화학습이 결정한다.

### 1.4 논문이 보고한 효과

**같은 성능에 도달하는 데 걸리는 튜닝 시간이 약 73% 줄었다.**

BD-CATS라는 애플리케이션을 Cori 슈퍼컴퓨터에서 500노드 1600프로세스로 튜닝한 결과다.

기존 H5Tuner를 끝까지 돌리면 1,750분이 걸리고 90.8 GB/s에 도달한다. 조기 종료를 휴리스틱으로 붙이면 538분에 47.7 GB/s다. TunIO는 468분에 88 GB/s다.

최대 성능만 보면 H5Tuner가 3% 앞선다. 하지만 시간을 3.7배 쓴다.

논문은 이 트레이드오프를 재기 위해 RoTI라는 지표를 만들었다. 계산은 간단하다. 튜닝으로 올라간 대역폭을 튜닝에 쓴 시간으로 나눈다. 단위는 "튜닝 1분당 올라간 MB/s"다. 이 지표로 보면 H5Tuner + 휴리스틱이 41.6이고 TunIO가 215다.

---

## 2. 지금 우리 코드의 파라미터 설정은 잘못되어 있다

### 2.1 결론

**원래 6개 차원을 탐색하도록 설계되었는데 지금은 4개만 탐색한다. 남은 것 중 하나는 값 범위가 잘못되어 있다.**

이건 논문을 따라가는 작업과 별개다. 지금 상태로는 무엇을 붙여도 측정이 왜곡된다.

### 2.2 현재 탐색하는 것

[`evo/evolve.py:89`](evo/evolve.py#L89)의 `ALLESES`에 유전자 5개가 정의되어 있다. 각 유전자는 후보 값 목록을 가진다. 유전 알고리즘은 그 목록에서만 값을 고른다.

| 유전자 | 파라미터 | 하는 일 | 후보 값 |
|---|---|---|---|
| 0 | `striping_factor` | 파일을 몇 개의 OST에 나눠 쓸지 | 10개: `4, 8, 16, 24, 32, 48, 64, 96, 128, -1` |
| 1 | `striping_unit` | 한 조각의 크기 | 8개: 1MB부터 128MB까지 |
| 2 | `cb_nodes` | 쓰기를 모아주는 프로세스 개수 | 12개: 1부터 256까지 |
| 3 | `alignment` | HDF5 객체를 어느 경계에 정렬할지 | 14개: `"1, 1"`부터 `"16384, 262144"`까지 |
| 4 | `sieve_buf_size` | 부분 쓰기용 버퍼 크기 | **1개: 512MB** |

유전자가 5개지만 실질 자유도는 4다. 이유는 두 가지다.

`sieve_buf_size`는 후보가 하나뿐이다. 고를 게 없으니 유전자가 아니라 상수다.

`cb_buffer_size`는 애초에 독립 유전자가 아니다. [`evolve.py:216`](evo/evolve.py#L216)에서 `striping_unit` 값을 그대로 가져다 쓴다. 코드 주석에 "Ruth(David's) Suggestion"이라고만 적혀 있다.

그래서 실제 탐색 공간은 10 × 8 × 12 × 14 = **13,440가지**다. 논문이 말하는 21.8억과 비교하면 16만 배 작다.

### 2.3 원본을 보면 원래 6차원이었다

**분석 시작 지점인 커밋 `eeb75ab`의 원본 코드에 주석 처리된 후보 목록들이 남아 있다.**

```python
# cb_buf_size = [4194304, 8388608, 16777216, 33554432, 67108864, 134217728];
# NEW, NOT FOR IPDPS: alignment = [65536, 131072, 262144, 524288, 1048576, 2097152];
#alignment = [65535, 131070, 262140, 524280, 1048560, 2097120];
alignment = ["1, 1", "0, 4096", ... "16384, 262144"]
# PRABHAT
#siv_buf_size = [65535, 131070, 262140, 524280, 1048560, 2097120];
siv_buf_size = [536870912]
```

`cb_buffer_size`는 원래 6개 후보를 가진 독립 유전자였다. 4MB부터 128MB까지다. 주석 처리된 `# this_cb_buf_size = genome[3]`이 그 흔적이다.

`sieve_buf_size`도 원래 6개 후보가 있었다. 64KB부터 2MB까지다. 이걸 512MB 하나로 바꾼 사람이 남긴 표시가 "PRABHAT"이다.

즉 설계는 6차원이었고 사람이 4차원으로 줄였다. 줄인 근거는 코드에 남아 있지 않다. 이름 두 개뿐이다.

### 2.4 구체적인 결함 다섯 가지

**첫째, `sieve_buf_size`의 512MB는 이상한 값이다.**

HDF5의 기본값은 64KB다. 512MB는 그것의 8,192배다. 이 버퍼는 프로세스마다 잡히므로 128프로세스면 이론상 64GB가 된다.

왜 이런 값이 들어갔을까. 한 가지 가설이 있다. **이 파라미터가 애초에 효과가 없어서 상수로 접혔을 수 있다.**

HDF5의 data sieving은 청킹하지 않은 데이터셋에 부분 쓰기를 할 때 쓰는 최적화다. 그런데 MPI-IO 드라이버를 쓰면 ROMIO의 two-phase I/O가 그 역할을 대신한다. 그렇다면 병렬 HDF5에서는 이 버퍼가 거의 작동하지 않는다. 효과가 없으니 "최대한 크게 잡아서 제약을 없애자"는 뜻으로 512MB를 넣었다고 볼 수 있다.

이건 추정이다. 확인해야 한다. 다행히 확인 비용이 싸다. 값을 여러 개로 바꿔서 성능이 변하는지만 보면 된다. 어느 쪽으로 결론이 나도 지금 상태는 틀렸다. 효과가 없으면 탐색 대상에서 빼는 게 맞고, 있으면 원래 범위를 복구해야 한다.

**둘째, `alignment`의 값 범위가 잘못되어 있다.**

현재 최대값은 262144, 즉 256KB다. 그런데 `striping_unit`의 최소값이 1MB다. 정렬 단위가 스트라이프 크기보다 작다.

정렬을 쓰는 주된 이유는 데이터를 스트라이프 경계에 맞추는 것이다. 지금 범위로는 그게 구조적으로 불가능하다.

원본 코드를 보면 저자들도 알고 있었다. `NEW, NOT FOR IPDPS`라고 표시된 미사용 목록이 2MB까지 올라간다. IPDPS 논문 실험에서는 의도적으로 빼놓은 것이고, 그대로 방치되어 있다.

**셋째, 주석 처리된 목록에 숫자 오류가 있다.**

`65535, 131070, 262140, 524280, 1048560, 2097120`을 보자. 2의 거듭제곱처럼 보이지만 아니다. 65535는 2¹⁶ − 1이다. 그 뒤는 65535를 2배, 4배, 8배 한 값들이다.

정렬이나 버퍼 크기에서 2의 거듭제곱이 아니면 정렬 자체의 목적이 무너진다. `NEW` 줄이 이걸 65536부터 시작하도록 고쳤지만 활성화되지 않았다. 주석 처리된 `sieve_buf_size` 목록도 같은 숫자를 복사한 것이다.

**넷째, 주석과 코드가 서로 모순된다.**

`striping_factor`의 주석은 이렇다. "The default value for stripe_count is 1. We are going to skip 1 and 4 because they are shown to be very bad."

1과 4를 건너뛴다고 써놓고 목록에는 `4`가 들어 있다. 원본부터 그렇다. 주석이 틀렸는지 `4`를 빼야 하는지 알 수 없다.

**다섯째, `alignment`가 문자열 하나로 두 값을 담고 있다.**

`"1024, 65536"` 같은 형태다. threshold와 alignment 두 값이 한 유전자에 들어 있다. 그래서 변이가 일어나면 두 값이 함께 바뀐다. 하나만 조금 움직이는 것이 불가능하다.

또 threshold가 `0`인 것과 `1`인 것은 사실상 같은 동작이다. 둘 다 "모든 객체를 정렬"을 뜻한다. 서로 다른 후보인데 결과가 같다.

---

## 3. 준비 작업 — 다른 모든 것의 전제

### 3.1 결론

**세 가지를 먼저 만들어야 한다. 파라미터 공간 정리, 대역폭 측정, 그리고 기록·재생 장치다.**

이 셋이 없으면 논문의 컴포넌트를 구현해도 검증할 수가 없다.

### 3.2 파라미터 공간 정리

먼저 §2에서 지적한 결함을 고친다. 죽은 유전자 두 개를 살리고, `alignment` 범위를 넓히고, 숫자 오류를 바로잡는다.

그다음 논문에 있고 우리에게 없는 파라미터를 추가한다. 논문 §III-C가 스윕한 파라미터는 11개다. 그중 우리 코드에 없는 것이 다섯 개다.

| 파라미터 | 하는 일 | 필요한 HDF5 함수 | 난이도 |
|---|---|---|---|
| `meta_block_size` | 메타데이터를 최소 이 크기 단위로 할당한다. 작은 쓰기를 합치는 효과가 있다 | `H5Pset_meta_block_size` | 쉽다. `sieve_buf_size`와 구조가 같다 |
| `chunk_cache` | 청크를 캐시하는 슬롯 수와 크기 | `H5Pset_cache` 또는 `H5Pset_chunk_cache` | 보통. 인자가 4개라 파싱이 필요하다 |
| `coll_metadata_write` | 메타데이터 쓰기를 collective로 한다 | `H5Pset_coll_metadata_write` | 쉽다. **HDF5 1.10 이상 필요** |
| `col_meta_ops` | 메타데이터 읽기를 collective로 한다 | `H5Pset_all_coll_metadata_ops` | 쉽다. **HDF5 1.10 이상 필요** |
| `mdc_conf` | 메타데이터 캐시 세부 설정 | `H5Pset_mdc_config` | 어렵다. 20개가 넘는 필드를 가진 구조체다 |

고칠 곳은 두 군데다. [`src/autotuner_hdf5.c:123-176`](src/autotuner_hdf5.c#L123-L176)의 분기에 항목을 추가하고, [`:264-265`](src/autotuner_hdf5.c#L264-L265)에 호출을 한 줄씩 추가한다.

**주의할 점이 있다. HDF5 버전을 올려야 한다.**

[`.misc/config.sh:24`](.misc/config.sh#L24)가 `hdf5-1.8.12`를 고정하고 있다. 위 표의 `coll_metadata_write`와 `col_meta_ops`는 HDF5 1.10.0에서 도입되었다. 1.8.12에서는 컴파일이 안 된다.

두 가지를 함께 해야 한다. 부트스트랩 스크립트의 버전을 올린다. 그리고 새 함수 호출을 `#if H5_VERSION_GE(1,10,0)`으로 감싸서 1.8 빌드가 깨지지 않게 한다.

같은 파일의 다운로드 URL도 확인이 필요하다. 2013년경 경로라서 지금 살아 있는지 알 수 없다.

**거의 공짜로 얻는 것도 있다.**

ROMIO 힌트는 새 C 코드가 필요 없다. `set_mpi_parameter` 함수는 이름을 받아서 `MPI_Info_set`에 그대로 넘기는 일반 코드다. 그래서 [`:257-262`](src/autotuner_hdf5.c#L257-L262) 옆에 이름만 한 줄씩 추가하면 된다.

추가할 수 있는 이름은 `romio_cb_write`, `romio_cb_read`, `romio_ds_write`, `romio_ds_read`, `ind_wr_buffer_size`, `ind_rd_buffer_size`다. 논문의 12개에 없는 것들이다.

단, 여기에는 §5에서 설명하는 문제가 걸린다. 파라미터 개수를 늘리는 일이라서 강화학습에 부담이 된다. 먼저 §5를 읽기를 권한다.

**이미 있는데 안 쓰는 것도 하나 있다.**

`chunk`는 shim에 구현되어 있다. 다만 `H5Fcreate`가 아니라 `H5Dcreate1`과 `H5Dcreate2` 경로에서 주입된다([`:335`](src/autotuner_hdf5.c#L335), [`:373`](src/autotuner_hdf5.c#L373)). 데이터스페이스의 차원을 읽어서 `H5Pset_chunk`를 호출한다.

`evolve.py`는 이걸 탐색하지 않는다. 논문의 스윕 목록에도 없다. 그런데 논문 §II-A는 "HDF5 청크 하나가 Lustre 스트라이프 하나에 대응하는 것이 이상적"이라고 명시한다. 중요하다고 말하면서 아무도 튜닝하지 않는 파라미터다.

### 3.3 목적함수를 시간에서 대역폭으로 바꾸기

**이게 준비 작업 중 가장 중요하다.**

현재 `evolve.py`는 앱 전체의 벽시계 시간을 재고 그것을 최소화한다. TunIO는 대역폭을 재고 그것을 최대화한다.

논문의 정의는 이렇다. `perf = (1 − α)·BW_r + α·BW_w`. 여기서 α는 전체 전송량 중 쓰기가 차지하는 비율이다. BW는 읽기와 쓰기 각각의 대역폭이고 단위는 MB/s다.

**이 차이를 무시하면 논문의 설계가 통째로 무너진다.** 이유는 네 가지다.

조기 종료 학습이 깨진다. 논문은 튜닝 성능이 로그 곡선을 따라 **올라간다**고 가정하고 그런 곡선으로 학습시킨다. 시간은 내려간다. 곡선의 방향, 보상의 부호, 정체 판정이 모두 뒤집힌다.

보상 함수가 깨진다. 논문의 보상은 대역폭을 `1/(BW_single × num_nodes)`로 정규화한다. MB/s 스케일을 전제한 식이다.

RoTI를 계산할 수 없다. RoTI는 대역폭 증가분을 시간으로 나눈 값이다. 기준값으로 "튜닝하지 않은 상태의 대역폭"이 필요하다.

I/O만 보는 의미가 없어진다. 벽시계 시간에는 연산 시간이 섞인다. 논문이 I/O 커널을 뽑는 이유 자체가 I/O만 남기려는 것이다.

**대역폭을 재는 방법은 세 가지다.**

첫째는 Darshan을 쓰는 것이다. 논문이 언급한 방법이다. 앱을 고치지 않아도 되고 읽기·쓰기 비율까지 나온다. 단점은 클러스터에 설치가 필요하고, `LD_PRELOAD`에 Darshan과 H5Tuner를 함께 넣어야 한다는 것이다. 둘 다 함수 가로채기를 쓰므로 충돌 가능성을 확인해야 한다.

둘째는 앱의 출력을 파싱하는 것이다. 정규식으로 앱이 찍는 대역폭 값을 잡는다. 즉시 가능하다. `measure()` 함수가 이미 표준출력을 캡처하고 있어서([`evolve.py:305-310`](evo/evolve.py#L305-L310)) 새 훅이 필요 없다. 단점은 앱이 값을 출력해야만 된다는 것이다.

셋째는 파일 크기를 시간으로 나누는 것이다. 의존성이 없다. 하지만 연산 시간이 섞이고 읽기는 잴 수 없다.

**둘째를 기본으로 하고 첫째를 실측 경로로 두기를 권한다.** 둘째는 이 맥에서 가짜 앱으로 바로 검증할 수 있다. 첫째는 클러스터 작업이므로 뒤로 미룬다.

### 3.4 기록하고 재생하는 장치

**강화학습 학습을 클러스터 밖으로 빼내는 것이 목적이다.**

논문의 두 강화학습 에이전트는 모두 오프라인 사전학습을 요구한다. 파라미터 선택기는 대표 커널에 파라미터 스윕을 돌린 데이터로 학습한다. 조기 종료기는 합성 곡선으로 학습한다.

학습 루프를 매번 실기에서 돌리면 비용을 감당할 수 없다. 필요한 것은 단순하다. **설정과 성능의 쌍을 파일에 기록하고 나중에 재생하는 층이다.**

```
evolve.py --record traces/hacc-4node.jsonl     # 실기에서 한 번, 모든 평가를 기록
evolve.py --replay traces/hacc-4node.jsonl     # 앱 실행 없이 기록된 값으로 학습
```

이걸 만들면 네 가지가 가능해진다.

강화학습 에이전트를 이 맥에서 반복 학습할 수 있다.

탐색 알고리즘을 바꿔가며 **같은 측정치 위에서** 비교할 수 있다. 선택 방식을 룰렛에서 토너먼트로 바꾸면 정말 좋아지는지 같은 질문에 답할 수 있다.

논문 Fig 10(a)의 실패 사례를 재현해볼 수 있다. 휴리스틱 조기 종료기가 정체 구간에 걸려 14회차에 멈추는 장면이다.

기존 `--dry-run`보다 낫다. `--dry-run`의 `synthetic_cost`는 인공 지형이다. 재생은 실제 지형이다.

설계에서 한 가지 결정할 것이 있다. 기록에 없는 설정을 만나면 어떻게 할지다. 오류를 내거나, 가까운 값으로 보간하거나, 기록된 격자 안에서만 탐색을 허용하는 방법이 있다. 초기에는 세 번째가 가장 정직하다.

---

## 4. 논문의 세 컴포넌트를 어떤 순서로 만들까

### 4.1 결론

**조기 종료를 먼저 만들고, 소스 분석을 마지막에 만든다. 논문 순서와 반대다.**

이유는 세 가지다. 조기 종료는 코드가 가장 적다. 논문이 학습 방법을 거의 다 알려준다. 그리고 MPI나 HDF5 없이 이 맥에서 끝낼 수 있다.

소스 분석은 반대다. 코드가 압도적으로 많다. 그리고 **단독 효과가 가장 작다.**

| 순서 | 무엇 | 논문이 보고한 효과 | 코드량 짐작 | 이 맥에서 검증 |
|---|---|---|---|---|
| 준비 | §3의 세 가지 | 전제 조건 | C 150줄 + 파이썬 250줄 | 일부만 |
| 1단계 | 조기 종료 | 튜닝 예산 73% 감소 (전체 파이프라인 기준) | 파이썬 200~350줄 | **전부 가능** |
| 2단계 | 똑똑한 설정 생성 | 목표 도달이 43회차에서 6회차로 | 파이썬 400~600줄 | 일부만 |
| 3단계 | 소스 분석 | 14% 감소 (단독) / RoTI 9배 (루프 축약 포함) | 파이썬 800~1500줄 | **전부 가능** |

### 4.2 1단계 — 조기 종료

**인터페이스가 아주 좁다.** 논문 Table I을 보면 입력은 "현재 회차 번호"와 "그 회차의 최고 성능" 두 개다. 출력은 "멈춤" 또는 "계속" 하나다.

우리 코드에 그대로 꽂힌다. `evolve()` 함수의 `on_generation` 콜백([`:195-196`](evo/evolve.py#L195-L196))이 이미 매 세대마다 호출된다. 그 반환값을 보고 루프를 끊게만 바꾸면 된다.

**휴리스틱 버전을 먼저 만들어야 한다.**

논문이 비교 대상으로 쓴 규칙은 간단하다. 5회차 동안 5% 이상 개선이 없으면 멈춘다. 20줄이면 된다.

먼저 만드는 이유가 두 가지다. 지금은 40세대를 무조건 완주하므로 즉시 실질 이득이 있다. 그리고 강화학습 버전의 성능을 주장할 때 비교 대상이 필요하다.

부수 효과도 있다. 원본 코드에 `ConvergenceCriteria`라는 종료 조건이 정의되어 있지만 등록하는 줄이 주석 처리되어 죽어 있다. 그 의도를 되살리는 셈이다.

**그다음 강화학습 버전을 만든다.**

논문 §III-D가 학습 방법을 거의 다 알려준다.

관찰의 근거는 이렇다. 튜닝 성능은 로그 곡선을 따른다. 처음에 크게 오르고 점점 완만해진다. 논문 Fig 2가 그걸 보여준다.

그래서 학습 데이터를 **직접 만든다.** 로그 곡선을 여러 개 생성한다. 곡선마다 초기값과 성장률을 다르게 준다. 그리고 노이즈를 섞는다. 노이즈는 곡선 아래로 랜덤하게 내려가는 형태다. 잘못된 파라미터를 잠깐 골랐다가 회복하는 상황을 흉내내는 것이다.

보상은 5회차 지연을 준다. 단기 이득에 끌리는 것을 막기 위해서다.

학습은 대부분의 가상 애플리케이션에서 평균 보상 증가가 5회차에 5% 이하로 정체할 때 끝낸다.

**이 전체가 순수 파이썬과 합성 데이터다. 클러스터가 필요 없다.**

여기서 판단할 것이 하나 있다. 논문은 Keras와 OpenAI Gym을 쓴다. 그런데 우리 `evolve.py`는 표준 라이브러리만 쓴다는 원칙으로 재작성되었다. 충돌한다.

두 가지 길이 있다. 강화학습 부분을 `evo/tunio/` 하위 패키지로 분리하고 거기서만 외부 라이브러리를 쓰는 방법이 있다. 라이브러리가 없으면 휴리스틱으로 폴백한다. 아니면 문제 규모가 작으니(상태 2차원, 행동 2개) 표준 라이브러리만으로 테이블 기반 Q-learning을 만드는 방법도 있다.

**앞의 방법을 권한다.** 신경망을 이산화 테이블로 바꾸면 논문 재현이라 말하기 어려워진다. 다만 폴백 경로는 반드시 두어야 한다.

### 4.3 2단계 — 똑똑한 설정 생성

**핵심은 매 회차에 파라미터 일부만 튜닝하는 것이다.**

인터페이스는 이렇다. 입력은 성능과 지금 쓰고 있는 파라미터 집합이다. 출력은 다음 회차에 쓸 파라미터 집합이다.

**먼저 유전 알고리즘 쪽을 고쳐야 한다.**

"일부만 튜닝한다"는 것은 나머지가 변이와 교차에서 제외된다는 뜻이다. 그리고 고정값을 유지한다. 지금 코드에는 그런 개념이 없다.

[`mutate()`](evo/evolve.py#L125-L131)는 모든 유전자를 확률 0.15로 후보에 넣는다. [`single_point()`](evo/evolve.py#L134-L137)는 유전자 구분 없이 자른다.

그래서 "활성 유전자 집합"을 인자로 받도록 바꿔야 한다. 그리고 비활성 유전자의 고정값을 무엇으로 둘지 결정해야 한다. 기본값으로 둘 수도 있고 지금까지의 최적값으로 둘 수도 있다. **논문에 안 나와 있다.** 최적값이 자연스럽지만 국소 최적에 갇힐 위험이 커진다.

함께 도입할 것이 하나 있다. **토너먼트 선택**이다. 논문 §III-A가 쓰는 방식이다. 개체 3명을 무작위로 뽑아서 상위 2명을 부모로 삼는다. 지금은 룰렛휠을 쓴다([`:140`](evo/evolve.py#L140)).

논문이 토너먼트를 택한 이유가 명시적이다. 엘리티즘 때문에 집단이 과도하게 특화되어 국소 최적에 갇히는 문제를 완화하려는 것이다. 20줄이면 되고, `roulette()` 옆에 나란히 두어 옵션으로 비교할 수 있다.

**두 단계로 나눠 접근하기를 권한다.**

첫 단계는 강화학습 없이 하는 것이다. 논문의 오프라인 학습 전반부는 사실 강화학습이 아니다. 대표 커널에 파라미터 스윕을 돌리고, 성능을 모으고, PCA로 영향이 큰 파라미터를 골라낸다. 이 결과를 그냥 **고정 우선순위**로 쓰면 "영향 큰 것 먼저"의 상당 부분을 얻는다.

PCA는 numpy만 있으면 된다. 없으면 상관계수만으로도 순위는 나온다. §3.4의 기록·재생 장치가 있으면 분석을 반복할 수 있다.

**다만 여기서 멈추면 문제가 있다.** 논문 §V-C는 H5Tuner 후속 연구가 "고정된 개수의 영향 큰 파라미터로 축소"한 것을 비판한다. 애플리케이션과 스케일에 걸쳐 일반화되지 않는다는 것이다. 첫 단계만 하고 멈추면 논문이 비판한 것을 다시 만드는 셈이다.

두 번째 단계가 강화학습 버전이다. 논문의 구조는 이렇다. 상태 관측기가 애플리케이션과 환경의 관계를 상태로 요약한다. 부분집합 선택기가 그 상태를 보고 다음 집합을 고른다. 앞은 문맥적 밴딧이고 뒤는 Q-learning이다.

보상은 성능에서 페널티를 뺀 값이다. 페널티는 고른 파라미터 개수에 비례한다. 여기에도 5회차 지연을 준다.

**이 단계에는 확장성 문제가 있다. §5에서 따로 다룬다.**

### 4.4 3단계 — 소스 분석으로 I/O 커널 만들기

**논문의 알고리즘은 재구현 가능한 수준으로 서술되어 있다.**

흐름은 이렇다. 소스를 `clang-format`으로 정리한다. libclang으로 구문 트리를 만든다. I/O 호출과 관련 코드를 표시한다. 표시된 줄만 모아 다시 소스로 만든다. 그리고 축약한다.

**전처리를 먼저 하는 이유가 있다.** 이상적으로는 문장 단위로 표시해야 한다. 그런데 Clang에서 문장 경계를 가려내기가 까다롭다고 논문이 밝힌다. 그래서 **줄 단위**로 표시한다.

줄 단위가 되게 하려면 한 줄에 한 문장이 오도록 만들어야 한다. 그래서 `clang-format`을 커스텀 설정으로 돌린다. 컬럼 제한을 200자로 두어 줄바꿈을 막는다. 중괄호를 별도 줄에 놓는다. 한 줄에 여러 문장이 있으면 나눈다.

**표시 규칙은 이렇다.** I/O 호출을 "남길 것"으로 표시하고 시작한다. 그다음 그 호출이 의존하는 것들을 따라 올라간다.

함수 호출은 인자 전부와 대입 좌변이 의존 대상이다. 조건문은 조건식이다. 루프는 초기화, 갱신, 조건이다. 대입은 좌변이다. 변수가 표시되면 **거꾸로 훑어서** 그 변수에 대입하는 모든 줄을 표시한다.

그다음 각 의존 대상의 **문맥상 부모**를 표시한다. 예를 들어 for 루프 본문에 있는 문장의 부모는 루프 헤더다. 그 부모도 자기 의존 대상과 부모를 가지므로 다시 표시한다. 최상위에 도달할 때까지 반복한다.

커널이 오류를 내면 전체 앱으로 되돌린다.

**우리 저장소에 딱 맞는 입력이 이미 있다.**

[`test/ph5example.c`](test/ph5example.c)가 1,333줄이고 [`examples/ph5example.c`](examples/ph5example.c)가 1,133줄이다. 실제 병렬 HDF5 코드다.

더 좋은 것이 있다. `test/`에 거의 동일한 파생본이 14개 있다. 파일 간 차이는 검증 블록 40줄 정도뿐이다. **그러면 생성된 I/O 커널들도 거의 동일해야 한다.** 자연스러운 회귀 테스트가 된다.

**반드시 테스트해야 하는 함정이 있다.**

슬라이싱이 `H5Pset_fapl_mpio()` 호출을 떨어뜨리면 어떻게 될까. shim은 드라이버가 MPI-IO가 아니라고 판단한다. 그러면 원래의 `H5Fcreate`를 호출하지 않고 `-1`을 반환한다([`src/autotuner_hdf5.c:215-224`](src/autotuner_hdf5.c#L215-L224)).

즉 커널이 조용히 파일을 만들지 못한다. 그런데 프로세스는 계속 돌 수 있다. 논문의 "오류가 나면 폴백" 규칙이 이걸 잡아줄지 불확실하다.

의존 규칙상으로는 잡아야 맞다. `fapl_id`가 `H5Fcreate`의 인자이고 `H5Pset_fapl_mpio(fapl_id, ...)`는 그 변수에 대한 호출이다. 그런데 이건 "대입"이 아니라 **부작용을 내는 호출**이다. 논문의 규칙 목록에 그 항목이 없다. **알고리즘의 실질적 공백으로 보인다.**

**루프 축약을 먼저 떼어내기를 권한다.**

논문 수치를 보면 효과의 대부분이 슬라이싱이 아니라 축약에서 나온다.

원본 앱의 최대 RoTI가 2.47이다. AST 슬라이싱으로 커널을 뽑으면 2.87이 된다. 16% 개선이다. 여기에 루프를 1%로 줄이면 **23.30**이 된다. 원본의 9배가 넘는다.

**그런데 루프 축약은 AST 슬라이싱을 필요로 하지 않는다.** 앱이 반복 횟수를 인자나 환경변수로 받으면, 튜너가 낮은 값을 주고 측정된 지표에 배수를 곱하면 된다. `ph5example.c`류에 반복 인자를 추가하는 것으로 시작할 수 있다.

정확도도 논문이 보고한다. 축약 커널의 대역폭은 97.10% 정확했다. 쓴 바이트 수 오차는 0.19%다. 쓰기 연산 횟수 오차는 4.87%다. 다만 반복이 1회 미만으로 줄어드는 경우에는 적용할 수 없다.

또 하나 더 싼 기법이 있다. **I/O 경로 바꾸기**다. 모든 파일 경로 앞에 `/dev/shm` 같은 메모리 경로를 붙인다. 실제 디스크를 건드리지 않으니 빠르다. 대신 목표 스토리지의 특성을 잃는다. 디버그나 스모크 테스트용으로만 쓰는 게 맞다.

**따라서 3단계를 둘로 쪼갠다.** 앞은 루프 축약과 경로 바꾸기다. 싸고 효과가 크다. 1단계와 병행할 수 있다. 뒤는 AST 슬라이서다. 비싸고 효과가 작다. 언제 해도 된다.

---

## 5. 파라미터를 얼마나 늘려도 되는가

### 5.1 결론

**"파라미터를 늘린다"에는 두 가지 뜻이 있다. 하나는 안전하고 하나는 강화학습을 망가뜨린다.**

값의 후보를 늘리는 것은 안전하다. 예를 들어 스트라이프 크기 후보를 8개에서 16개로 늘리는 것이다. 강화학습은 아무 영향을 받지 않는다.

파라미터 자체를 새로 추가하는 것은 위험하다. 예를 들어 ROMIO 힌트 6개를 추가하는 것이다. 강화학습의 선택지가 64배로 늘어난다.

### 5.2 왜 그런가 — 두 개의 서로 다른 공간

**핵심은 강화학습이 값을 고르는 게 아니라는 점이다.**

우리 시스템에는 탐색 공간이 두 개 있다. 헷갈리기 쉽다.

첫 번째는 **설정 공간**이다. 모든 파라미터 값 조합의 집합이다. 지금은 13,440가지다. 논문은 21.8억이라고 말한다. **이 공간을 탐색하는 것은 유전 알고리즘이다.**

두 번째는 **강화학습의 선택지**다. 파라미터의 부분집합들이다. **강화학습이 하는 일은 "이번 회차에 어느 파라미터를 튜닝할지" 고르는 것뿐이다.** 그 파라미터의 값을 무엇으로 할지는 유전 알고리즘이 정한다.

그래서 결과가 갈린다.

값 후보를 늘려도 강화학습의 선택지는 그대로다. 파라미터 개수가 안 변했으니까. 설정 공간만 커진다.

파라미터를 하나 추가하면 선택지가 두 배가 된다. 그 파라미터를 포함하는 경우와 포함하지 않는 경우로 나뉘기 때문이다. 파라미터가 n개면 부분집합은 2ⁿ개다.

숫자로 보면 이렇다.

| 파라미터 개수 | 부분집합 개수 |
|---|---|
| 12개 (논문) | 4,096 |
| 21개 (§3.2를 다 하면) | 약 210만 |
| 30개 (§8의 항목까지 넣으면) | 약 10억 |

§3.2에서 제안한 것들을 이 기준으로 나눠보자.

**강화학습에 영향이 없는 것.** `sieve_buf_size` 후보를 6개로 복구하는 것. `alignment` 값 범위를 넓히는 것. 숫자 오류를 고치는 것. 이건 값만 바뀌므로 안전하다.

**파라미터 개수를 늘리는 것.** `alignment`를 두 유전자로 쪼개면 하나 늘어난다. `cb_buffer_size`를 독립시키면 하나 늘어난다. `chunk`를 넣으면 하나 늘어난다. ROMIO 힌트 6개는 여섯 개 늘어난다. 합치면 12개에서 21개가 된다.

### 5.3 무엇이 구체적으로 깨지는가

**첫째, 신경망 출력이 너무 커진다.**

선택지마다 점수 하나를 내는 구조라면 출력 개수가 2ⁿ이다. 12개일 때 4,096개는 신경망 출력층으로 아슬아슬하게 가능하다. 21개일 때 210만 개는 불가능하다.

**둘째, 학습 기회가 너무 적다. 이게 가장 심각하다.**

강화학습은 회차마다 한 번 배운다. 그런데 논문에서 튜닝은 금방 끝난다. BD-CATS는 9회차에 멈췄다. HACC는 35회차에 멈췄다.

즉 **한 번의 튜닝 캠페인에서 얻는 학습 기회가 수십 번뿐이다.**

선택지가 4,096개인데 30번 배우면 0.7%만 본 것이다. 210만 개면 0.0014%다. 10억 개면 거의 0이다.

논문이 오프라인 사전학습을 두는 이유가 바로 이것이다. 미리 배워둬야 온라인에서 조금이라도 쓸 만하다. 그런데 사전학습 자체가 파라미터 스윕이므로 비용이 든다. 파라미터를 늘리면 사전학습 비용도 함께 커진다.

**여기에 근본적인 모순이 있다.** TunIO의 목적은 앱 실행 횟수를 줄이는 것이다. 73% 감소가 논문의 핵심 주장이다. 그런데 강화학습은 많이 실행해봐야 배운다. 파라미터를 늘리면 이 두 요구가 정면으로 부딪힌다.

덧붙이면, 이 관찰은 논문의 다른 주장에도 영향을 준다. 논문은 "온라인 학습이 계속 개선한다"고 말한다. 그런데 온라인 학습 기회가 수십 번뿐이면 그 개선폭은 작을 것이다. 논문에 전이 실험이 없다는 점(§8.6)과 같은 뿌리의 문제다.

**셋째, 보상을 누구에게 줄지 알 수 없다.**

보상에 5회차 지연이 있다. 5회차 동안 서로 다른 부분집합을 썼는데, 그중 어느 것이 이득을 냈는지 알기 어렵다. 선택지가 많아지면 이 문제가 심해진다.

**넷째, 보상 함수가 파라미터 개수에 따라 성질이 바뀐다.**

논문의 보상은 성능에서 페널티를 뺀 값이다. 페널티는 "고른 파라미터 개수 ÷ 전체 파라미터 개수"다.

전체가 12개일 때 하나를 더 고르면 페널티가 1/12 늘어난다. 전체가 30개면 1/30만 늘어난다. **즉 파라미터가 많아질수록 하나 더 고르는 부담이 줄어든다.**

그러면 에이전트는 점점 다 고르는 쪽으로 기울어진다. 부분집합을 작게 유지하려던 목적이 사라진다. 파라미터를 늘리려면 이 페널티를 다시 설계해야 한다.

### 5.4 어떻게 해결할까

다섯 가지 방법이 있다.

**첫째, 선택지를 파라미터별로 쪼갠다.** 2ⁿ개 중 하나를 고르는 대신, 파라미터마다 "넣을까 말까"를 독립적으로 결정한다. 그러면 출력이 2ⁿ개가 아니라 n개가 된다. 표준적인 기법이다.

잃는 것이 있다. 파라미터 사이의 상호작용을 배울 수 없다. 예를 들어 "청크를 넣을 때만 스트라이프 크기를 함께 넣는 게 좋다"는 관계를 학습할 수 없다.

**그런데 우리 경우에는 그 손실을 메울 방법이 있다.** §8.3에서 다루는 계층 간 제약을 명시적으로 넣으면 된다. 청크와 스트라이프 크기의 관계를 에이전트가 210만 개 선택지에서 배우기를 기대하는 대신, 도메인 지식으로 못박는 것이다. **즉 §8.3은 단순한 공간 축소 기법이 아니라 강화학습을 확장하기 위한 전제조건이 된다.**

**둘째, 한 번에 하나씩 결정한다.** "다음에 어느 파라미터를 추가할까" 또는 "어느 것을 뺄까"를 고른다. 선택지가 n개가 된다. 대신 한 회차에 한 걸음만 나아간다.

**셋째, 파라미터별로 따로 학습한다.** 부분집합 하나를 고르는 밴딧 대신, 파라미터마다 영향도를 추정하는 밴딧을 둔다. 그리고 상위 몇 개를 고른다. 논문의 PCA 사전학습이 사실 이것과 비슷한 일을 한다.

**넷째, 계층 단위로 먼저 고른다.** HDF5, MPI-IO, Lustre 중 어느 계층을 튜닝할지 먼저 정한다. 그 안에서 세부 파라미터를 정한다. 선택지가 훨씬 작아지고, 도메인 구조와도 맞는다. 설정 XML이 이미 계층으로 나뉘어 있다.

**다섯째, 파라미터를 늘리지 않고 값 후보만 늘린다.** §5.2에서 본 대로 이건 강화학습과 무관하다. 가장 안전하다.

### 5.5 유전 알고리즘 쪽에도 손볼 것이 있다

**파라미터를 늘리면 변이율을 다시 맞춰야 한다.**

지금 변이율은 유전자당 0.15다([`evolve.py:117`](evo/evolve.py#L117)). pyevolve 원본에서 온 값이다.

유전자가 5개면 자손 하나에 평균 0.75개가 바뀐다. 적절한 수준이다.

유전자가 21개면 평균 3.15개가 바뀐다. 한 번에 3개가 바뀌면 좋은 조합이 매번 깨진다.

관례는 유전자 개수의 역수 정도로 두는 것이다. 자손당 평균 1개가 바뀌게 하는 것이다. 21개면 0.05 근처가 맞다.

부분집합 선택과 함께 쓰면 더 신경 써야 한다. 활성 유전자가 k개면 변이는 그 k개에만 일어난다. 그래서 `1/k`로 동적으로 조정하는 편이 맞다.

### 5.6 그래서 실무적으로 어떻게 할까

**강화학습에 영향 없는 것을 먼저 한다.** `sieve_buf_size` 복구, `alignment` 범위 수정, 숫자 오류 정정이다. 설정 공간은 커지지만 파라미터 개수는 그대로다. 강화학습 설계와 무관하게 즉시 이득이고, 무엇보다 지금 있는 결함을 고치는 일이다.

**파라미터 개수는 논문의 12개에서 출발한다.** 늘리려면 선택지를 어떻게 표현할지 먼저 정해야 한다. 2ⁿ개를 그대로 쓰는 구조라면 12개가 사실상 상한이다.

**파라미터별로 쪼개는 방식과 §8.3의 제약을 함께 채택하면** 20개 중반까지 열 수 있다.

**변이율을 `1/k`로 동적화한다.** 파라미터 확장의 필수 동반 수정이다.

**파라미터를 크게 늘리고 싶으면 강화학습이 아닌 길을 봐야 한다.** §4.3의 첫 단계, 즉 PCA 기반 고정 순위는 이 문제가 없다. 파라미터 개수에 대해 선형이다. 계층 단위 선택도 마찬가지다.

여기서 앞의 평가를 조금 수정해야 한다. §4.3에서 나는 PCA 고정 순위를 "논문이 비판한 것을 재현하니 디딤돌로만 쓰라"고 했다. 확장성을 고려하면 더 균형 잡힌 그림이 나온다. **고정 순위는 애플리케이션과 스케일 간 일반화가 약하지만 파라미터 개수에 견고하다. 강화학습은 반대다.** 어느 쪽이 나은지는 파라미터를 얼마나 늘릴 계획인지에 달렸다.

---

## 6. 함께 고쳐야 하는 기존 버그

### 6.1 결론

**메모 §9에 적어둔 미해결 버그 중 네 개는 단독으로는 사소하다. 그런데 TunIO 설계에서는 측정 신뢰도 문제로 바뀐다.**

이유는 단순하다. 튜너가 `config.xml`을 수천 번 다시 쓰고 앱을 수천 번 재실행한다. 드물게 일어나던 문제가 매번 일어난다.

### 6.2 설정 파일을 읽을 때 확인을 안 한다

가장 급하다. 동적 shim은 `fopen` 결과를 확인하지 않고 그대로 XML 파서에 넘긴다([`:207-208`](src/autotuner_hdf5.c#L207-L208)).

튜너는 `config.xml`을 계속 덮어쓴다. 그러면 랭크가 **쓰기 도중인 파일**을 읽을 가능성이 실제로 생긴다.

고치기는 쉽다. 정적 버전에는 이미 null 확인이 있다([`autotuner_hdf5_static.c:48`](src/autotuner_hdf5_static.c#L48)). 그걸 옮기면 된다.

튜너 쪽에도 손볼 것이 있다. **원자적 쓰기**를 해야 한다. 임시 파일에 쓴 다음 이름을 바꾸는 방식이다.

### 6.3 MPI-IO 드라이버가 아니면 파일 생성이 실패한다

드라이버가 MPI-IO가 아니면 오류 메시지를 내고 `-1`을 반환한다. 그런데 **원래의 `H5Fcreate`를 호출하지 않는다**([`:215-224`](src/autotuner_hdf5.c#L215-L224)).

즉 파일이 안 만들어진다. §4.4의 함정이 이것이다. 소스 분석으로 만든 커널이 fapl 설정을 잃으면 조용히 실패한다.

### 6.4 설정 파일 경로가 하드코딩되어 있다

경로가 `"config.xml"`로 박혀 있다([`:200`](src/autotuner_hdf5.c#L200)). 환경변수로 지정하는 버전이 바로 아래에 주석 처리되어 있다([`:201-202`](src/autotuner_hdf5.c#L201-L202)).

**이게 후보를 병렬로 평가하는 것을 원천적으로 막는다.** 후보마다 다른 설정 파일이 필요한데 전부 같은 경로를 본다. §8.2의 연구 방향이 여기서 막혀 있다.

주석 두 줄을 살리고 null 확인을 넣으면 된다.

### 6.5 힌트가 실제로 적용됐는지 확인하지 않는다

테스트가 확인하는 것은 "H5Tuner가 힌트를 fapl에 써넣었다"까지다([`test_collective_write_04_striping_factor.c:550`](test/test_collective_write_04_striping_factor.c#L550) 부근). "파일시스템이 그 값을 적용했다"는 확인하지 않는다. 저장소 전체에 `lfs getstripe` 확인이 없다.

이게 왜 문제인지는 §8.4에서 자세히 다룬다. 요점은 이렇다. **요청한 값이 클램프되면 서로 다른 후보가 같은 설정이 된다.** 유전 알고리즘은 구별할 수 없는 점들을 각각 평가하며 예산을 낭비한다. 그리고 그 사이의 측정 편차를 신호로 오인한다.

### 6.6 GPFS 프리픽스가 작동하지 않는 것 같다

`set_gpfs_parameter`는 파일 이름에 `bglockless:`를 붙이는 함수다([`:48`](src/autotuner_hdf5.c#L48)). 그런데 지역 변수에만 쓰고 호출자에게 돌려주지 않는다. `H5Fcreate` 훅은 원래 파일 이름을 그대로 넘긴다.

`IBM_lockless_io`를 탐색 대상에 넣으면 아무 효과 없는 유전자가 된다.

---

## 7. 논문이 알려주지 않는 것

### 7.1 결론

**논문에 코드나 아티팩트 링크가 없다. 그래서 전부 서술로부터의 재구현이다.**

본문과 참고문헌 어디에도 저장소 주소가 없다. 열 가지 지점에서 막힐 것으로 보인다. 저자에게 문의할 가치가 있다.

### 7.2 막히는 지점들

**가장 중요한 것은 부분집합을 어떻게 표현하느냐다.** 파라미터 12개의 부분집합은 4,096가지다. Q-learning의 출력을 4,096개로 두는 것은 현실적이지 않다. 그러면 실제로는 크기를 고정했거나, 파라미터별로 독립 결정했거나, 하나씩 추가·제거하는 방식일 것이다. 논문은 "부분집합을 반환하고 영향도 순으로 정렬한다"까지만 말한다. **§5의 확장성이 여기서 결정된다.**

나머지 아홉 가지는 이렇다.

신경망 구조를 알 수 없다. 층 개수, 폭, 활성화 함수가 없다. 상태 벡터의 차원과 표현 방식도 없다.

~~`BW_single`의 정의가 없다.~~ **완화 (2026-08-12 재독).** 원문은 "The perf is normalized using the constant 1/(BW_single × num_nodes)"다. `BW_single × num_nodes`는 완전 선형 확장을 가정한 **이상적 총 대역폭**이므로, `norm_perf`는 *이상적 최대치의 몇 %를 달성했는가*이고 대략 [0,1]에 든다. **의미는 명확하고 없는 것은 값이다.** 단일 노드 측정 한 번으로 얻는다. 다만 측정 절차(어떤 워크로드로, 몇 프로세스로)는 여전히 미명시.

**조기 종료기의 보상 함수는 정말 없다.** §III-D가 주는 것은 "Smart Configuration Generation과 유사한 RL 기법", "5회차 지연", 그리고 절 제목 "Balancing Tuning Cost and Benefit"뿐이다. 본문의 "성능 이득과 튜닝에 쓴 시간의 균형"이 가장 구체적인 단서다. 즉 `이득 − 시간 비용` 형태라는 것까지는 읽을 수 있고, 함수는 우리가 정해야 한다. 구현 기록은 `06-진행-상황.md` §4.1.

보상의 두 항의 상대적 가중치가 없다. 성능 항과 파라미터 개수 페널티 항의 비중이다. 부분집합 크기 선호가 여기서 결정된다.

PCA 결과를 초기 정책으로 어떻게 옮기는지 없다. 오프라인에서 온라인으로 넘어가는 지점이다.

문맥적 밴딧의 구체적 알고리즘이 없다. 인용된 논문의 어느 변형인지 알 수 없다.

합성 로그 곡선의 파라미터 분포가 없다. 초기값, 성장률, 노이즈 크기의 범위다. 조기 종료 학습 데이터가 여기서 나온다.

사용한 HDF5 버전이 없다. §3.2에서 본 대로 1.10 이상이어야 하는 함수를 쓴다.

12번째 파라미터가 무엇인지 모른다. §III-C는 11개만 열거하는데 §IV는 12개라고 쓴다.

`chunk_cache`가 어느 함수인지 모른다. `H5Pset_cache`는 파일 전체에 적용되고 `H5Pset_chunk_cache`는 데이터셋마다 적용된다. shim이 후킹할 지점이 달라진다.

Darshan과 H5Tuner를 함께 `LD_PRELOAD`하는 방법도 없다. 둘 다 함수 가로채기를 쓰므로 순서에 따라 한쪽이 가려질 수 있다.

**연락처:** nrajesh@hawk.iit.edu, kbateman@hawk.iit.edu (Illinois Institute of Technology)

---

## 8. 후속 연구 가능성

### 8.1 이 절을 읽는 방법

**아래는 연구 방향의 후보 목록이다. 확정된 기여가 아니다.**

두 가지를 미리 밝힌다.

첫째, "열려 있다"는 판단은 **TunIO 논문 본문과 그 참고문헌 범위 안에서만** 확인한 것이다. 별도 문헌 조사를 하지 않았다. 각 항목에 확인할 문헌 후보를 적어두었다. **읽어보면 이미 해결된 것으로 밝혀질 항목이 있을 것이다.**

둘째, 기법 자체가 새로운 항목은 사실상 없다. 병렬 평가, 제약 처리, 전이 학습, 최적 정지는 모두 성숙한 분야다. **기여가 성립한다면 "I/O 튜닝에서의 구체적 형태"와 "측정 결과"에 있다.**

우선순위를 매기면 이렇다. 기준은 기여 가능성과 이 환경에서의 실행 가능성이다.

| 번호 | 방향 | 신규성 확신 | 필요 자원 | 판단 |
|---|---|---|---|---|
| §8.4 | 실효 설정 정규화 | 높음 | 작음 | **먼저 하라** |
| §8.2 | 병렬 후보 평가 | 중간 | 큼 | 가장 큰 주제 |
| §8.3 | 계층 간 제약 | 중간 | 중간 | 실험 설계가 깔끔하다 |
| §8.7 | 다목적화 | 중간 | 중간 | 논문이 기각한 기법이다 |
| §8.6 | 스케일 전이 | 중간 | 매우 큼 | 자원이 관건 |
| §8.5 | 실행 횟수 인지 정지 | 낮음 | 작음 | 단독으로는 약하다 |
| §8.2a | 간섭 하 측정 신뢰도 | 중간 | **작음** | 뉴론이 실험장을 준다 |
| §8.8 | 읽기 경로 튜닝 | 중간 | 중간 | 논문 목적함수가 요구한다 |
| §8.9 | 최신 스토리지 재평가 | 낮음 | 큼 | 다른 항목에 흡수하라 |

서로 묶으면 좋은 조합이 있다.

**§8.4와 §8.2를 묶으면** "튜닝 예산을 줄이는 두 가지 독립적인 방법"이라는 하나의 이야기가 된다. 앞은 낭비되는 평가를 없애고, 뒤는 남은 평가를 병렬로 돌린다.

**§8.3과 §8.6과 §8.9를 묶으면** "무엇이 시스템과 스케일을 넘어 이전되는가"가 공통 질문이 된다.

**§8.7과 §8.8을 묶으면** 읽기 대역폭이 다목적화의 재료가 된다.

**§8.5는 단독으로 약하므로** 어느 조합에든 구성요소로 붙이는 것이 좋다.

### 8.2 병렬 후보 평가와 측정 간섭

**질문은 이것이다. 여러 후보를 동시에 평가하면 서로의 측정을 오염시킨다. 신뢰도를 지키면서 병렬로 돌릴 방법이 있을까.**

지금 TunIO는 완전히 순차적이다. 세대마다 후보를 하나씩 실행하고 대역폭을 잰다. 그런데 개체 15개는 원리상 동시에 돌릴 수 있다. 노드가 있으면 세대 시간을 크게 줄일 수 있다.

**논문이 이 축을 전혀 언급하지 않는다.** 튜닝 예산이 논문의 핵심 지표인데도 그렇다. 순차 실행이 설계 선택인지 오염 우려 때문인지도 밝히지 않는다.

**여기서 흥미로운 점은 노이즈의 성질이다.**

후보를 여러 개 동시에 돌리면 같은 파일시스템을 두고 경쟁한다. 그러면 오차가 서로 독립적이지 않다. 함께 움직인다.

그런데 노이즈를 다루는 표준 기법들은 대체로 독립적인 노이즈를 가정한다. 여러 번 재서 평균이나 최소를 취하는 방식도 그렇다. 우리 코드도 `min(times)`를 쓴다. 알고리즘 설정 분야의 racing 기법도 그렇다.

**이 가정이 깨지는 지점이 기여가 성립할 수 있는 자리다.**

**우리 저장소에서는 진입 장벽이 거의 없다.** §6.4의 설정 파일 경로 문제만 풀면 된다. 주석 두 줄을 살리는 일이다.

**실험을 어떻게 설계할까.**

먼저 간섭을 특성화한다. 배치 크기를 1, 2, 4, 8, 16으로 바꾸면서 **같은 설정**을 동시에 평가한다. 그리고 두 가지를 본다. 평균이 얼마나 낮아지는가. 그리고 후보 사이의 순위가 얼마나 뒤바뀌는가.

**순위가 보존되는지가 핵심이다.** 유전 알고리즘은 절대값이 아니라 선택 순서를 쓴다. 순위만 유지되면 대역폭이 좀 낮게 나와도 문제가 없다. 이게 가장 중요한 초기 결과다.

그다음 완화 기법을 비교한다. OST를 후보별로 나누는 방법, 시간 슬롯을 나누는 방법, 노드를 격리하는 방법, 배치 구성을 변수로 넣어 보정하는 방법이 있다.

최종 지표는 RoTI로 봐야 한다. 시간은 줄었는데 잘못된 설정을 고르면 손해다.

**확인할 문헌.** HPC I/O 간섭 자체는 별도 문헌군이 있다. 여러 애플리케이션 사이의 간섭과 그 완화를 다룬다. 목적이 다르지만 간섭 모델은 재사용할 수 있다. 배치 Bayesian Optimization과 racing 기법도 확인 대상이다. 상관된 노이즈를 가정하는 변형이 이미 있는지가 관건이다. 논문이 인용한 SCTuner와 Bez 외의 연구도 봐야 한다.

**위험.** 다중 노드와 **전용 파일시스템 시간**이 필요하다. 다른 사용자의 부하가 섞이면 간섭 측정 자체가 오염된다. 이게 가장 큰 실무 위험이다.

### 8.2a 간섭 하에서 신뢰할 수 있는 측정 — 공유 노드 확장 (2026-09-02)

**§8.2가 "우리가 만든 간섭"을 다룬다면 이것은 "남이 만든 간섭"이다.** 뉴론이 공유 노드 정책이라 실험장이 저절로 주어졌다.

#### 문제를 정확히 나누면 둘이다

**귀속은 이미 해결돼 있다.** Darshan 은 프로세스 단위로 계측하므로 남의 I/O 가 내 로그에 들어오지 않는다. 공유 노드에서도 "내가 쓴 바이트"는 정확하다.

**문제는 간섭이다.** 이웃이 같은 OST 를 두드리면 내 I/O 가 느려진다. 바이트는 맞는데 시간이 늘어 대역폭이 떨어진다.

**그래서 풀 질문은 "누구 것인가"가 아니라 "이 느림이 내 설정 탓인가 이웃 탓인가"다.**

#### 새 프로파일러를 만드는 것은 권하지 않는다

I/O 간섭의 측정과 완화는 계보가 두꺼운 분야다. Yildiz 외의 cross-application interference 원인 분석(IPDPS'16), Gainaru 외의 혼잡 하 I/O 스케줄링(IPDPS'15) 등이 있다. 계측 계층에서 새로 만들 여지가 적고, 만들면 Darshan 과 Lustre jobstats 를 재발명하기 쉽다.

**기존 도구로 되는 것.** Darshan 이 내 프로세스의 바이트·시간·패턴을 준다. DXT 를 켜면 개별 연산의 타임스탬프까지 나온다. Lustre jobstats(`obdfilter.*.job_stats`)는 **서버 측에서 job 별 부하를 집계하므로 이웃까지 보인다** -- 다만 일반 사용자는 대개 읽을 권한이 없고 시간 해상도가 낮다. 클라이언트 측 `llite`/`osc` 통계로 내 RPC 의 대기 분포는 볼 수 있다.

#### 새로운 각도: 튜닝 루프 안의 측정 신뢰도

**기존 연구의 목적은 간섭을 줄이거나 예측하는 것이다. 우리 목적은 다르다.** 설정 A 와 B 중 어느 쪽이 나은지만 판정하면 되고 절대 대역폭은 중요하지 않다.

그래서 필요한 것은 프로파일러가 아니라 **"이 측정을 믿어도 되는가"를 판정하고 못 믿겠으면 버리거나 다시 재는 온라인 판정기**다. 튜너의 부품이다.

**§8.2 의 통찰이 여기서도 핵심이다. 유전 알고리즘은 절대값이 아니라 순위를 쓴다.** 대역폭이 전부 20% 낮게 나와도 순위가 보존되면 탐색은 멀쩡하다. **순위를 뒤집는 간섭만이 진짜 문제다.**

#### 신호 셋

**카나리아 설정이 가장 실용적이다.** 세대마다 한 번 고정된 기준 설정을 다시 측정한다. 그 값이 그 시점의 시스템 상태를 말해 주고, 다른 후보를 그것으로 정규화하면 시간에 따른 드리프트가 상쇄된다. 비용은 세대당 후보 하나 -- 개체 15면 7% 다. 오염된 세대를 통째로 버리는 것보다 싸다.

**랭크 간 편차가 둘째다.** 간섭이 있으면 일부 랭크만 느려져 collective write 의 대기가 벌어진다. Darshan DXT 나 h5bench 의 랭크별 시간에서 읽고, 편차가 임계를 넘으면 낮은 신뢰도를 매긴다.

**연속 측정의 자기상관이 셋째다.** 인접한 후보가 함께 나쁘면 설정이 아니라 시스템 요인이다. 설정은 무작위로 생성되므로 그렇게 뭉칠 이유가 없다.

**구현은 작다.** 지금 `Evaluator` 는 반복을 `mean`/`max` 로 접기만 한다. 여기에 신뢰도 축을 더하는 것이 전부다.

#### 기여가 성립한다면 어디인가

**"새 프로파일러"로는 약하다.** 계측은 기존 도구로 충분하다.

**"간섭 하에서 신뢰할 수 있는 오토튜닝"으로는 성립할 수 있다.** 기여 셋이다. 순위 보존을 지표로 삼는 프레이밍, 카나리아 정규화의 비용 대비 효과, 그리고 **RoTI 로 측정한 최종 손익** -- 재측정은 튜닝 시간을 늘리므로 신뢰도와 비용의 교환이 정량화된다.

**두 시스템을 대조군으로 쓸 수 있는 것이 이 프로젝트의 특이한 자산이다.** 누리온은 exclusive 라 간섭을 인위적으로 만들어야 하고, 뉴론은 공유라 그냥 돌리면 생긴다. 같은 코드로 같은 워크로드를 양쪽에서 돌린 비교는 흔치 않다.

#### 누리온 대조군을 쟀다 (2026-09-02): 3.4%

**설정을 완전히 고정하고 30회 반복한 결과다.** 2노드 128랭크, 최적 설정 그대로.

```
평균 552.4 MB/s · 표준편차 18.8 · 변동계수 3.4% · 최소~최대 511.5~571.8 (폭 10.9%)
lag-1 자기상관 +0.232 · 전반 15회 대비 후반 15회 -1.4%
```

**3.4% 는 위에 적은 "5% 미만이면 연구 주제가 아니다"에 해당한다.** 누리온에서는 `--reps` 로 덮인다.

반복을 늘렸을 때 관측이 안정되는 정도다(max 축약, 부트스트랩 4,000회).

| `--reps` | 1 | 2 | **3** | 5 |
|---|---:|---:|---:|---:|
| 변동계수 | 3.4% | 1.7% | **0.9%** | 0.5% |

**`--reps 3` 이면 0.9% 다.** TunIO 논문 설정이 마침 3회이고 그 값이 이 환경에 충분하다는 것이 실측으로 확인됐다.

**노이즈가 실제로 문제되는 지점.** 파라미터 효과를 노이즈 표준편차(18.8)와 견주면 이렇다.

| 대상 | 크기 (MB/s) | 노이즈 대비 |
|---|---:|---:|
| 캠페인 전체 향상 | 314.8 | 17배 |
| `striping_factor` (1위) | 88.7 | 4.7배 |
| `cb_nodes` (2위) | 52.2 | 2.8배 |
| `cb_buffer_size` (9위) | 13.5 | **0.7배** |
| `coll_metadata_write` (12위) | 2.2 | **0.12배** |

**상위 4개는 노이즈보다 충분히 크고 탐색이 안정적으로 구분한다. 하위권은 묻힌다.** `--reps 1` 로 뽑은 sweep 순위에서 10~12위의 상대 순서는 신뢰하지 않는 편이 안전하다.

**자기상관 +0.232 는 실재하지만 약하다.** 위에 적은 셋째 신호가 존재는 하되 카나리아 정규화를 정당화할 크기는 아니다.

#### 이제 필요한 것은 뉴론 값이다

**누리온 3.4% 가 대조군이다.** 노드는 전용이고 Lustre 만 공유하는 조건에서 나온 값이다.

**뉴론에서 같은 측정을 해서 15~30% 가 나오면 그 차이가 "노드 공유의 대가"이고 §8.2a 가 성립한다.** 3~5% 대로 같게 나오면 접는다.

뉴론 측정은 병렬 HDF5 빌드가 선행되어야 한다(`05-뉴론-이식.md` §9). 측정 자체는 2노드 25분, SRU 소모는 무시할 수준이다.

#### 확인할 문헌

**"노이즈 하 알고리즘 설정"이 성숙한 분야다.** racing(irace, Hoeffding races)과 배치 Bayesian Optimization 에 상관 노이즈를 다루는 변형이 이미 있을 수 있다. §8.1 의 경고가 그대로 적용된다 -- 기법 자체가 새로운 항목은 사실상 없고, 기여가 성립한다면 "I/O 튜닝에서의 구체적 형태와 측정 결과"에 있다.

### 8.3 계층 간 제약을 탐색 공간에 넣기

**질문은 이것이다. 계층 사이의 물리적 정합 조건을 제약으로 넣는 것이, 통계로 공간을 줄이는 것과 비교해 얼마나 효과적일까.**

논문 §II-A가 계층 간 협조의 필요성을 **명시적으로 말한다.** "이상적으로는 HDF5 청크가 Lustre 스트라이프에 대응해서 청크 하나가 OSD 하나에 쓰인다." "HDF5와 MPI-IO가 collective I/O를 함께 켜거나 끈다."

**그런데 TunIO는 이를 강제하지 않는다.** 부분집합 선택기가 스트라이프 크기만 고르고 청크는 빼는 조합이 가능하다. 애초에 청크는 스윕 목록에 없다.

즉 논문이 문제를 진단하고 해법은 통계적 축소로만 답한 구도다.

**흥미롭게도 우리 코드에 원시적인 구현이 이미 있다.** [`evolve.py:216`](evo/evolve.py#L216)의 `cb_buffer_size = striping_unit`이다. 손으로 넣은 계층 간 제약이고, 유전자 하나를 없애 공간을 8배 줄인다.

**§5.4에서 본 대로 이 방향에는 또 다른 의미가 있다.** 강화학습의 선택지를 파라미터별로 쪼개면 상호작용을 배울 수 없게 된다. 그 손실을 제약으로 메울 수 있다. 즉 이 항목은 확장성 문제의 해법 중 하나이기도 하다.

**제약 후보는 이렇다.** 전부 문헌과 매뉴얼로 근거를 확인해야 한다.

청크 크기와 스트라이프 크기를 맞춘다. `cb_buffer_size`와 스트라이프 크기를 맞춘다. 이건 이미 구현되어 있다. aggregator 개수와 OST 개수를 맞춘다. 정렬 단위를 스트라이프 경계에 맞춘다. 메타데이터 collective 설정과 전송 모드를 맞춘다.

**구현 방식은 세 가지다.**

종속 파라미터를 다른 것의 함수로 계산할 수 있다. 공간이 확실히 줄고 불법 조합이 아예 안 생긴다. 대신 제약이 틀리면 최적해를 잘라낼 수 있다.

자유롭게 탐색한 뒤 위반한 조합을 고칠 수도 있다. 유연하지만 연산자마다 수리 규칙이 필요하다.

위반 정도를 적합도에 반영할 수도 있다. 구현이 가장 쉽지만 위반 조합도 실행해야 하므로 평가를 낭비한다.

**첫 번째를 기본으로 하고 세 번째를 대조군으로 두기를 권한다.** 제약이 정말 최적해를 자르는지 확인해야 하므로 제약 없는 실행도 함께 돌린다.

**실험 설계.** 같은 예산에서 네 조건을 비교한다. 제약 없음, 제약만, 강화학습 부분집합만, 제약과 강화학습을 함께. 지표는 목표 대역폭 도달 회차와 최종 RoTI다. 넷째가 둘째와 셋째보다 좋으면 둘이 보완재라는 가설이 지지된다.

**확인할 문헌.** 진화 알고리즘의 제약 처리는 성숙한 분야다. 기법은 새롭지 않다. HDF5 청크와 Lustre 스트라이프 정렬은 이미 잘 알려진 상식이고 관련 논문들이 논의한다. **"제약으로 인코딩해서 통계적 축소와 정면 비교한 연구"가 있는지**가 관건이다.

**위험.** 제약이 시스템에 의존한다. OST 개수나 NVMe 여부에 따라 정합 조건이 달라지면 "이전 가능하다"는 주장이 약해진다. §8.9와 함께 여러 시스템에서 확인하는 편이 안전하다.

### 8.4 실효 설정 정규화 — 별칭 문제

**질문은 이것이다. 요청한 설정과 시스템이 실제로 적용한 설정이 다르면, 탐색에 얼마나 손해가 나고 고치면 얼마를 되찾을까.**

**이 항목을 가장 먼저 하기를 권한다.** 값싸고, 결과가 다른 실험들의 신뢰도 전제를 흔들 수 있다.

**문제는 이렇다.** OST가 24개인 시스템을 생각해보자. `striping_factor` 후보 중 32, 48, 64, 96, 128, -1은 전부 24로 클램프될 수 있다. **10개 후보 중 6개가 한 점이 된다.**

`striping_unit`도 64KB 배수로 반올림된다. 지원되지 않는 ROMIO 힌트는 조용히 무시된다. `cb_nodes=256`은 128프로세스에서 의미가 없다.

**그러면 유전 알고리즘은 구별할 수 없는 점들을 각각 평가한다.** 예산을 낭비하고, 그 사이의 측정 편차를 신호로 오인한다. 캐시도 맞지 않는다.

**강화학습에는 더 나쁘다.** 존재하지 않는 파라미터 영향도를 학습할 수 있다.

**두 논문 모두 이를 다루지 않는다.** TunIO는 대역폭만 재고 실효 설정을 되읽지 않는다.

**실험을 셋으로 나눈다.**

첫째가 측정 기여다. 가장 값싸고 가장 확실하다. 명목 공간의 각 점을 실제로 적용하고 실효 설정을 되읽는다. 그리고 **서로 다른 실효 설정이 몇 개인지 센다.** 논문들이 인용하는 "21.8억 조합"이 실효로 몇 개인지에 대한 최초 수치가 될 수 있다. 시스템마다 다르므로 두세 시스템에서 재면 더 좋다.

둘째가 탐색 손해 정량화다. 정규화 캐시를 켜고 끄면서 같은 예산의 유전 알고리즘을 비교한다. 별개 평가 횟수, 캐시 적중률, 도달 대역폭을 본다. §3.4의 재생 장치로 반복할 수 있다.

셋째가 학습에 미치는 영향이다. 파라미터 영향도 순위를 정규화 전후로 뽑아 비교한다. 순위가 바뀌면 "별칭이 학습을 왜곡한다"는 직접 증거가 된다.

**구현.** Lustre에서는 `lfs getstripe`로, 아니면 생성된 파일의 `MPI_Info`를 되읽어서 실효 설정을 얻는다. 그걸 캐시 키로 쓴다. §6.5의 버그 수정과 같은 작업이므로 준비 단계에서 한 번에 처리할 수 있다.

**확인할 문헌.** 진화 계산의 중립성 연구가 "여러 유전자형이 같은 표현형"을 다룬다. 이론적 배경으로 쓸 수 있다. 소프트웨어 설정 튜닝 분야의 무효·중복 설정 처리도 확인 대상이다. I/O 쪽에서 힌트 적용 여부를 확인한 연구도 있을 것이다. **다만 "탐색 공간의 실효 크기 측정"이라는 형태로 보고된 적이 있는지가 핵심이다.**

**위험.** 결과가 시스템마다 다를 수 있어 일반화 주장이 약해진다. 측정 기여로 포지셔닝하고 시스템 개수를 늘려 대응한다.

### 8.5 프로덕션 실행 횟수를 아는 정지 결정

**질문은 이것이다. 튜닝을 언제 멈춰야 할까. "성능이 정체했는가"가 아니라 "남은 튜닝 비용이 앞으로 회수되는가"로 물어야 한다.**

**이건 논문 자신의 future work다.** §VI에 "예상 프로덕션 실행 횟수를 조기 종료 모델의 입력으로 넣고 싶다"고 적혀 있다.

근거도 논문 안에 있다. Fig 12가 튜닝이 이득으로 전환되는 실행 횟수를 보여준다. TunIO는 1,394회, H5Tuner는 5,274회다. 그리고 TunIO가 유리한 구간이 399만 회까지다.

즉 논문은 생애 총시간 관점의 분석을 이미 제시하면서도, **정지 결정에는 그 관점을 쓰지 않는다.** 지금 입력은 회차 번호와 성능 둘뿐이다.

**형식화는 간단하다.** RoTI 대신 총 생애 시간을 목적으로 두면 된다.

```
총시간(t, N) = 튜닝시간(t) + N × 실행시간(달성 성능(t))
계속할 조건: 다음 회차의 기대 실행시간 감소 × N > 다음 회차의 튜닝 비용
```

N이 작으면, 예를 들어 100회라면 거의 즉시 멈추는 게 맞다. 튜닝이 회수되지 않는다.

N이 크면, 예를 들어 10만 회라면 정체 구간을 참고 계속하는 게 맞다. 작은 개선도 N배로 커진다.

N을 모른다면 분포로 주고 기대 총시간을 최소화하는 확장도 자연스럽다.

**실험 설계.** 재생 트레이스에서 N을 10, 100, 1000, … 10⁶으로 바꿔가며 세 방식의 총 생애 시간을 비교한다. TunIO 정지, 휴리스틱 정지, N을 아는 정지다. 각 N에서 사후적 최적 정지점과의 격차를 지표로 쓴다.

**N이 클 때 TunIO 정지가 너무 일찍 멈춰 손해를 보는 구간이 있는지**가 핵심이다. 있다면 논문이 스스로 인정한 한계의 정량적 증거가 된다.

**확인할 문헌.** **이 방향은 신규성 확신이 가장 낮다.** 최적 정지 이론, 비용을 고려하는 Bayesian Optimization, 정보 가치 기반 정지는 잘 확립된 분야다. 형식화 자체는 표준적일 것이다. 게다가 논문 자신의 future work이므로 저자 그룹이 이미 하고 있을 수 있다.

**위험.** 실행 가능성은 문제가 없다. 트레이스만 있으면 전부 오프라인이다. 위험은 신규성이다. 단독 논문으로는 약하다.

### 8.6 스케일 전이 — 논문의 미검증 주장

**질문은 이것이다. 작은 규모에서 배운 것이 큰 규모에서도 통할까. 통하지 않으면 무엇이 깨질까.**

**논문이 검증하지 않은 주장이 있다.** §V-C는 선행 연구를 "같은 애플리케이션의 다른 스케일에서 모델 적용성에 한계가 있다"고 비판한다. 그리고 TunIO는 온라인 학습으로 계속 배우니까 낫다고 주장한다.

**그런데 이를 분리해서 측정한 실험이 없다.**

논문의 실험 구성을 보자. 컴포넌트별 평가는 전부 4노드 128프로세스다. end-to-end만 500노드 1600프로세스다. 오프라인 사전학습의 스케일은 명시되지 않았다.

**"4노드에서 배운 것이 500노드에서 통했다"를 뒷받침하는 실험이 없다.** end-to-end 결과가 좋았다는 것이 곧 전이의 증거는 아니다. 온라인 학습이 500노드에서 처음부터 다시 배웠을 수도 있다.

**가설이 하나 있다.** 절대값 기반 영향도는 스케일을 넘으면 깨진다. 하지만 **비율로 표현하면 이전될 것이다.** 예를 들어 `cb_nodes`의 절대값보다 "`cb_nodes` ÷ 총 프로세스 수" 또는 "`cb_nodes` ÷ `striping_factor`"가 스케일에 덜 민감할 것이다. §8.3의 계층 간 제약이 비율 형태라는 점과 맞물린다.

**실험 설계.** 네 단계로 한다.

먼저 여러 스케일에서 같은 파라미터 스윕을 돌린다. 4, 16, 64, 256노드다. 스케일별로 영향도 순위를 뽑는다. **스케일 차이가 커질 때 순위가 얼마나 무너지는지**가 1차 결과다.

그다음 전이 실험을 한다. 한 스케일에서 배우고 다른 스케일에 **온라인 학습 없이** 배포한다. 처음부터 그 스케일에서 배운 경우와 비교한다. 격차가 전이 손실이다.

그다음 온라인 학습의 기여를 분리한다. 전이만, 온라인만, 전이와 온라인 둘 다를 비교한다. **논문 주장을 직접 검증하는 구성이다.**

마지막으로 가설을 검증한다. 절대값으로 표현한 모델과 비율로 표현한 모델의 전이 손실을 비교한다.

**확인할 문헌.** 설정 가능한 시스템의 성능 모델링에서 전이 학습은 활발한 주제다. 환경과 워크로드와 하드웨어 변화에 걸친 전이를 다룬다. 방법론은 그쪽에서 가져올 수 있고 신규성은 없다. HPC I/O 튜닝에서 스케일 전이를 분리 측정한 연구가 있는지가 관건이다. 논문이 비판한 선행 연구의 원문도 확인해야 한다. 비판이 정확한지도 검증 대상이다.

**위험. 자원이 가장 큰 장벽이다.** 네 스케일에 파라미터 스윕은 상당한 코어시간이고 256노드 할당이 필요하다. 스케일을 셋으로 줄이거나, §8.4의 정규화로 낭비를 없애거나, 분석을 오프라인으로 돌려 완화한다. 그래도 이 목록에서 가장 비싸다.

### 8.7 다목적화로 국소 최적을 벗어나기

**질문은 이것이다. 보조 목표를 추가해서 국소 최적을 벗어나는 기법이 I/O 튜닝에서 작동할까. 어떤 보조 목표가 맞을까.**

**논문이 인용하고도 기각한 기법이다.**

§III-A는 엘리티즘의 부작용을 스스로 인정한다. 집단이 과도하게 특화되고 탐색이 좁아져 국소 최적에 갇힌다는 것이다. 그리고 그 대응으로 토너먼트 선택을 택한다.

그런데 §V-A에서 Chen 외의 다목적화 기법을 인용한다. 보조 성능 목표를 추가해서 국소 최적 함정을 42% 줄였다는 결과다. 그러면서 **"이 개선은 도메인 비특이적이다"라는 이유로 기각한다.**

**도메인 비특이적이라는 게 기각 사유가 되는지 자체가 반박 가능하다.** 도메인 비특이적 기법이 도메인에 잘 들어맞으면 오히려 좋은 일이다.

게다가 논문의 실험이 문제의 실재를 보여준다. Fig 10(a)에 10회차부터 20회차까지 정체 구간이 있다. Fig 11(a)에서는 4회차와 5회차 사이에 개선이 없다. 그리고 논문 자신이 한계 절에서 국소 최적 고착을 인정한다.

**보조 목표 후보는 이렇다.**

메타데이터 연산 지연이 있다. 대역폭과 다른 파라미터군에 반응한다.

I/O 연산 횟수나 요청 크기 분포가 있다. 같은 대역폭을 더 적은 연산으로 달성하는 설정을 선호하게 된다.

읽기 대역폭이 있다. 쓰기 위주 튜닝이 읽기를 희생하는 것을 억제한다. §8.8과 연결된다.

저장 공간 사용량이 있다. 청킹이나 압축을 넣으면 실질적인 트레이드오프가 생긴다. 논문 §I도 목적이 다양하다며 저장 공간을 언급한다.

결과의 안정성도 있다. 평균 대역폭이 아니라 나쁜 경우를 피하는 설정을 선호하게 된다.

**실험 설계.** 같은 예산에서 네 조건을 비교한다. 단일 목표에 엘리티즘, 단일 목표에 토너먼트, 다목적화 각 보조 목표별, 다목적화와 토너먼트를 함께.

지표는 두 가지다. 정체 구간 탈출 성공률과 최종 RoTI다. 탈출 성공률은 논문 Fig 10(a)의 실패 상황을 재현한 트레이스에서 잰다.

보조 목표마다 주 목표와의 상관계수를 함께 보고하면 좋다. "어느 상관 수준에서 다목적화가 듣는지"라는 더 일반적인 결과로 확장할 수 있다.

**확인할 문헌.** 다목적화 자체는 인용된 연구가 확립했다. **기여는 I/O 도메인 적용과 보조 목표 설계에 있다.** 다목적 진화 알고리즘은 성숙한 분야다. I/O 튜닝에 다목적 접근을 쓴 연구가 이미 있는지 확인해야 한다. 논문 §I이 인용한 에너지 인지 오토튜닝 계보를 봐야 한다.

**위험.** 비용은 적다. §3.3에서 대역폭 측정을 만들 때 **보조 지표를 함께 모아두면 추가 실행 비용이 거의 없다.** Darshan을 쓰면 연산 횟수와 요청 크기 분포가 이미 나온다.

진짜 위험은 다른 데 있다. 보조 목표가 주 목표와 너무 강하게 상관되면 다목적화가 아무 효과가 없다. 다만 그 자체로 보고 가치가 있는 음성 결과다.

### 8.8 읽기 경로 튜닝

**질문은 이것이다. 읽기 위주 워크로드의 튜닝은 쓰기와 어떻게 다를까. 파라미터 영향도 순위가 달라질까.**

**논문의 목적함수가 읽기 대역폭을 명시적으로 포함하는데 구현이 없다.**

`perf = (1 − α)·BW_r + α·BW_w`에서 α가 1보다 작으면 읽기가 들어간다. 그런데 논문의 평가는 전부 쓰기 워크로드로 보인다. MACSio 쓰기, `h5_write`, HACC와 FLASH와 VPIC의 체크포인트 쓰기, BD-CATS다. **α가 1보다 작은 실험이 제시되지 않는다.**

우리 저장소도 마찬가지다. 동적 shim은 `H5Fcreate`만 가로챈다([`:185-188`](src/autotuner_hdf5.c#L185-L188)). 정적 버전에는 `H5Fopen` 훅이 있지만([`autotuner_hdf5_static.c:158`](src/autotuner_hdf5_static.c#L158)) 주 경로인 동적 버전에는 없다. **기존 파일을 여는 읽기 워크로드는 튜닝이 아예 걸리지 않는다.**

TunIO가 H5Tuner를 그대로 쓴다는 것은 **읽기 튜닝 능력도 그대로 없다는 뜻이다.**

**가설.** 읽기의 영향도 순위는 쓰기와 유의하게 다를 것이다.

Lustre 스트라이핑은 파일을 만들 때만 유효하다. 읽을 때는 이미 결정되어 있어서 튜닝 대상이 아니다. 대신 data sieving, 청크 캐시, collective read 힌트, 메타데이터 읽기가 지배적일 것이다.

**즉 쓰기에서 1순위인 파라미터군이 읽기에서는 통제 불가능하다.** 구조적인 차이다.

**구현.** 동적 shim에 `H5Fopen` 훅을 추가한다. 정적 버전에 이미 있으므로 패턴을 옮기는 데 가깝다.

한 가지 결정할 것이 있다. 읽기 경로에서 의미 없는 파라미터를, 예를 들어 스트라이핑을, 조용히 무시할지 경고할지다. §8.4의 정규화가 여기서 특히 유용하다. 읽을 때 스트라이핑 설정은 요청해도 적용되지 않으므로 정확히 별칭을 만든다.

**실험 설계.** 같은 파일에 대해 쓰기 튜닝과 읽기 튜닝을 각각 하고 영향도 순위를 비교한다. α를 0, 0.5, 1로 바꾸면서 최적 설정이 어떻게 움직이는지 본다.

**혼합 워크로드에서 쓰기 최적 설정이 읽기를 얼마나 희생하는지**가 실용적으로 가장 흥미롭다. §8.7의 보조 목표 설계로 바로 이어진다.

**확인할 문헌.** 읽기 성능 튜닝, prefetching, collective read 최적화는 별도 문헌군이 있다. HDF5 읽기 파라미터 특성화 연구도 확인 대상이다. **"읽기와 쓰기의 영향도 순위 비교"라는 형태의 결과가 보고되었는지가 관건이다.**

**위험.** shim 수정은 작다. 하지만 읽기 벤치마크를 새로 준비해야 한다. 현재 저장소 테스트는 전부 쓰기다.

측정이 까다롭다는 것이 더 큰 위험이다. 읽기 대역폭은 캐시 효과에 크게 좌우된다. 페이지 캐시와 클라이언트 캐시를 무효화하는 절차를 엄격히 세워야 한다. 이건 §8.2의 간섭 문제와도 겹친다.

### 8.9 최신 스토리지에서의 재평가

**질문은 이것이다. 논문의 결론이 지금 시스템에서도 유지될까.**

**논문의 모든 실측이 Cori에서 이루어졌고 Cori는 2023년에 운영을 종료했다.** Haswell 프로세서와 회전 디스크 기반 Lustre다.

지금 시스템은 NVMe 기반 scratch가 일반적이다. burst buffer, 노드 로컬 SSD, 객체 스토리지 계층이 추가된다. 논문이 인용한 Hermes와 UnifyFS 자체가 그런 계층화를 다루는 시스템이다.

**바뀔 가능성이 있는 것들이 있다.**

스트라이핑 파라미터의 상대적 중요도가 바뀔 수 있다. 회전 디스크에서 탐색 시간을 피하려고 중요했던 큰 스트라이프 크기가 NVMe에서는 덜 결정적일 수 있다. **그러면 영향도 순위가 바뀌고, "영향 큰 것 먼저"라는 전제 자체가 흔들린다.**

collective buffering의 이득이 줄 수 있다. 장치 지연이 짧아지면 모아서 쓰는 것의 상대적 이득이 준다.

탐색 지형이 평탄해질 수 있다. 파라미터 영향이 전반적으로 줄면 튜닝의 RoTI가 낮아진다. 그러면 조기 종료가 더 일찍 정당화된다. **논문의 결론을 오히려 강화하는 방향이다.**

계층이 하나 더 생긴다. 노드 로컬 NVMe에서 PFS로 옮기는 파라미터는 논문의 12개에 없다.

**실험 설계.** 논문의 핵심 실험 셋을 지금 시스템에서 반복한다. 파라미터 스윕으로 영향도 순위를 뽑는다. 튜닝 곡선이 정말 로그 형태인지 본다. 조기 종료의 RoTI를 비교한다. 그리고 Cori 결과와의 차이를 보고한다.

가능하면 회전 디스크 시스템과 NVMe 시스템을 나란히 두어 아키텍처 요인을 분리한다.

**확인할 문헌.** 스토리지 세대 교체에 따른 I/O 튜닝 재평가 연구가 이미 있을 가능성이 높다. NVMe와 burst buffer가 도래한 시점에 여러 편 나왔을 것이다. 논문이 인용한 프로덕션 HDF5 특성화 연구의 후속판도 확인 대상이다. **재현이 아니라 "결론이 어떤 조건에서 유효한가"를 밝히는 것으로 포지셔닝해야 기여가 선다.**

**위험.** 파라미터 스윕이 필요하므로 규모가 크다. 형식적 신규성은 낮다. 재현성 워크숍 트랙이 적합하다. **다만 §8.3과 §8.6의 "이전 가능성" 주장에는 이 결과가 필수 근거다.** 독립 논문보다 그 실험의 일부로 흡수하는 것이 효율적이다.

### 8.10 논문 자신의 future work 중 남은 것

§8.5의 실행 횟수 항목은 위에서 따로 다뤘다. 나머지 두 개는 §VI에 이렇게 적혀 있다.

**소스 변형 기법 확장이다.** 루프 시뮬레이션, blind write 제거, 필요한 연산 시뮬레이션 등이다. 논문은 "TunIO에서는 채택하지 않았지만 다른 데서는 쓸 수 있다"고 밝힌다. §4.4의 루프 축약을 만들면 자연스러운 확장 경로가 된다. **정확도와 속도의 트레이드오프 곡선을 체계적으로 그리는 것**이 기여가 될 수 있다. 논문은 루프 1% 축약에서 97.10% 정확도라는 점 하나만 제시한다.

**대화형 세션이다.** 여러 번의 실행에 걸쳐 설정을 조금씩 다듬는 방식이다. 실행마다 배우는 온라인 튜닝이므로 "언제 다시 튜닝할지" 문제와 직결된다. 워크로드나 시스템 변화를 감지하는 문제다. §8.6의 스케일 전이와 뿌리가 같다. 배운 것이 조건 변화에 얼마나 견디는가다.

**이 두 항목은 저자 그룹이 이미 하고 있을 가능성이 가장 높다. 후속 발표를 먼저 확인하는 것이 순서다.**

---

## 9. 어디서 검증할 수 있나

**이 맥에는 MPI와 HDF5와 autotools가 없다. 실기 검증은 CAOS 랩 서버나 HPC 클러스터가 필요하다.**

| 항목 | 이 맥 | 클러스터 |
|---|---|---|
| 유전 알고리즘 변경 단위 테스트 | 가능 | |
| 조기 종료 학습과 검증 | 가능 | |
| 재생 기반 알고리즘 비교 | 가능 (트레이스 확보 후) | |
| 소스 슬라이서 구현 | 가능 | |
| 생성된 커널의 컴파일 | | 필요 |
| C shim 신규 파라미터 검증 | | 필요 |
| 파라미터 스윕 학습 데이터 | | 필요 |
| Darshan 대역폭 측정 | | 필요 |
| 힌트가 Lustre에 적용됐는지 | | 필요 (Lustre 필수) |
| RoTI 실측과 end-to-end 비교 | | 필요 |

**트레이스 하나가 병목을 푼다.** 클러스터에서 스윕과 기록을 **한 번** 돌려 격자 트레이스를 확보하면, 1단계와 2단계의 알고리즘 작업 대부분이 이 맥으로 내려온다. 클러스터 접근이 열리는 즉시 최우선으로 할 일이다.

---

## 10. 실행 순서 요약

```
[준비 단계]  서로 병행 가능
  3.3 대역폭 목적함수 (파이썬)        ← 다른 모든 것의 전제
  3.4 기록·재생 장치 (파이썬)          ← 학습을 이 맥으로 내림
  3.2 파라미터 공간 정리 (C + 파이썬)  ← §5를 먼저 읽을 것. HDF5 버전 상향 선행
  §6  승격된 버그 4건 (C)             ← 3.2와 같은 파일

[1단계]  조기 종료          ← 이 맥에서 완결. 휴리스틱 먼저, 그다음 강화학습
[3단계 앞]  루프 축약        ← 1단계와 병행 가능. 효과 대비 비용이 가장 좋다
      ↓ 클러스터에서 스윕과 기록을 한 번
[2단계]  똑똑한 설정 생성    ← PCA 고정 순위 먼저, 그다음 강화학습
[3단계 뒤]  AST 슬라이서     ← 가장 크다. 독립적이므로 언제 해도 된다
```

**첫 스프린트로 이 네 가지를 권한다. 클러스터가 필요 없다.**

첫째, 대역폭 목적함수와 정규식 파싱, 기준 성능 측정, RoTI 출력을 만든다.

둘째, 기록과 재생과 스윕 모드를 만든다.

셋째, 휴리스틱 조기 종료기와 토너먼트 선택을 넣는다.

넷째, 가짜 앱으로 위 셋을 통합 검증한다.

이 네 개면 논문의 비교 기준선을 우리 저장소에서 재현할 수 있는 상태가 된다. 논문 표에서 RoTI 41.6에 해당하는 열이다. 강화학습 두 개는 그 위에 올린다.

---

## 11. 참조

**저장소 분석.** [`03-원본-코드-분석.md`](03-원본-코드-분석.md). §3이 지원 파라미터, §5가 사용 시 함정, §6이 오토튜너, §7이 테스트, §11이 TunIO 요약이다.

**논문.** Zotero 항목 키 `UCN6VL82`. DOI `10.1109/IPDPS57955.2024.00050`.

**계보.** Behzad 외의 H5Tuner 원조 논문에서 시작한다. 그다음 모델 기반으로 파라미터를 20개로 줄인 후속 연구가 있다. 그다음 TOPC 저널 논문이 있다. TunIO가 그 뒤를 잇는다.

**인접 연구.** Bez 외는 문맥적 밴딧으로 I/O forwarding 계층을 튜닝한다. 시스템 관리자 관점이다. Menon 외는 Bayesian Optimization으로 공간을 줄인다. Vidya는 소스 분석으로 I/O 영역을 격리하지만 커널로 재조립하지는 않는다. Golovin 외의 Vizier는 휴리스틱 조기 종료를 쓴다.
