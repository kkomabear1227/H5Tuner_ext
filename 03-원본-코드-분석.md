# H5Tuner 코드 분석 메모

> **이 문서는 무엇인가.** 우리가 포크한 **HDF Group 원본 H5Tuner** 저장소를 뜯어본 기록이다. 디렉터리 구조, 동작 원리, 발견한 버그와 고친 내역이 들어 있다.
>
> **§11 이 TunIO 논문 요약이다.** 논문(IPDPS'24)을 직접 읽지 않아도 이 절만으로 구조를 잡을 수 있다.
>
> **언제 읽나.** `src/` 의 C 코드(LD_PRELOAD shim)를 건드리기 전에 읽는다. 파이썬 쪽만 볼 사람은 건너뛰어도 된다.

**작성일:** 2026-08-11 (분석·수정·push) / **최종 갱신:** 2026-08-11 (TunIO 논문 반영)
**현재 커밋:** `f8ead15` (브랜치 `main`)
**목적:** 세션 간 인수인계. 저장소 구조·동작 원리·발견한 버그·수정 내역 정리.

> **추적 상태:** 2026-09-14 부터 공개 저장소(`h5tuner-ext`)에 함께 올린다. 그 전까지는 `.gitignore`에 있었다(§10.2).

---

## 현재 상태 (새 세션은 여기부터)

**완료:**

- 저장소 전체 분석 → §1~§8
- 버그 **7건** 발견, `evo/evolve.py` **전부 수정 + py3 재작성** (의존성 0) → §6.4
- 불필요한 중복 파일 **4개 삭제** (1,380줄) + `configure.ac` 정리 → §6.5
- 커밋 2개 → **public 저장소에 push 완료** → §10.1
- git 자격증명 노이즈(`-128`) 원인 규명 및 설정 개선 → §10.3
- `.gitignore` 추가 — 실행 산출물 4개 + 이 메모 (커밋 `f8ead15`) → §10.2
- **TunIO (IPDPS'24) 논문 정독 → §11.** 확장 계획은 별도 문서 [`07-후속-연구-계획.md`](07-후속-연구-계획.md)

**미완료 / 다음 세션 작업:** → §9 (H5Tuner 자체), §11.4 + 계획서 (TunIO 확장)

**한 줄 상태:** 코드 수정은 끝났고 검증도 됐지만, **실기(MPI + parallel HDF5 + Lustre) 환경 검증이 전무하다.** 이 맥에는 MPI/HDF5/autotools/python2가 전부 없다 (§10.4).

```
f8ead15  Updated gitignore                                <- HEAD (main) == h5tuner-ext/master
f7c704b  chore: drop redundant duplicate sources
bf6c1b9  fix(evo): rewrite evolve.py for Python 3, drop pyevolve
eeb75ab  Merge pull request #3 from HDFGroup/autotools     <- 분석 시작 지점 == origin/master
```

브랜치 이름 주의: 로컬은 **`main`**, 원격은 양쪽 다 `master`다. `main`의 upstream이 `origin/master`(= `eeb75ab`, upstream 원본)로 잡혀 있어 인수 없는 `git push`는 **upstream을 향한다** — push 시 반드시 `git push h5tuner-ext main:master`처럼 remote와 refspec을 명시할 것.

---

## 0. 한 줄 요약

H5Tuner는 **애플리케이션 재컴파일 없이 HDF5 함수 호출을 가로채 I/O 파라미터를 주입하는 shim 라이브러리**다. 저장소는 24.6K줄이지만 실제 로직은 **약 400줄**이고, 나머지 88%는 복제된 테스트/예제다. 오토튜닝(`evo/`)은 라이브러리와 완전히 분리된 외부 GA 드라이버이며, **현재 상태로는 실행되지 않는다** (§6.2).

---

## 1. LOC 현황

| 범위 | 분석 시점 (`eeb75ab`) | **현재 (`f7c704b`)** |
|---|---|---|
| 전체 추적 파일 | 24,632 | **23,393** |
| 소스 코드 (C / H / Python / Shell) | 21,611 | **20,727** |

정리로 1,380줄 삭제, `evolve.py` 재작성으로 +142줄 (354 → 496).

### 소스 분류별 (현재)

| 분류 | LOC | 비고 |
|---|---|---|
| 테스트 | 18,170 | `test/` — 14개 `test_*_write_*.c` (16,801) + `ph5example.c` (1,333) + 유틸 2개 (36) |
| 예제 | 1,133 | `examples/ph5example.c` |
| GA 튜너 (Python) | **496** | `evo/evolve.py` 1파일 — 재작성 + 중복 2파일 삭제 (1,062 → 496) |
| **핵심 라이브러리** | **563** | 공백·주석 제외 실질 SLOC **404** |
| 스크립트 | 365 | `autogen.sh` 228, `.misc/` 137 |
| ~~백업 dead code~~ | ~~318~~ | **삭제됨** (`src/.autotuner_hdf5_backup_Apr03.c`) |

여전히 **88%가 테스트/예제**다. 중복 제거 시 대략 3K줄 규모로 축소 가능 (§9.7).

---

## 2. 핵심 코드 위치

| 파일 | LOC | 인터포지션 방식 | 후킹 함수 |
|---|---|---|---|
| `src/autotuner_hdf5.c` | 392 | **동적** — `LD_PRELOAD` + `dlsym(RTLD_NEXT, …)` (`MAP_OR_FAIL`, L38-46) | `H5Fcreate`, `H5Dwrite`, `H5Dcreate1`, `H5Dcreate2` |
| `src/autotuner_hdf5_static.c` | 167 | **정적** — `ld --wrap` (`__wrap_` / `__real_`, L26-32) | `H5Fcreate`, `H5Fopen` |
| `src/autotuner.h` | 4 | — | include guard만, 내용 없음 |
| `src/.autotuner_hdf5_backup_Apr03.c` | 318 | — | 빌드에 포함되지 않는 백업 |

### 파라미터 주입기 3개 (`src/autotuner_hdf5.c`)

| 함수 | 위치 | 계층 | 주입 방법 |
|---|---|---|---|
| `set_hdf5_parameter` | L107 | HDF5 | `H5Pset_sieve_buf_size` / `H5Pset_alignment` / `H5Pset_chunk` — **확정적** |
| `set_mpi_parameter` | L78 | MPI-IO + 파일시스템 | `MPI_Info_set` → fapl에 되꽂음 — **권고(hint)** |
| `set_gpfs_parameter` | L48 | GPFS | 파일명에 `bglockless:` 프리픽스 (L64-68) — **현재 무효, §5.7** |

### 동작 흐름

```
앱이 H5Fcreate() 호출
  → shim이 먼저 잡음 (L190)
  → cwd의 config.xml 로드 (L200, 경로 하드코딩)
  → H5Pget_fapl_mpio로 기존 MPI_Info 획득 (L219)
  → 주입기 3개 호출 (L254-265)
  → H5Pset_fapl_mpio로 되꽂음 (L274)
  → __fake_H5Fcreate(...) 실제 호출 (L291)
```

`H5Dcreate1/2` 훅(L317, L355)은 청킹 전용. `H5Dwrite` 훅(L296)은 디버그 출력만 하고 통과.

---

## 3. 지원 파라미터 (하드코딩 화이트리스트)

`src/autotuner_hdf5.c` **L254-L265**에 함수 호출이 문자열로 박혀 있음. 아래 9개 + `chunk`가 전부:

| 파라미터 | 계층 | 주입 함수 |
|---|---|---|
| `sieve_buf_size` | HDF5 | `set_hdf5_parameter` |
| `alignment` (`"threshold,alignment"`) | HDF5 | `set_hdf5_parameter` |
| `chunk` | HDF5 | `set_hdf5_parameter` (H5Dcreate 경로) |
| `cb_buffer_size` | MPI-IO | `set_mpi_parameter` |
| `cb_nodes` | MPI-IO | `set_mpi_parameter` |
| `striping_factor` | Lustre | `set_mpi_parameter` |
| `striping_unit` | Lustre | `set_mpi_parameter` |
| `IBM_largeblock_io` | GPFS | `set_mpi_parameter` |
| `bgl_nodes_pset` | BlueGene | `set_mpi_parameter` |
| `IBM_lockless_io` | GPFS | `set_gpfs_parameter` |

**이 목록에 없는 이름은 XML에 써도 조용히 무시된다.** `romio_cb_write`, `romio_ds_write`, `ind_wr_buffer_size`, `num_target_nodes` 등 흔한 ROMIO 힌트는 미지원 — 추가하려면 C 코드 수정 + 재컴파일.

---

## 4. 계층 지원의 실체 (중요)

README는 "HDF5 / MPI-IO / 병렬 파일시스템 3계층"이라 주장하지만, 코드상 실제로는 **2계층**이다.

- **HDF5 계층만 직접 설정.** `H5Pset_*`로 property list를 확정적으로 변경.
- **MPI-IO와 파일시스템은 동일 경로.** `striping_factor` / `striping_unit`는 `cb_nodes`와 **완전히 같은 `set_mpi_parameter`**를 타고 MPI_Info 키로만 들어간다. Lustre 전용 코드(`llapi_file_create`, `lfs setstripe`)는 **없다**. ROMIO의 Lustre ADIO 드라이버가 힌트를 해석해주기를 기대하는 구조.

### 4.1 XML 섹션 계층은 파서가 무시한다

`xml/*.xml`의 `<High_Level_IO_Library>` / `<Middleware_Layer>` / `<Parallel_File_System>` 구조는 **사람이 읽기 위한 장식**이다. 세 주입 함수 모두

```c
mxmlFindElement(tree, tree, parameter_name, NULL, NULL, MXML_DESCEND)  // 예: L83
```

로 **문서 루트부터 태그명만으로 전역 검색**한다. `striping_factor`를 `<High_Level_IO_Library>` 안에 넣어도 동일하게 동작. 계층 라우팅은 XML이 아니라 L254-265의 하드코딩된 호출이 결정한다.

### 4.2 파일별 오버라이드

세 주입 함수 모두 노드의 `FileName` 속성을 본다. 속성 없으면 실행 전역, 있으면 `strstr(filename, node_file_name)` 매칭 시에만 적용.

```xml
<striping_factor>11</striping_factor>
<striping_factor FileName="ParaEg0.h5">7</striping_factor>
```

(코드에 `// TODO: Change this strstr() function call with a use of basename!` 주석이 세 군데 반복됨 — 부분 문자열 매칭이라 오탐 가능.)

---

## 5. 사용법과 함정

### 5.1 동적 방식 (주 경로)

```bash
# ① 빌드
export HDF5ROOT=/path/to/parallel-hdf5   # --enable-parallel --enable-shared 필수
export MXMLROOT=/path/to/mxml
export MPIROOT=/path/to/mpich
./autogen.sh && ./configure && make      # → lib/libautotuner.so

# ② config.xml을 앱 cwd에 배치 (xml/ 아래 템플릿 참고)

# ③ 실행 — 앱 바이너리는 그대로
export LD_PRELOAD=/path/to/H5Tuner/lib/libautotuner.so
mpiexec -n 32 ./my_app
```

의존 버전은 `configure.ac` L109-L116, `.misc/tuner.makefile`, `.misc/config.sh` 참고. `.misc/config.sh`가 mxml-2.9 / zlib-1.2.8 / hdf5-1.8.12를 `opt/` 아래에 내려받아 빌드해주는 부트스트랩.

### 5.2 앱이 MPI-IO 드라이버를 써야 한다 — 아니면 파일 생성이 깨진다

`src/autotuner_hdf5.c` **L215-L224**: 드라이버가 `H5FD_MPIO`가 아니면 에러 출력 후 `-1` 반환하는데 **원본 `H5Fcreate`를 호출하지 않는다.** serial HDF5 앱에 `LD_PRELOAD`를 걸면 튜닝이 안 되는 게 아니라 **파일 생성 자체가 실패**한다. 앱에 `H5Pset_fapl_mpio()`가 있는지 먼저 확인.

### 5.3 config.xml 경로가 하드코딩

**L200**: `strcpy(file_path, "config.xml")` — 프로세스 cwd에서만 찾는다.
**L201-L202**: `AT_CONFIG_FILE` 환경변수 버전이 **주석 처리된 채** 남아 있음 → 설정 경로를 외부에서 지정하는 기능은 죽어 있다. 랭크별 cwd가 다르면 각 디렉터리마다 파일 필요.

### 5.4 config.xml이 없으면 크래시 가능

**L207-L208**: 동적 버전은 `fopen` 결과를 검사하지 않고 `mxmlLoadFile(NULL, fp, …)`에 그대로 넘긴다.
대조: 정적 버전은 `src/autotuner_hdf5_static.c` **L48-L58**에 null 체크가 있다. 주 경로인 동적 버전에만 없음.

### 5.5 LD_PRELOAD 랭크 전파

런처가 환경을 자동 전달하지 않으면 후킹이 안 걸린다. OpenMPI `mpiexec -x LD_PRELOAD`, Slurm `srun --export=ALL` 등. 테스트가 `getenv("LD_PRELOAD")`를 찍어 확인하는 것도 이 문제 때문 — 같은 방식으로 검증 가능.

### 5.6 기타 빌드 제약

- **HDF5는 공유 라이브러리여야 함.** `dlsym(RTLD_NEXT, …)` 기반이라 정적 링크된 HDF5는 가로챌 수 없음. 실패 시 `H5Tuner failed to map symbol: …` 후 `exit(1)`.
- **MPICH 전제.** `configure.ac` L116 `AM_MPI_LIB="-L${MPIROOT}/lib -lmpich"` 하드코딩 → OpenMPI는 수정 필요.
- **`-DDEBUG` 항상 켜짐.** `configure.ac` L118-L119. 단 내부 `printf`가 대부분 주석 처리돼 출력은 조용함.
- 의존 버전이 오래됨: HDF5 1.8.12, mxml 2.9, zlib 1.2.8.

### 5.7 `bglockless:` 프리픽스는 효과가 없어 보인다

`set_gpfs_parameter`(L48)는 `filename`을 지역 버퍼에 `strcpy`할 뿐 호출자에게 돌려주지 않는다. `H5Fcreate` 훅은 `const char *filename`을 **그대로** L291의 실제 호출에 넘긴다 → 프리픽스가 반영되지 않음. (`IBM_lockless_io` 사용 시 확인 필요.)

### 5.8 정적 방식

`lib/libautotuner_static.a`. GNU ld `--wrap` 사용 → **앱 재링크 필요**:

```bash
mpicc my_app.c -o my_app \
  -Wl,--wrap=H5Fcreate,--wrap=H5Fopen \
  /path/to/lib/libautotuner_static.a -lhdf5 -lmxml
```

주의:
- **이 `--wrap` 플래그는 저장소 어디에도 없다** (Makefile / configure / README 전부 미제공). 직접 붙여야 함.
- `H5Fcreate` / `H5Fopen` 두 개만 후킹 → **청킹 미동작**.
- Lustre 스트라이핑은 파일 생성 시에만 유효하므로 `H5Fopen` 후킹에서는 의미 없음.

### 5.9 붙이기 전 체크리스트

```
□ 앱이 H5Pset_fapl_mpio()를 호출하는가        ← 아니면 파일 생성 실패 (§5.2)
□ HDF5가 --enable-parallel --enable-shared 인가
□ config.xml이 각 랭크의 cwd에 존재하는가      ← 없으면 크래시 (§5.4)
□ 튜닝할 파라미터가 지원 목록(§3) 안에 있는가   ← 밖이면 조용히 무시
□ LD_PRELOAD가 랭크까지 전파되는가            ← 앱에서 getenv로 확인
```

권장 순서: `examples/ph5example.c` 스모크 테스트 → `make check` (14개 테스트) → 실제 앱.

---

## 6. 오토튜닝 (`evo/`)

### 6.1 구조 — 앱 1회 실행 = XML 1개

`config.xml`은 `H5Fcreate` 시점에 **한 번** 읽힌다. 실행 중 XML을 바꿔도 이미 만들어진 파일에는 영향 없음.

세대를 넘기는 주체는 **앱 바깥의 드라이버** `evo/evolve.py`다. 매 후보마다 XML을 덮어쓰고 **앱을 처음부터 재실행**한다.

```
evolve.py (외부 드라이버)
 └─ 세대 1
     ├─ 개체 1: config.xml 새로 씀 → 앱 실행 → 종료 → 시간 측정
     ├─ 개체 2: …  (개체 15개)
     └─ 상위 3개 남기고 교배·변이
 └─ 세대 2 … 40
```

**총 앱 실행 횟수: 15(pop) × 40(gen) × 5(내부 반복) = 3,000회.** 앱 1회 1분이면 약 50시간. `evolve.py` L214-215에 59분 알람(초과 시 fitness 10000 페널티)이 있고 HPC 배치 잡을 전제로 함.

#### 탐색 공간

| 유전자 | 파라미터 | 후보 수 |
|---|---|---|
| `genome[0]` | `striping_factor` | 10 (`4…128`, `-1`=전체 OST) |
| `genome[1]` | `striping_unit` | 8 (1MB~128MB) |
| `genome[2]` | `cb_nodes` | 12 (1~256) |
| `genome[3]` | `alignment` | 14 (`"thresh, align"` 문자열) |
| `genome[4]` | `sieve_buf_size` | **1** (512MB 고정) |

유전자 5개지만 **실질 자유도 4**: `cb_buffer_size`는 독립 유전자가 아니라 L120에서 `genome[1]`(striping_unit)에 묶임(주석: "Ruth(David's) Suggestion"), `sieve_buf_size`는 후보 1개로 상수. 실제 공간 **10×8×12×14 = 13,440 조합**, 600회 평가로 약 4.5% 탐색.

#### GA 설정 (`run_main`, L312)

룰렛휠 선택 / 변이율 0.15 / 엘리트 3 / **최소화**. fitness는 `return float(elapsed)` (L302) — I/O 시간이 아니라 **앱 전체 벽시계 시간**. 순수 write 커널 벤치마크 전제. `ConvergenceCriteria`(L304, best ≤ 40초)는 정의돼 있으나 **등록하는 줄이 주석 처리**(L341)돼 항상 40세대 완주.

### 6.2 `evo/evolve.py` 버그 — **2026-08-11 수정 완료**

> **상태: 해결.** py3 포팅 + pyevolve 제거(stdlib GA로 대체)로 재작성했다. 아래는 원본에 있던 버그 기록이며, 수정 내역은 §6.4 참조.

원본의 문제, 심각한 순서:

1. **파싱 실패 (확인됨).** L106 들여쓰기가 3칸이어야 하는데 4칸.
   ```python
      Logdir = './Logs/'          # 3칸
       HOME = os.environ['PWD']   # 4칸 ← IndentationError
      # set SCRATCH variable...   # 3칸
   ```
   `ast.parse` 결과: `SyntaxError: line 106 -> unexpected indent`. Python 2/3 공통 토크나이저 오류. `HOME`은 이후 미사용이므로 그 줄 삭제 가능.

2. **config.xml이 엉뚱한 경로에 쓰인다 (확인됨).** L186-L187에 구분자 누락.
   ```python
   home_dir = os.environ['PWD']        # '/home/user'
   file_path = home_dir + 'config.xml' # → '/home/userconfig.xml'
   ```
   shim은 cwd의 `./config.xml`을 읽으므로 **GA 설정이 앱에 전달되지 않는다.** 앱은 낡은/없는 파일을 읽고, GA는 사실상 동일 설정의 노이즈를 재며 40세대를 돈다 — 조용히 무의미해지는 버그. `os.path.join()`으로 수정.

3. **5회 반복 중 4회가 버려진다.** L232-L251 루프가 `elapsed`를 덮어쓰기만 하고 평균/최소를 안 취함 → 마지막 값만 반환. 원래는 출력 파일 크기를 확인해 `break`하는 재시도 루프였는데 그 검사가 전부 주석 처리(L257-268)되며 변질. `valid`도 항상 0으로 남아 `running_time.txt`에 늘 `=0` 기록. `min()`을 쓰거나 1회로 줄이는 편이 낫다.

4. **실행 커맨드 하드코딩.** L246 `$SCRATCH/h5_write`. 위에 VPIC / VORPAL / GCRM 예시가 주석으로 남아 있음.

5. **`LD_PRELOAD`를 스크립트가 설정하지 않는다.** `Popen(shell=True)`이 부모 환경을 상속하므로 실행 전 직접 export 필요:
   ```bash
   export LD_PRELOAD=/path/to/H5Tuner/lib/libautotuner.so
   export SCRATCH=/path/to/scratch      # 미설정 시 PWD로 대체
   python2 evo/evolve.py
   ```

6. **Python 2 + pyevolve 전용.** `print` 문법이 py2, pyevolve는 2012년경 이후 유지보수 중단된 py2 전용 라이브러리. 현대 환경에서는 py3 포팅 + DEAP 등으로 교체가 현실적. 로직 자체는 단순(allele에서 값 뽑아 XML 쓰고 시간 재기)해서 재작성 부담은 작다.

7. **`shell=True` + 리스트 인자 (확인됨).** L246 `Popen(["$SCRATCH/h5_write", "rm SDS.h5"], shell=True)` — POSIX에서 `shell=True`에 리스트를 주면 **첫 원소만 커맨드**가 되고 나머지는 `$0`, `$1`로 들어간다. 즉 의도한 `rm SDS.h5` 정리가 **한 번도 실행되지 않았다.** 실측 확인:
   ```
   Popen(["echo CMD_RAN", "rm SDS.h5"], shell=True) -> "CMD_RAN"만 출력
   ```
   → 후보마다 이전 실행의 출력 파일이 남아 있었을 가능성. 측정값 신뢰도에 영향.

### 6.3 `evo/` 3파일 중복

| 파일 | 차이 |
|---|---|
| `evolve.py` | 기준 |
| `h5evolve_h5write.py` | **차이 0줄** (완전 동일) |
| `h5evolve_sample.py` | **1줄** — L246 실행 커맨드 (`$SCRATCH/h5_write` → `mpiexec -n 2 $SCRATCH/ph5example`) |

1,062줄 중 고유분 355줄. `h5evolve_sample.py.in`도 존재.

**→ 전부 삭제 완료 (§6.5).** 재작성된 `evolve.py`의 `--app-cmd` 플래그가 세 파일의 유일한 차이(실행 커맨드)를 흡수했다. 현재 `evo/`에는 `evolve.py` 하나만 있다.

### 6.4 재작성 내역 (2026-08-11)

`evo/evolve.py` 재작성: 474줄 추가 / 332줄 삭제. **의존성 0 (stdlib만), Python 3.**

**고친 것 (버그 7건 전부):**

| # | 원본 문제 | 조치 |
|---|---|---|
| 1 | L106 들여쓰기 → `IndentationError` | 미사용 `HOME` 줄 삭제 |
| 2 | L187 경로 결합 누락 | `os.path.join(os.getcwd(), 'config.xml')` |
| 3 | 5회 반복 중 4회 폐기 | `min(times)` — 사용자 선택 |
| 4 | 실행 커맨드 하드코딩 | `--app-cmd` / `H5TUNER_APP_CMD` |
| 5 | `LD_PRELOAD` 미검증 | 미설정 시 stderr 경고 |
| 6 | py2 + pyevolve | py3 + stdlib GA |
| 7 | `shell=True` + 리스트 → `rm` 미실행 | 문자열 커맨드 + `CLEANUP_FILES` 명시 정리 |

**GA 재현 (pyevolve 설정 그대로):** 룰렛휠, 변이율 0.15(유전자당), 엘리트 3, 최소화, **단일점 교차 0.9**.

> 교차는 원본이 명시하지 않았지만 pyevolve `G1DList`의 기본값(`G1DListCrossoverSinglePoint`)과 `GSimpleGA` 기본 교차율 0.9가 적용되고 있었다. 재작성 시 이걸 빠뜨리면 탐색 동작이 달라지므로 명시적으로 구현했다.

**의도적 차이 (원본과 다름):**

- **룰렛 스케일링.** pyevolve는 linear scaling(multiplier 1.2)을 거쳤다. 내부 구현을 복제하는 대신 선택 비율만 재현했다(최우수:최열등 = 1.2:1). → **점수가 원논문 실행과 숫자까지 같지는 않다.**
- **엘리트 비교 시점.** pyevolve는 신규 세대 평가 전에 비교하는 quirk가 있었다. 표준 방식(평가 후 최악과 교체)으로 구현.
- **캐시 추가.** 동일 genome 재평가를 생략(원본 L131-133의 주석 처리된 의도를 복원). 실측: 20세대 × 15개체에서 300 → 158회로 **47% 절감**. `--no-cache`로 해제.
- **`valid` 컬럼 의미.** 원본은 항상 `0`이었다(검사 코드가 주석 처리돼서). 이제 **앱 종료 코드**를 기록 — 전 회차 exit 0이면 1. 파일 형식은 동일.
- **`perf_feat_filenames.txt` 제거.** 열리기만 하고 아무것도 쓰지 않던 파일.
- **타임아웃 정리.** 원본은 `signal.alarm(0)` 취소가 없어 알람이 나중에 튈 수 있었고 자식 프로세스도 방치했다. 이제 `finally`에서 취소 + 자식 `kill()`.

**유지한 것:** `result_output.txt` / `config_feat.txt` / `running_time.txt` 형식, allele 리스트 값, `cb_buffer_size`를 `striping_unit`에 묶은 것(원본 L120), 59분 타임아웃, 페널티 10000, XML 구조.

**신규: `--dry-run`.** MPI/HDF5/앱 없이 GA와 XML 생성 경로만 실행. 합성 비용 함수(`synthetic_cost`)는 **성능 모델이 아니라** 수렴 확인용 인공 지형이다.

**검증 결과 (전부 실행 확인):**

| 항목 | 결과 |
|---|---|
| GA 수렴 | 12세대에서 avg 79.6 → 64.5, 합성 최적점 정확히 도달 |
| `config.xml` 생성 | `xml/config_lustre.xml`과 동일 구조 |
| 실측 경로 | 가짜 앱이 `config.xml`을 읽어 시간 반영 (경로 버그 해소 증거) |
| `min()` 동작 | `[0.457, 0.059, 0.127]` → `0.059` (콜드스타트 폐기) |
| 타임아웃 | 페널티 10000 적용, 자식 프로세스 누수 없음 |
| 비정상 종료 | 경고 출력 + `running_time.txt`에 `=0` |
| 재현성 | 동일 `--seed` → 동일 결과 |
| 컴파일 / `--help` | 정상 |

**미검증:** 실제 MPI + parallel HDF5 + Lustre 환경에서의 동작. 이 맥에는 MPI/HDF5가 없어 실앱 실행은 확인 못 했다.

**사용법:**

```bash
export LD_PRELOAD=/path/to/H5Tuner/lib/libautotuner.so
python3 evo/evolve.py --app-cmd 'mpiexec -n 32 ./my_app'

# 앱 없이 GA만 확인
python3 evo/evolve.py --dry-run --seed 1 --generations 12

# 실행 횟수 줄이기 (3,000회 -> 600회)
python3 evo/evolve.py --app-cmd '...' --reps 1
```

### 6.5 중복 파일 정리 (2026-08-11, 커밋 `f7c704b`)

**1,380줄 삭제.**

| 파일 | 줄 | 삭제 근거 |
|---|---|---|
| `evo/h5evolve_h5write.py` | 354 | 원본 `evolve.py`와 **바이트 단위 동일** (diff 0줄) |
| `evo/h5evolve_sample.py` | 354 | **configure 생성 파일**인데 추적되고 있었음 |
| `evo/h5evolve_sample.py.in` | 354 | 위의 템플릿. `@VAR@` 치환 변수 없음 → 단순 복사본 |
| `src/.autotuner_hdf5_backup_Apr03.c` | 318 | 날짜 붙은 백업, Makefile 참조 없음 |

**함께 수정: `configure.ac`.** `evo/h5evolve_sample.py`가 `AC_CONFIG_FILES`(L607)에 등록된 **생성 파일**이었다. 즉 템플릿과 생성 결과물이 나란히 커밋돼 있었고 둘은 완전히 동일했다(치환할 변수가 없으니까). 파일만 지우면 `configure`가 "cannot find input file"로 깨지므로 해당 항목도 제거했다.

**건드리지 않은 것:** `.gitkeep` 5개. `m4/`는 `.gitkeep`이 유일한 파일이고, `lib/`는 빌드 산출물 디렉터리(`src/Makefile.am`이 `../lib/libautotuner.so`를 만든다)라 디렉터리 존재 자체가 필요하다.

**검증:** 삭제 파일에 대한 잔여 참조 grep → 없음(남은 `h5evolve` 문자열은 `evolve.py` 내부 로그 접두어). `AC_CONFIG_FILES` 블록 괄호 균형·파일 목록 파싱 확인.

> **미검증:** `autoreconf`를 돌려보지 못했다 — 이 맥에 autoconf/automake가 없다. `configure.ac` 수정이 실제로 `configure`를 정상 생성하는지는 autotools 있는 환경에서 `./autogen.sh`로 확인 필요. **§9.1의 일부.**

---

## 7. 테스트/예제의 역할

### 7.1 복제 구조

HDF5 표준 병렬 예제 `ph5example.c`를 **14벌 복사**하고 각 사본에 검증 블록 ~40줄만 다르게 삽입. 파일 간 차이는 정말 그 40줄뿐 (`diff`로 확인). 구성은 **2(전송 모드) × 7(파라미터)**.

`test/Makefile.am`이 14개를 Automake `check_PROGRAMS`로 등록, `RUNPARALLEL`로 실행. 기본값은 `configure.ac` L568 `mpiexec -n ${NPROCS:=3}`.

**복제 이유:** 공용 테스트 하니스 없이 예제를 통째로 카피. Automake는 "프로그램 1개 = 테스트 1개"이고 파라미터별 독립 PASS/FAIL 신호가 필요했음. 16,801줄 중 논리적 고유분은 **약 600줄**.

### 7.2 각 테스트의 검증 대상

기대값 출처: `test/config.xml.in`

| 테스트 | 검증 대상 | 되읽는 API | 기대값 |
|---|---|---|---|
| `…_01_threshold` | HDF5 정렬 임계값 | `H5Pget_alignment()[0]` | 88 |
| `…_02_alignment` | HDF5 정렬 단위 | `H5Pget_alignment()[1]` | 44 |
| `…_03_sieve_buffer_size` | HDF5 sieve 버퍼 | `H5Pget_sieve_buf_size()` | 77 |
| `…_04_striping_factor` | Lustre 스트라이프 수 | `MPI_Info_get` | 11 / 7 / 1 |
| `…_05_striping_unit` | Lustre 스트라이프 크기 | `MPI_Info_get` | 6556 |
| `…_06_cb_buffer_size` | collective buffering 버퍼 | `MPI_Info_get` | 631136 |
| `…_07_cb_nodes` | aggregator 노드 수 | `MPI_Info_get` | 22 |

흐름: ① `LD_PRELOAD` 경로 출력 → ② 실제 병렬 HDF5 쓰기 → ③ 담당 파라미터 1개 되읽어 비교 → ④ 불일치 시 `nerrors++`.

유닛 테스트가 아니라 **통합 스모크 테스트**. "심볼 가로채기가 성립했고 값이 스택까지 갔는가"는 실제 `mpiexec` 없이 확인 불가하므로 매 테스트가 실 워크로드를 필요로 한다.

collective / independent 두 세트: 파라미터 주입은 `H5Fcreate` 시점이라 결과는 같아야 하지만, 전송 모드가 인터포지션을 방해하지 않는지 양쪽 확인. 검증 블록이 각 모드의 코드 경로 안에 위치.

### 7.3 테스트의 한계 (중요)

`test_collective_write_04_striping_factor.c` **L550**:

```c
ret = H5Pget_fapl_mpio(acc_tpl1, &comm_test, &info_test);
```

**fapl 자신의 MPI_Info를 되읽는다.** 즉 검증하는 것은 "H5Tuner가 힌트를 fapl에 써넣었다"이고, "Lustre가 실제로 11-way 스트라이핑을 걸었다"가 **아니다.** 저장소 전체에 `lfs getstripe` / `llapi` 확인은 없다.

→ ROMIO가 힌트를 무시하거나 파일시스템이 값을 클램프(예: OST 수보다 큰 `striping_factor`)해도 **테스트는 PASS**한다. 실제 반영 여부는 생성된 파일에 `lfs getstripe`를 직접 돌려 확인해야 함.

### 7.4 예제 2벌

| 파일 | LOC | 성격 |
|---|---|---|
| `examples/ph5example.c` | 1,133 | HDF5 원본 예제 + 파라미터 출력만. assert 없는 데모 |
| `test/ph5example.c` | 1,333 | 위 + H5Tuner 검증 블록. 14개 테스트의 공통 조상 |

`test/random_num.c` (21), `test/unrandom.c` (15)는 LD_PRELOAD 개념 데모용 소품.

---

## 8. 설정 파일 템플릿

`xml/` 아래:

| 파일 | 용도 |
|---|---|
| `config.xml` | 기본 |
| `config_lustre.xml` | Lustre |
| `config_gpfs.xml` | GPFS (`IBM_largeblock_io`, `IBM_lockless_io` 포함) |
| `config_chunking.xml` | 청킹 (`<chunk>32,32</chunk>`) |
| `config_chunking_IBM.xml` | 청킹 + IBM |

`test/config.xml.in`, `examples/config.xml.in`은 빌드 시 생성되는 테스트용.

---

## 9. 다음 세션에서 볼 것 (미해결 / 확인 필요)

우선순위 순:

1. ~~**`evo/evolve.py` L106, L187 수정**~~ — **완료 (2026-08-11), §6.4.** 남은 작업: **실제 MPI + Lustre 환경에서 실앱 대상 검증.** 이 맥에는 MPI/HDF5가 없어 `--dry-run`과 가짜 앱까지만 확인했다.
2. **§5.7 `bglockless:` 프리픽스 무효 여부 확정** — 코드 읽기로는 반영 안 되는 것으로 보이나 실행 확인 안 함.
3. **§7.3 갭 메우기** — 힌트가 실제 파일시스템에 반영되는지 검증하는 테스트가 없음. `lfs getstripe` 기반 확인 추가 여부 판단.
4. **§5.2 동작 판단** — 비-MPIO 드라이버에서 `-1` 반환하며 원본을 호출하지 않는 것이 의도인지, fallthrough가 맞는지.
5. **§5.3 `AT_CONFIG_FILE` 부활 여부** — 주석 해제 시 랭크별 cwd 문제 해결.
6. **파라미터 확장 필요 여부** — `romio_cb_write` 등 추가하려면 L254-265에 한 줄씩 + 재컴파일 (§3).
7. **테스트 중복 제거** — 공용 하니스로 뽑으면 16.8K → ~1K줄. 리팩터링 가치 판단.
8. ~~**`evo/` 중복 파일 삭제**~~ — **완료 (§6.5).**
9. **GitHub 토큰 위생** — 현재 토큰은 만료가 없고 `repo` + `workflow` 스코프다 (§10.3). `workflow` 제거 + 만료 있는 fine-grained PAT로 교체 권고. GitHub 웹에서만 가능.

**별 트랙: TunIO 확장.** 논문 요약은 §11에 있다. 구현 계획은 [`07-후속-연구-계획.md`](07-후속-연구-계획.md)에 있다. 계획서 §1~§7이 단계별 구현이고 §8이 후속 연구 가능성 9건이다.

위 목록 중 **1·3·4·5번은 TunIO 확장의 선행 조건이기도 하다.** 이유는 이렇다. 튜너가 `config.xml`을 수천 번 다시 쓰고 앱을 수천 번 재실행한다. 그러면 드물게 일어나던 문제가 매번 일어난다. §5.4(`fopen` null 미검사), §5.2(비-MPIO 시 파일 생성 실패), §7.3(힌트 반영 미검증)이 단순 버그가 아니라 측정 신뢰도 문제로 승격된다. 계획서 §6에 정리했다.

**특히 §7.3은 계획서 §8.4의 연구 방향으로 직결된다.** 요청한 설정과 시스템이 실제 적용한 설정이 다르면, 명목 탐색 공간의 여러 점이 같은 실효 설정으로 붕괴한다. 그러면 GA와 RL이 구별할 수 없는 점들을 각각 평가하고 측정 노이즈를 신호로 학습한다. 계획서에서 **가장 먼저 할 것**으로 꼽은 항목이다. 값싸고, 결과가 다른 실험들의 신뢰도 전제를 흔들 수 있기 때문이다.

**파라미터를 늘릴 계획이라면 계획서 §5를 먼저 읽을 것.** 값 후보를 늘리는 것은 RL과 무관하지만, 파라미터 개수를 늘리면 RL의 선택지가 2ⁿ으로 폭발한다.

---

## 10. 환경 / 인프라 상태

### 10.1 원격 저장소 — **public 주의**

| remote | URL | 성격 |
|---|---|---|
| `origin` | `https://github.com/hdffede/H5Tuner.git` | 원래 upstream. **push하지 않았다** |
| `h5tuner-ext` | `https://github.com/kkomabear1227/H5Tuner_ext.git` | 본인 fork. **여기에 push했다** |

**`H5Tuner_ext`는 public 저장소다** (`visibility: public`, `hdffede/H5Tuner`의 fork). API로 확정 확인. push 당시에는 `gh` CLI가 없어 공개 여부를 확인하지 못한 상태였고, 사후에 토큰으로 API 조회해 확정했다.

`h5tuner-ext/master` == 로컬 `main` == `f8ead15` (동기 상태). `origin/master`는 `eeb75ab`로 그대로 — upstream에는 아무것도 push하지 않았다.

**공개된 것:** `evo/evolve.py` 재작성, `configure.ac` 1줄, 삭제 4파일, 커밋 메시지 2개(버그 7건 서술 포함).

**유출 감사 결과 — 비밀값 없음:**
- push된 diff 전체에서 토큰/키 패턴(`gho_`, `ghp_`, `github_pat_`, `PRIVATE KEY`, `api_key`, `secret`, `password`, `token=`) **0건**
- 커밋 메시지 동일 패턴 **0건**
- `~/.git-credentials`, `~/.gitconfig`는 저장소 밖(홈 디렉터리)이라 커밋 대상이 아님
- 노출된 개인정보는 커밋 메타데이터의 이름·이메일뿐 — git 표준 동작이며 upstream 이력에도 이미 동일

되돌리려면: 저장소를 private으로 전환(**fork는 먼저 fork network를 떠나야 한다**), 또는 `git reset --hard eeb75ab && git push --force h5tuner-ext master`(이미 공개된 이력은 완전히 지워지지 않는다). **force-push는 하지 않았다.**

### 10.2 로컬 전용 문서의 추적 상태 — **해결됨 (`f8ead15`)**

`03-원본-코드-분석.md`는 **의도적으로 커밋하지 않았다** — 원격이 public일 가능성 때문에 로컬 유지를 선택했고, 사후 확인 결과 실제로 public이었으므로 그 판단이 맞았다. 원격에 없음을 API로 확인(`Not Found`).

이후 커밋 `f8ead15`에서 `.gitignore`를 추가해 실수 커밋 경로를 막았다. 현재 내용:

```
/config.xml  /config_feat.txt  /result_output.txt  /running_time.txt   # evolve.py 실행 산출물
/03-원본-코드-분석.md                                             # 이 메모
/07-후속-연구-계획.md                                               # TunIO 확장 계획서 (§11)
```

경로가 **`/`로 앵커**된 게 중요하다 — 추적 중인 `examples/config.xml`, `test/config.xml`, `xml/config.xml`은 영향받지 않는다.

### 10.3 git 자격증명 (`-128` 노이즈 해결)

**증상:** push 시 `fatal: failed to get: -128`. **push는 성공했다.**

**원인:** 헬퍼가 두 개 등록돼 있었다 — system(`/opt/homebrew/etc/gitconfig`)의 `osxkeychain`, global(`~/.gitconfig`)의 `store`. git은 등록 순서대로 호출하므로 `osxkeychain`이 먼저 실행되고, **비대화형 셸에서는 키체인 잠금 해제 프롬프트를 띄울 데가 없어** 즉시 `errSecUserCanceled = -128`을 반환한다(macOS SDK `SecBase.h:329`에서 확인). 그 뒤 `store`가 성공. 즉 `fatal:`은 헬퍼 프로세스의 메시지이고 git의 실패가 아니다.

**해결:** `~/.gitconfig`를 호스트별 설정으로 변경.

```ini
[credential]
	helper =                       # system의 osxkeychain 상속 리셋 (중복 호출 방지)
	helper = osxkeychain           # 기본: 모든 호스트는 암호화된 키체인
[credential "https://github.com"]
	helper =                       # github.com만 목록 리셋
	helper = store                 # 평문 우선 (비대화형 push용, -128 회피)
	helper = osxkeychain           # 폴백
```

빈 값이 헬퍼 목록을 리셋하는 것은 git-config 문서에 명시된 동작이다.

**효과:** github.com은 노이즈 없이 동작하고, 앞으로 추가하는 다른 호스트는 평문에 쌓이지 않고 키체인으로 간다. 즉 평문 노출이 기존 토큰 하나로 한정된다.

**검증 (`GIT_TRACE=1`로 실제 호출 추적):**

| 대상 | `store` | `osxkeychain` | `-128` |
|---|---|---|---|
| `github.com` | 1회 | **0회** | 없음 |
| 그 외 호스트 | **0회** | 1회 (중복 아님) | 없음 |

`git push --dry-run h5tuner-ext master` → `Everything up-to-date`, `-128` 0회.

**백업:** `~/.gitconfig.bak-20260811-170046`(1차 변경 전), `~/.gitconfig.bak-20260811-170622`(2차 변경 전).

**토큰 현황 (미해결 권고 — §9.9):**

| 항목 | 값 |
|---|---|
| 위치 | `~/.git-credentials` (평문, `0600`) **및** macOS 키체인 (중복 저장) |
| 종류 | `gho_` GitHub OAuth 토큰, 40자 |
| 소유자 | `kkomabear1227` (id 100303513) |
| 스코프 | `read:user, repo, user:email, workflow` |
| 만료 | **없음** |

위험 평가: FileVault **On**, Time Machine **미설정**, 홈이 클라우드 동기화 경로 **아님**, 권한 `0600` → 분실·백업·타 사용자 경로는 이미 막혀 있다. 남는 실질 위험은 **내 권한으로 실행되는 모든 프로세스가 프롬프트 없이 읽을 수 있다**는 점(키체인의 앱별 ACL이 없음) — `npm/pip` 설치 스크립트, VS Code 확장 등 서플라이 체인이 가장 현실적인 표면이다. 증폭 요인은 **만료 없음** + **`repo`(전 저장소 읽기·쓰기)** + **`workflow`(CI 코드 주입 → 저장소 secrets 유출 가능)**.

→ 보관 위치보다 **토큰 속성**이 핵심. `workflow` 제거 + 만료 있는 fine-grained PAT 교체가 우선.

### 10.4 이 맥에 없는 것 (검증 한계의 원인)

| 도구 | 상태 | 영향 |
|---|---|---|
| MPI (`mpiexec`, `mpicc`) | 없음 | H5Tuner 빌드·실행 검증 불가 |
| HDF5 (`h5cc`) | 없음 | 동일 |
| autoconf / automake | 없음 | `configure.ac` 수정 검증 불가 (§6.5) |
| python2 | 없음 | 원본 `evo/` 실행 불가 (재작성 이유 중 하나) |
| pyevolve / deap / numpy | 없음 | 재작성을 stdlib로 한 이유 |
| `gh` CLI | 없음 | 저장소 공개 여부를 즉시 확인 못 한 원인 (§10.1) |

Python은 3.12 / 3.13 / 3.14, conda 있음.

→ **실기 검증은 CAOS 랩 서버나 HPC 클러스터에서 해야 한다.**

---

## 11. TunIO (IPDPS'24) — H5Tuner를 확장한 후속 연구

> Rajesh, Bateman, Bez, Byna, Kougkas, Sun. *TunIO: An AI-powered Framework for Optimizing HPC I/O.* IPDPS 2024, pp.494–504. DOI `10.1109/IPDPS57955.2024.00050`. Zotero item key **`UCN6VL82`** (컬렉션 `8SUVTA8R`).
>
> 논문 전량(§I~§VI + 참고문헌) 정독 완료. 확장 계획서 → [`07-후속-연구-계획.md`](07-후속-연구-계획.md)

### 11.1 이 저장소와의 관계

**TunIO는 H5Tuner를 버리지 않는다. 감싼다.**

논문 §III-A에 이렇게 적혀 있다. "It builds off of the existing H5Tuner library, using its mechanisms to override the configuration parameters of HDF5 applications via an XML file."

즉 이 저장소의 C 코드가 TunIO의 파라미터 주입 계층 그대로다. 바뀐 것은 그 위의 탐색 드라이버다.

우리 코드에서 `src/`는 그대로 남는다. 앱의 `H5Fcreate`를 가로채고, `config.xml`을 읽고, 파라미터를 주입하는 부분이다.

교체되는 것은 `evo/evolve.py`다. `config.xml`을 새로 쓰고, 앱을 재실행하고, 성능을 재고, 세대를 넘기는 부분이다. TunIO는 이 자리에 DEAP 파이프라인과 세 개의 AI 컴포넌트를 넣었다.

**논문이 보고한 효과는 튜닝 시간 약 73% 감소다.**

BD-CATS 애플리케이션을 Cori에서 500노드 1600프로세스로 튜닝한 결과다(§IV-D).

| | 튜닝 예산 | 달성 대역폭 | RoTI |
|---|---|---|---|
| H5Tuner (정지 없음) | 1,750분 | 90.8 GB/s | — |
| H5Tuner + 휴리스틱 정지 | 538분 | 47.7 GB/s | 41.6 |
| **TunIO** | **468분** | 88 GB/s | **215** |
| TunIO + I/O 커널 | — | — | **250** |

최대 대역폭만 보면 H5Tuner가 3% 앞선다. 하지만 시간을 3.7배 쓴다.

### 11.2 설계 — 세 개의 컴포넌트 (§III)

설계 원칙이 세 가지이고 컴포넌트가 하나씩 대응한다. I/O에 집중해서 목적을 평가한다. 영향 큰 파라미터를 먼저 튜닝한다. 튜닝 비용과 이득의 균형을 사용자 제약에 맞춘다.

#### (1) 애플리케이션 I/O 추출 (§III-B)

**소스 코드를 분석해서 I/O에 필요한 부분만 남긴 작은 프로그램을 만든다. 튜닝 전에 한 번만 하면 된다.**

libclang의 `cindex`와 `enumerations` 모듈로 구문 트리를 만든다. 그리고 **줄 단위로** 남길 것을 표시한다.

문장 단위로 표시하는 것이 이상적이다. 그런데 Clang에서 문장 경계를 가려내기가 까다롭다고 논문이 밝힌다. 그래서 줄 단위로 대체했다. 대신 `clang-format`을 커스텀 설정으로 먼저 돌린다. 컬럼 제한을 200자로 두어 줄바꿈을 막고, 중괄호를 별도 줄에 놓고, 한 줄의 여러 문장을 나눈다. 이렇게 해서 한 줄에 한 문장이 오도록 만든다.

표시 규칙은 이렇다.

| 대상 | 함께 표시할 것 |
|---|---|
| 함수 호출 | 인자 전부와 대입 좌변 |
| 조건문 | 조건식 |
| 루프 | 초기화, 갱신, 조건 |
| 대입 | 좌변 |
| 변수가 표시되면 | 거꾸로 훑어서 그 변수에 대입하는 모든 줄 |

그다음 각 대상의 문맥상 부모를 표시한다. for 루프 본문에 있는 문장의 부모는 루프 헤더다. 그 부모도 자기 의존 대상과 부모를 가지므로 다시 표시한다. 최상위에 도달할 때까지 반복한다.

**재구성한 뒤 선택적으로 축약할 수 있다. 두 가지 방법이 있다.**

첫째는 **루프 축약**이다. I/O를 포함한 루프의 반복 횟수를 비율로 줄인다. 그리고 측정된 지표에 배수를 곱해서 원래 루프를 추정한다. 데이터 지역성 정보를 일부 잃는다. 1%로 줄인 실측에서 대역폭은 **97.10% 정확**했고 RoTI는 2.47에서 **23.30**으로 올랐다.

둘째는 **I/O 경로 바꾸기**다. 모든 경로 앞에 `/dev/shm`이나 tmpfs를 붙인다. 실제 디스크를 건드리지 않아서 빠르다. 대신 목표 스토리지의 특성을 잃는다.

**정확도는 이렇다** (§IV-A, MACSio 기준).

쓴 바이트 수는 정확하다. 커널이 0.0002%, 축약 커널이 0.19% 오차다.

쓰기 연산 **횟수**는 부정확하다. 커널이 19.05% 오차다. 로깅이나 print 같은 자잘한 쓰기가 제거되기 때문이다. 축약 커널은 4.87%로 오히려 낫다.

**축약 없이는 효과가 작다.** 커널만 뽑으면 튜닝 시간이 639분에서 549분으로 준다. 14% 감소다. RoTI는 2.47에서 2.87이다.

커널이 오류를 내면 전체 앱으로 되돌린다.

#### (2) 똑똑한 설정 생성 (§III-C)

**강화학습 에이전트가 파라미터 부분집합을 고른다. 그리고 영향도 순으로 정렬한다. 매 회차에 그 집합만 튜닝한다.**

목적함수는 `perf = (1 − α)·BW_r + α·BW_w`다. α는 전체 전송량 중 쓰기 비율이고, BW의 단위는 MB/s다. **우리 `evolve.py`가 재는 벽시계 시간과 근본적으로 다르다**(§6.1).

구조는 두 단계다. 먼저 **상태 관측기**가 애플리케이션과 튜닝 환경의 관계를 상태로 요약한다. 신경망 기반 문맥적 밴딧이다. 그다음 **부분집합 선택기**가 그 상태를 보고 다음 집합을 고른다. 신경망 기반 Q-learning이다. 구현은 Keras와 OpenAI Gym이다.

보상은 성능에서 페널티를 뺀 값이다. 성능은 `1/(BW_single × num_nodes)`로 정규화한다. 페널티는 고른 파라미터 개수를 전체 개수로 나눈 값이다. 여기에 **5회차 지연**을 준다. 단기 이득에 끌리는 것을 막으려는 것이다.

오프라인 사전학습은 이렇게 한다. VPIC, FLASH, HACC의 I/O 커널에 파라미터 스윕을 돌린다. 성능을 모은다. **PCA로 영향이 큰 파라미터를 골라낸다.** 그 결과로 기본 모델을 만든다. 이후 온라인에서 계속 학습한다.

스윕한 파라미터는 11개다. `sieve_buf_size`, `chunk_cache`, `alignment`, `meta_block_size`, `col_meta_ops`, `mdc_conf`, `coll_metadata_write`, `striping_factor`, `striping_unit`, `cb_nodes`, `cb_buffer_size`다. 평가에서는 HDF5와 MPI와 Lustre에 걸쳐 12개를 쓴다고 하고, 조합이 21.8억이라고 말한다.

효과는 이렇다(§IV-B, FLASH). 2.3 GB/s 도달이 43회차에서 **6회차**로 줄었다. 86.05% 감소다. 최종 설정은 12개 중 7개만 기본값에서 바뀌었다.

#### (3) 조기 종료 (§III-D)

**회차 번호와 그 회차의 성능을 받아서 멈출지 계속할지 답한다.**

같은 강화학습 기법을 쓴다. 상태 관측기가 상태를 요약하고 **행동 결정기**가 판단한다.

오프라인 학습이 흥미롭다. 튜닝 성능이 **로그 곡선**을 따른다는 관찰에서 출발한다(Fig 2). 그래서 로그 곡선을 여러 개 **생성해서** 튜닝을 흉내낸다. 곡선마다 초기값과 성장률을 다르게 준다. 그리고 곡선 아래로 랜덤하게 내려가는 노이즈를 섞는다. 잘못된 파라미터를 잠깐 골랐다가 회복하는 상황을 반영하는 것이다. 학습은 대부분의 가상 애플리케이션에서 평균 보상 증가가 5회차에 5% 이하로 정체할 때 끝낸다. 보상에도 5회차 지연을 준다.

효과는 이렇다(§IV-C, HACC). 50세대 중 **35세대에서 멈췄고** 2.2 GB/s를 얻었다. 튜닝하지 않은 0.55 GB/s의 약 4배다. 남은 15세대를 더 돌려도 0.08 GB/s만 더 오른다.

**휴리스틱 정지와 대조하면 차이가 크다.** 5회차 동안 5% 개선이 없으면 멈추는 규칙이다. 이 규칙은 10회차부터 20회차의 정체 구간에 걸려서 14회차에 멈춘다. 그러면 1.2 GB/s만 얻는다. 2배다. TunIO 대비 83% 손실이다.

RoTI로 보면 TunIO가 2.09다. 최적값 2.31의 90.5%다. 휴리스틱은 1.37이고 59.3%다.

#### 유전 알고리즘 설정 (§III-A)

DEAP를 쓴다. **엘리티즘과 토너먼트 선택을 함께 쓴다.** 토너먼트는 개체 3명을 무작위로 뽑아 상위 2명을 부모로 삼는 방식이다.

논문이 이 조합을 택한 이유를 밝힌다. 엘리티즘 때문에 집단이 과도하게 특화되어 국소 최적에 갇히는 문제가 있고, 토너먼트가 그걸 완화한다는 것이다.

**우리 `evolve.py`는 룰렛휠에 엘리트 3이다**(§6.4). 선택 연산자가 다르다.

#### 외부 인터페이스 (§III-E, Table I)

| 함수 | 입력 | 출력 |
|---|---|---|
| `stop` | 현재 회차, 최고 성능 | 멈춤 / 계속 |
| `discover_io` | 소스 코드, 옵션 | I/O 커널 |
| `subset_picker` | 성능, 현재 파라미터 집합 | 다음 파라미터 집합 |

`options`에는 **수동으로 지정한 남길 영역**과 소스 변형 플래그가 들어간다. 즉 수동 마킹이 공식으로 지원되는 진입점이다.

#### 측정 지표 (§IV)

**RoTI는 튜닝으로 올라간 대역폭을 튜닝에 쓴 시간으로 나눈 값이다.** 단위는 튜닝 1분당 MB/s다.

```
RoTI(t) = (달성 성능(t) − 초기 성능) / t
```

논문 Fig 12에는 다른 개념도 나온다. **viability point**다. 튜닝이 이득으로 전환되는 프로덕션 실행 횟수다. TunIO는 1,394회, H5Tuner는 5,274회다.

### 11.3 논문이 스스로 밝힌 한계와 future work

**한계는 세 가지다** (§III-F).

첫째, I/O 커널은 전체 앱의 I/O를 덜 정확히 대표한다. 그래서 성능을 남길 수 있다.

둘째, 부분집합만 튜닝하므로 **전역 최적에는 도달하지 못한다.** 논문은 이걸 의도된 트레이드오프라고 밝힌다. 자원 효율을 위해 성능 잠재력을 일부 포기하는 것이다.

셋째, 조기 종료가 이후에 나올 더 좋은 설정을 놓칠 수 있다.

**future work은 세 가지다** (§VI).

소스 변형 기법을 확장하는 것이다. 루프 시뮬레이션, blind write 제거, 필요한 연산 시뮬레이션 등이다.

여러 번의 실행에 걸쳐 설정을 조금씩 다듬는 대화형 세션이다.

예상 프로덕션 실행 횟수를 조기 종료기의 입력으로 넣는 것이다.

### 11.4 우리 저장소와의 간극

| TunIO | 우리 저장소 현재 | 무엇이 없는가 |
|---|---|---|
| 파라미터 12개 / 21.8억 조합 | shim에 이름 10개, `evolve.py` **실질 자유도 4 / 13,440 조합** | `chunk_cache`, `meta_block_size`, `coll_metadata_write`, `col_meta_ops`, `mdc_conf` (§3) |
| 목적이 대역폭 | **벽시계 시간** | Darshan 같은 측정 훅 자체가 없다 |
| 조기 종료 (강화학습) | 없다. 40세대 완주 | 휴리스틱조차 없다 |
| 부분집합 선택 (강화학습) | 전 유전자를 항상 탐색 | 유전자를 비활성화하는 기능이 없다 |
| I/O 추출 (libclang) | 없다 | 단 `test/ph5example.c`가 좋은 입력 후보다 |
| 토너먼트 선택 | 룰렛휠 | 연산자가 다르다 |
| RoTI 지표 | 없다 | `running_time.txt`에 시간만 남는다 |

**논문에 코드나 아티팩트 링크가 없다.** 본문과 참고문헌 어디에도 TunIO 저장소 주소가 나오지 않는다. 따라서 확장은 논문 서술로부터의 재구현이 된다.

미명시 항목이 최대 리스크다. 신경망 구조, 상태와 행동의 표현 방식, PCA 결과를 순위로 옮기는 방법, `BW_single` 상수의 정의, 부분집합 크기를 정하는 방식이 모두 나오지 않는다.

**그중 하나가 특히 중요하다. 부분집합을 어떻게 표현하는지다.** 이게 파라미터를 몇 개까지 늘릴 수 있는지를 결정한다. 파라미터가 n개면 부분집합은 2ⁿ개이므로, 12개일 때 4,096개이고 21개일 때 210만 개다. 자세한 분석은 계획서 §5에 있다.

전체 계획은 [`07-후속-연구-계획.md`](07-후속-연구-계획.md)를 볼 것.

---

## 부록: 검증 방법 구분

이 메모의 주장은 두 종류다.

**실행으로 확인:**
- LOC 수치 전부 (`git ls-files` + `wc -l`)
- 파일 간 diff (테스트 14개, `evo/` 3개, `ph5example` 2벌)
- `evo/evolve.py` L106 `IndentationError` (`ast.parse`)
- L187 경로 결합 결과 (`'/home/user' + 'config.xml'`)
- L246 `shell=True` + 리스트 인자로 `rm`이 실행되지 않음
- `--wrap` / `LD_PRELOAD` 저장소 전체 grep
- §6.4 재작성판의 8개 검증 항목 (GA 수렴, min(), 타임아웃, 재현성 등)
- §6.5 삭제 파일 잔여 참조 grep, `AC_CONFIG_FILES` 파싱
- §10.1 저장소 공개 여부 (GitHub API), push 내용 비밀값 감사, 메모 원격 부재
- §10.3 `-128` = `errSecUserCanceled` (macOS SDK `SecBase.h:329` 직접 확인), 헬퍼 호출 순서 (`GIT_TRACE=1`), FileVault/Time Machine 상태, 토큰 유효성·스코프 (GitHub API)

**코드 읽기로 판단 (실행 미확인):**
- §5.2 비-MPIO 시 파일 생성 실패
- §5.4 `fopen` null 미검사로 인한 크래시
- §5.7 `bglockless:` 무효
- §4 ROMIO의 힌트 처리 동작
- §7.3 테스트가 파일시스템 상태를 검증하지 않음

실기(HPC + Lustre) 환경에서 확인이 필요한 항목은 후자 쪽이다.

**논문 서술 (제3의 범주 — §11):**

§11의 모든 수치·설계 서술은 **TunIO 논문의 주장을 옮긴 것**이며 우리가 재현하지 않았다. Cori(2024년 은퇴)에서 측정된 값이고, 코드 아티팩트가 공개되지 않아 검증 경로도 없다. §11.4의 "이 저장소 현재" 열만 실제 코드 확인 결과다(`src/autotuner_hdf5.c` L254-265, `evo/evolve.py` L69-92·L302 상당부).

---

## 부록 2: 새 세션 시작 시 빠른 복구

```bash
cd ~/H5Tuner
git log --oneline -3                      # f8ead15가 HEAD여야 함 (브랜치 main)
git status --short                        # 비어 있어야 함 (.gitignore가 로컬 문서 2개를 가림)
git status --short --ignored | head       # NOTES-*.md, PLAN-*.md가 ignored로 보이면 정상
git remote -v                             # origin(upstream, eeb75ab) + h5tuner-ext(본인 fork, public, f8ead15)

# evolve.py가 도는지 30초 확인 (MPI/HDF5 불필요)
python3 evo/evolve.py --dry-run --seed 1 --generations 6 --population 10
```

읽는 순서 제안: **현재 상태** → §9(다음 작업) → 필요한 절만 발췌. §2~§5는 라이브러리 동작 원리, §6은 오토튜닝, §7은 테스트, §10은 인프라·보안, **§11은 TunIO 논문**.

TunIO 확장 작업을 이어받는다면: §11.1(관계) → §11.4(간극 표) → [`07-후속-연구-계획.md`](07-후속-연구-계획.md) 순서. Zotero item key `UCN6VL82`.
