# 구현 매뉴얼 — autotuner/

> **이 문서는 무엇인가.** 우리가 만든 오토튜너(`autotuner/`)를 **왜 그렇게 만들었는지** 적은 기록이다. 무엇을 만들었는지가 아니라 왜 그 선택을 했는지가 중심이다.
>
> **언제 읽나.** 코드를 고치기 전에 읽는다. 특히 §7 의 **논문과 다른 점 22건**은 "TunIO 를 재현했다"고 말할 때의 근거 목록이므로, 재현성을 따질 일이 있으면 거기부터 본다.
>
> **어디부터.** §0 → §1(목적) → §8(검증 상태) → §14(실행법). 전체는 길다.

**작성일:** 2026-08-13
**대상 커밋:** `f8ead15` + 미커밋 작업 트리
**목적:** 설계 결정과 그 이유를 한데 모은다. 세션이 바뀌어도 "왜 이렇게 만들었는지"가 남아야 한다.
**추적 상태:** 2026-09-14 부터 공개 저장소(`h5tuner-ext`)에 함께 올린다.

---

## 0. 새 세션이라면 여기서 시작

**읽는 순서.** §1(무엇을 만드는가) → §2(파일 지도) → §8(검증 상태) → §10(다음 할 일). 나머지는 필요할 때 찾아 읽으면 된다.

**누리온에서 실제로 돌리려면 §14로 간다.** 확인된 경로와 명령이 거기 있다. 환경 상세는 `04-누리온-환경.md`에 따로 있다.

§12는 PBS Pro 런처와 h5bench 어댑터, §13은 I/O Discovery와 Darshan이다.

**함께 있는 문서 세 개.**

| 문서 | 내용 |
|---|---|
| [`03-원본-코드-분석.md`](03-원본-코드-분석.md) | 저장소 구조·기존 버그 분석, TunIO 논문 요약(§11) |
| [`07-후속-연구-계획.md`](07-후속-연구-계획.md) | 확장 계획과 설계 근거, 후속 연구 가능성 9건(§8) |
| [`06-진행-상황.md`](06-진행-상황.md) | 진행 상황 추적, 남은 작업, 결정 기록 |
| **이 문서** | 설계 결정과 이유를 모은 것 |

**30초 확인.**

```bash
cd ~/H5Tuner
git status --short                    # autotuner/ 는 untracked, src/ 등은 modified
python3 -m autotuner info             # 프레임워크 3개, 공간 3개가 보이면 정상
python3 -m autotuner run --framework h5tuner --dry-run --seed 1 \
    --generations 4 --population 8    # 합성 모델로 탐색 확인, MPI/HDF5 불필요
```

**가장 중요한 미완 사항 두 개.** TunIO의 I/O Discovery가 미착수다(§10). 그리고 C 코드를 955줄 고쳤는데 **실기 컴파일을 못 했다**(§8).

---

## 1. 무엇을 만들고 있는가

**목표는 신규 다계층 병렬 I/O 오토튜닝 프레임워크의 베이스라인 확보다.**

신규 프레임워크를 만들려면 비교 대상이 필요하다. H5Tuner와 TunIO가 그 대상이다. 그런데 논문의 숫자를 그대로 인용해서는 비교가 성립하지 않는다. 서로 다른 머신, 다른 워크로드, 다른 파라미터 공간에서 나온 값이기 때문이다.

**그래서 두 프레임워크를 같은 기반 위에 다시 구현했다.** 같은 파라미터 공간, 같은 목적함수, 같은 측정 방식, 같은 세대 예산을 쓴다. 교체되는 것은 탐색 알고리즘과 부가 컴포넌트뿐이다.

이게 이 코드베이스의 설계를 지배하는 원칙이다. 무언가를 공유할지 분리할지 판단할 때 기준은 **"이게 다르면 비교 결과를 알고리즘 차이로 설명할 수 있는가"**다.

### 세 프레임워크

| 이름 | 무엇인가 | 논문 |
|---|---|---|
| `h5tuner` | 유전 알고리즘, 룰렛휠 선택, 조기 종료 없음 | Behzad et al. |
| `h5tuner-original` | `evo/evolve.py`가 실제로 출하된 모습. 벽시계 시간 최소화, 실질 4차원, 13,440 조합 | 참고용 |
| `tunio` | 유전 알고리즘 + 토너먼트 선택 + RL 부분집합 선택 + RL 조기 종료 | IPDPS'24 |

`h5tuner-original`은 **비교 실험용이 아니다.** 다른 공간을 탐색하고 다른 양을 최적화하므로 점수가 다른 것들과 비교되지 않는다. 있는 이유는 하나다 — 논문이 보고한 격차 중 얼마가 알고리즘에서 오고 얼마가 실험 설정 차이에서 오는지 보여준다.

---

## 2. 파일 지도

```
autotuner/                  파이썬 탐색 드라이버 (신규, 4,238줄)
├── space.py                파라미터 공간
├── config_writer.py        config.xml 생성
├── objective.py            목적함수 + 대역폭 스크레이퍼
├── evaluate.py             앱 실행·측정·캐시 + 합성 모델
├── trace.py                기록·재생 (JSONL)
├── metrics.py              RoTI, 캠페인 이력
├── ga.py                   유전 알고리즘
├── early_stopping.py       정지 정책 + 합성 곡선 생성기
├── subset.py               부분집합 선택 + 트레이스 기반 영향도 순위
├── cli.py                  run / sweep / rank / info
├── launchers.py            로컬 실행 / PBS Pro 잡 제출 (신규)
├── apps.py                 h5bench 어댑터 (신규)
├── darshan.py              Darshan 로그에서 대역폭 읽기 (신규)
├── discovery/              I/O Discovery — TunIO 세 번째 컴포넌트 (신규)
│   ├── marking.py          AST 순회와 라인 마킹
│   ├── kernel.py           포맷·파싱·재구성
│   └── reduce.py           루프 축약, I/O 경로 전환
├── rl/
│   ├── nets.py             PyTorch MLP / 의존성 없는 폴백
│   ├── features.py         상태 인코딩
│   ├── stopper.py          DQN 조기 종료기
│   └── picker.py           문맥적 밴딧 부분집합 선택기
└── frameworks/
    ├── base.py             세대 루프 (공유)
    ├── h5tuner.py          설정만
    ├── h5tuner_original.py 설정만
    └── tunio.py            설정만

src/autotuner_hdf5.c        C shim (수정됨). 파라미터 주입 계층
test/test_hdf5_metadata_params.c   신규 파라미터 되읽기 테스트 (신규)
test/stubs/hdf5.h           파싱 전용 HDF5 선언. I/O Discovery 개발용 (신규)
evo/evolve.py               원본 튜너. 손대지 않았다
```

**`frameworks/`가 설정만 담는 이유.** 세대 루프를 각자 구현하면 루프 차이가 결과 차이로 새어든다. 그래서 [`base.py`](autotuner/frameworks/base.py)에 루프가 하나 있고, 각 프레임워크는 어떤 선택 연산자·정지기·부분집합 정책을 쓸지만 정한다.

이건 프레임워크를 상호 교체 가능하게 만들려는 게 아니다. 알고리즘은 각 논문이 정한 대로 고정되어 있다. 보장하려는 것은 **평가·기록·정지 메커니즘이 동일하다**는 것이다.

---

## 3. 공유 기반

### 3.1 파라미터 공간 — [`space.py`](autotuner/space.py)

**한 점은 정수 튜플이다.** 파라미터마다 후보 값 목록이 있고, 점은 그 목록의 인덱스를 담는다.

이렇게 한 이유는 탐색 알고리즘이 값의 종류를 몰라도 되게 하기 위해서다. 유전 알고리즘은 인덱스를 바꾸면 되고, 나중에 Bayesian Optimization처럼 수치 경계가 필요한 방법을 붙이면 `[0, len-1]`을 그냥 얻는다. 숫자든 범주형이든 문자열 쌍이든 똑같이 다뤄진다.

**공간이 세 개다.**

`paper12`는 TunIO가 튜닝하는 12개다. **주 실험 공간이다.** 4억 4,790만 조합.

`minimal`은 7개짜리 부분집합이다. 전체 격자를 쓸 수 있을 만큼 작아서 빠른 실행과 재생용 트레이스 기록에 쓴다. 실험 공간이 아니다.

`original`은 `evo/evolve.py`가 실제로 탐색했던 것이다. 유전자 5개지만 실질 4차원, 13,440 조합.

#### 파라미터가 12개인 근거 — 해석이지 사실이 아니다

**논문 §III-C는 이름을 11개만 열거하는데 §IV는 12개를 튜닝한다고 쓴다.**

우리는 **HDF5 alignment의 threshold와 값을 따로 세어** 12개를 맞췄다. 이건 읽기이고, 논문이 확인해 준 것이 아니다.

부수 효과가 하나 있고 그게 이 읽기를 택한 실질적 이유이기도 하다. 두 개로 나누면 **독립적으로 변이할 수 있다.** 원본처럼 `"threshold,alignment"` 문자열 하나면 변이가 두 값을 함께 바꾼다. 원본 재현판(`original` 공간)은 원래대로 문자열 쌍 하나를 쓴다.

#### 후보 값 목록 전체가 우리 것이다

**논문은 파라미터 이름만 주고 값 목록을 주지 않는다.**

`evo/evolve.py`에 있던 것은 그대로 가져왔다. 그래서 일부가 깔끔한 2의 거듭제곱이 아니다. 바꾼 것은 셋이고, 셋 다 원본의 결함을 고친 것이다.

**`sieve_buf_size`를 512MB 단일값에서 64KB~2MB 6개로 되살렸다.** 원본은 유전자가 아니라 상수였다. 그리고 주석 처리된 목록은 `65535` 계열, 즉 2¹⁶−1의 배수였다 — 정렬용 버퍼로는 의미가 없다. 원본의 `NEW, NOT FOR IPDPS` 줄이 고친 2의 거듭제곱 값을 썼다.

512MB가 왜 들어갔는지에 대한 가설이 있다. HDF5의 data sieving은 MPI-IO 드라이버에서 ROMIO의 two-phase I/O가 대신하므로 **병렬 HDF5에서는 거의 작동하지 않을 수 있다.** 효과가 없어서 "최대한 크게 = 제약 무력화"로 512MB를 넣었다고 볼 여지가 있다. **확인되지 않은 추정이고, 확인 자체가 값싼 실험이다.**

**`alignment` 상한을 256KB에서 4MB로 올렸다.** `striping_unit` 하한이 1MB이므로 원래 범위로는 **스트라이프 경계 정렬이 구조적으로 불가능했다.** 정렬을 쓰는 주된 이유가 그것인데 말이다. 원본에도 `NEW, NOT FOR IPDPS`로 표시된 2MB까지의 미사용 목록이 있었다 — 저자들도 알고 있었다.

**`cb_buffer_size`를 독립 유전자로 되살렸다.** 원본은 `striping_unit`에 묶어 놓았다(주석: "Ruth(David's) Suggestion"). 값은 원본의 주석 처리된 목록을 썼다.

**결과 공간은 4.48억이다.** 논문의 21.8억보다 작다. 파라미터당 평균 후보가 5.26개 대 5.9개다. **억지로 맞추면 가짜 정밀도가 되므로 실제 값을 그대로 보고한다.**

#### 두 파라미터는 축소했다

`chunk_cache`는 `rdcc_nbytes` 한 차원만 튜닝한다. `H5Pset_cache`는 인자가 4개다.

`mdc_conf`는 `default`/`aggressive`/`conservative` 프리셋 3종이다. `H5AC_cache_config_t`는 필드가 20개 넘는다.

**논문은 이 둘이 어떤 형태로 튜닝됐는지 밝히지 않는다.**

#### `--levels`로 격자를 조밀도 낮춘다

재생이 성립하려면 탐색이 **기록된 격자 위에** 있어야 한다. 재생은 받지 않은 측정을 만들어낼 수 없다. 그런데 `paper12`의 전체 격자는 4.48억 점이다.

`--levels K`가 각 파라미터를 K개 값으로 줄인다. 첫 값, 마지막 값, 기본값은 항상 남긴다. `--levels 2`면 `paper12`가 4,096점이 되어 쓸 수 있다.

### 3.2 config.xml 생성 — [`config_writer.py`](autotuner/config_writer.py)

**원자적으로 쓴다.** 임시 파일에 쓰고 `rename`한다.

이유가 있다. 튜너는 이 파일을 후보마다 덮어쓴다 — 기본 설정으로 3,000번이다. 그리고 shim은 이 파일을 읽는다. **랭크가 쓰기 도중인 파일을 읽을 경쟁 조건이 실재한다.** 읽기 쪽도 함께 고쳤다(§6.3).

계층 섹션(`High_Level_IO_Library` 등)은 사람이 읽기 위한 것이다. shim의 주입 함수들은 문서 루트부터 태그명만으로 전역 검색하므로 중첩을 무시한다.

### 3.3 목적함수 — [`objective.py`](autotuner/objective.py)

**두 개가 있고 방향이 반대다.** 그래서 모든 소비자가 `Objective.sense`를 거쳐야 한다.

`walltime`은 앱 벽시계 시간을 **최소화**한다. `evo/evolve.py`가 쓴 것이다. 연산 시간이 섞여 있어서 I/O 측정이 아니다.

`bandwidth`는 TunIO의 `perf`를 **최대화**한다.

```
perf = (1 − α)·BW_read + α·BW_write
α = 쓴 바이트 / 전송한 총 바이트
```

**이걸 틀리면 논문 설계가 통째로 무너진다.** 조기 종료기는 **올라가는** 로그 곡선으로 학습한다. 보상은 대역폭을 특정 상수로 정규화한다. RoTI는 대역폭 증가분을 시간으로 나눈다. 시간을 넣으면 부호가 전부 뒤집힌다.

**대역폭을 어디서 얻는가.** 세 가지가 있고 하나만 구현되어 있다.

`darshan`이 논문이 쓰는 방법이다. **미구현.** 클러스터에 설치가 필요하고, `LD_PRELOAD`에 Darshan과 `libautotuner.so`를 함께 넣을 때 후킹 충돌을 확인해야 한다.

`regex`는 앱이 찍는 값을 정규식으로 긁는다. **구현됨.** `--perf-regex`. `measure()`가 이미 stdout을 캡처하고 있어서 새 훅이 필요 없었다.

`derived`는 파일 크기를 시간으로 나눈다. 의존성이 없지만 연산 시간이 섞이고 읽기는 못 잰다.

**α는 바이트 수를 모르면 1.0(순수 쓰기)으로 둔다.** 이 저장소의 모든 워크로드가 쓰기이므로 맞다. 읽기가 섞이면 Darshan이 필요하다.

**반복 측정의 축약은 기본 `best`다.** 최소화면 최소값, 최대화면 최대값. `evolve.py`가 `min(times)`를 쓴 이유와 같다 — 가장 빠른 실행이 콜드 캐시와 이웃 부하에 가장 덜 오염된다. **논문은 3회 평균을 쓴다.** `--reduction mean`으로 재현되고 TunIO의 기본값을 `mean`으로 잡아뒀다.

### 3.4 평가기 — [`evaluate.py`](autotuner/evaluate.py)

**네 모드가 있다.** `live`(실제 실행), `replay`(트레이스에서 조회), `dry-run`(합성 모델), `cache`(이번 세션에서 이미 평가).

**모든 프레임워크가 이 클래스를 통과한다.** H5Tuner와 TunIO가 후보를 다르게 측정하면 점수가 비교되지 않기 때문이다.

**합성 모델(`SyntheticApp`)은 성능 모델이 아니다.** MPI·HDF5·파일시스템 없이 탐색과 정지 로직을 돌려보기 위한 인공 지형이다. 최적점이 하나 있는 매끄러운 지형이라 작동하는 탐색이 눈에 보이게 수렴한다. **모델이 의견을 갖지 않는 파라미터는 무시되므로, dry-run으로는 그 파라미터의 튜닝 가치를 알 수 없다.**

### 3.5 기록과 재생 — [`trace.py`](autotuner/trace.py)

**이 하나가 병목을 푼다.**

```
sweep --record  →  실기에서 한 번, 모든 측정을 JSONL로 기록
run --replay    →  앱 실행 0회로 같은 측정 위에서 탐색
```

세 가지가 열린다.

**RL 에이전트를 노트북에서 학습할 수 있다.** TunIO의 두 에이전트는 모두 오프라인 사전학습을 요구한다. 실기에서 매 학습 에피소드를 돌리면 튜닝이 절약하려던 비용보다 더 든다.

**탐색 알고리즘을 같은 측정치 위에서 비교할 수 있다.** 실기 두 번은 알고리즘 차이만큼 파일시스템 노이즈로도 다르다.

**보고한 결과를 재현할 수 있다.** 코드만으로는 안 되고 측정치가 필요하다.

**항목은 점 인덱스가 아니라 디코딩된 파라미터 값으로 키를 만든다.** 그래서 `space.py`의 파라미터 순서를 바꿔도 트레이스가 살아남는다. 다만 파라미터 **집합**이 바뀌면 로딩을 거부한다 — 조용히 다른 실험과 비교하는 것을 막기 위해서다.

**트레이스에 없는 설정을 만나면 명확한 오류를 낸다.** 보간하지 않는다. 없는 측정을 만들어내는 것보다 멈추는 게 낫다.

### 3.6 지표 — [`metrics.py`](autotuner/metrics.py)

**RoTI가 논문의 중심 지표다.**

```
RoTI(t) = (달성 성능(t) − 초기 성능) / t
```

단위는 튜닝 1분당 MB/s다. 논문의 핵심 주장이 여기 걸려 있다 — TunIO 215 대 H5Tuner+휴리스틱 41.6.

**두 가지를 주의해야 한다.**

`초기 성능`은 **튜닝하지 않은** 설정의 성능이다. 그래서 캠페인이 시작할 때 기본 설정을 한 번 측정하고, **그 측정도 예산을 소비한다.**

RoTI는 최대화 목적을 가정한다. `--objective walltime`에서는 이득이 대역폭이 아니라 절약된 초이므로 **논문 숫자와 비교되지 않는다.** `unit`이 어느 쪽인지 알려준다.

**논문은 최종 RoTI가 아니라 최대 RoTI를 보고한다.** 튜닝이 이득을 못 내기 시작하면 분모만 계속 커지므로 RoTI가 떨어진다. 이 성질이 §5.2의 보상 설계에서 중요해진다.

---

## 4. 유전 알고리즘 — [`ga.py`](autotuner/ga.py)

**두 프레임워크 모두 유전 알고리즘이다.** 다른 것은 선택 연산자와 주변에 붙는 것들이다.

`h5tuner`는 룰렛휠 선택, 엘리트 3, 단일점 교차 0.9, 유전자당 변이 0.15다. 원본이 pyevolve로 돌았고, 교차 연산자와 비율은 `evo/evolve.py`가 명시하지 않은 채 `G1DList`의 기본값을 물려받은 것이다.

`tunio`는 **토너먼트 선택**이다 — 3명을 무작위로 뽑아 상위 2명을 부모로. 논문 §III-A가 이유를 명시한다. 엘리티즘이 집단을 과도하게 특화시켜 국소 최적에 갇히게 하고, 토너먼트가 그 완화책이다.

**일반화하지 않고 각각 재현했다.** 이건 베이스라인이고, 알고리즘 자체가 비교 대상이기 때문이다.

### 부분집합 마스킹

TunIO는 매 회차 파라미터 일부만 튜닝한다. 마스크는 연산자가 건드려도 되는 파라미터 이름의 집합이다.

**빠진 파라미터가 어떤 값을 유지하는가 — 논문에 없다.** 기본값일 수도, 현재값일 수도, 지금까지의 최적값일 수도 있다.

**우리는 개체가 지금 들고 있는 값을 유지한다.** 근거는 논문 자신의 서술이다. 엘리티즘이 이전 세대의 최적 설정을 그대로 넘긴다고 하는데, 빠진 파라미터를 기본값으로 되돌리면 **엘리티즘이 보존하려는 것을 바로 버리는 셈**이다.

**교차도 부분집합 안에서만 한다.** 활성 위치들만 모아 그 안에서 단일점 교차를 한다. 전체 유전자를 교차하면 비활성 파라미터도 섞이므로 "이 부분집합만 튜닝한다"는 말과 모순된다.

### 변이율 — 논문대로 고정, 그런데 문제가 있다

**유전자당 0.15 고정이다.** 논문대로다.

그런데 파라미터가 21개면 자손 하나에 평균 3.15개가 바뀐다. **좋은 조합이 매번 깨지는 수준이다.** 관례는 유전자 수의 역수, 즉 자손당 평균 1개다.

`--scale-mutation` 옵션을 뒀지만 **기본은 꺼져 있다.** 논문과 다른 동작을 기본값으로 두지 않기 위해서다. 파라미터를 크게 늘리면 켜야 한다.

---

## 5. TunIO의 컴포넌트

### 5.1 부분집합 선택기 — [`rl/picker.py`](autotuner/rl/picker.py)

#### 문제: 선택지 4,096개, 학습 기회 40번

**강화학습이 배워야 할 것이 4,096개인데 배울 기회는 40번이다.**

파라미터 12개의 부분집합은 2¹² = 4,096가지다. 강화학습에서 이것을 **행동**이라 부른다. Q-learning은 각 행동에 점수를 붙이고 높은 것을 고르는데, **점수를 알려면 그 행동을 해보고 결과를 봐야 한다.**

세대는 40번이다. 4,096개 중 최대 40개만 해볼 수 있다. 나머지의 점수는 알 방법이 없다.

**논문은 이걸 어떻게 피했는지 밝히지 않는다.** §III-C는 "부분집합을 반환하고 영향도 내림차순으로 정렬한다"까지만 말한다.

#### 우리 선택: 파라미터별 점수 + 상위 k

**신경망이 파라미터마다 "지금 얼마나 중요한가" 점수 하나를 내고, 상위 k개를 튜닝한다.** 배울 것이 4,096개에서 12개로 줄어든다.

이유가 셋이다.

**논문 문장과 가장 정합한다.** "영향도 내림차순으로 정렬"이 정확히 점수 매기고 정렬하는 구조다.

**논문의 오프라인 사전학습과 이어진다.** 사전학습은 스윕으로 파라미터별 영향도를 뽑는 것이고, 그게 곧 이 신경망이 출력하는 것이다. 다른 표현 방식이면 사전학습 결과를 초기 정책으로 옮기는 방법을 또 따로 정해야 한다.

**실험 설계가 깔끔해진다.** `--subset static`이 고정 순위로 상위 k개를 고르고, `--subset rl`은 **그 순위를 학습으로 만든다.** 차이가 "순위를 어디서 얻는가" 하나로 좁혀지므로 강화학습의 기여를 분리 측정할 수 있다.

**잃는 것:** 부분집합 크기 k를 학습하지 못한다. 논문도 크기를 밝히지 않으므로 설정값(`--subset-size`, 기본은 자유 파라미터의 1/3)으로 뒀다.

**확장성:** 파라미터를 30개로 늘려도 학습 대상이 30개다. 평면 2ⁿ 표현이면 10억이 된다.

#### 왜 밴딧인가

**한 세대가 끝나면 그 세대에 튜닝했던 파라미터들의 점수를 그 세대의 보상 쪽으로 당긴다.** 1스텝 값 갱신이므로 문맥적 밴딧이다.

논문은 Subset Picker를 Q-learning이라 하고 State Observer를 문맥적 밴딧이라 한다. **밴딧으로 구현하면 논문이 이름 붙인 두 조각이 하나의 일관된 알고리즘이 된다.** 다단계 Q-learning으로 만들면 Observer의 자리가 사라진다.

#### 보상 — 논문과 한 군데 다르다

논문 §III-C가 공식을 준다.

```
reward = norm_perf(perf) − norm_param(부분집합 크기)
norm_perf:  perf × 1/(BW_single × num_nodes)
norm_param: 크기 × 1/전체_파라미터_수
5회차 지연
```

`BW_single × num_nodes`는 **완전 선형 확장을 가정한 이상적 총 대역폭**이다. 그러니 `norm_perf`는 *이상적 최대치의 몇 %를 달성했는가*이고 대략 [0,1]에 든다. 의미는 명확하고 없는 것은 값이다 — 단일 노드 측정 한 번으로 얻는다.

**우리는 `perf`의 수준 대신 baseline 대비 상대 개선을 쓴다. 논문과 다르다.**

의도적이다. 수준을 쓰면 **나중 회차가 그냥 나중이라는 이유로 높은 보상을 받는다.** best-so-far가 단조 증가하므로 후반 회차는 어떤 부분집합을 골랐든 높은 perf를 본다. 부분집합의 좋고 나쁨을 배우려면 그 교란이 없어야 한다.

**논문에 충실한 arm이 필요하면 플래그 하나로 추가할 수 있다.** 필요한 것은 `BW_single` 값 하나다.

두 항의 상대 가중치는 논문에 없어서 `--size-penalty` 기본값 1.0(논문의 단순 차이)으로 뒀다.

#### 오프라인 사전학습이 정책에 도달하는 경로

**스윕에서 뽑은 영향도 순위를 두 가지로 쓴다.**

하나는 **신경망 입력 특징**이다. 다른 하나는 **warm-start 회귀 목표**다 — 캠페인 시작 시 신경망의 순위가 스윕의 순위와 같아지도록 미리 맞춘다.

논문은 PCA로 영향도를 뽑는다고 하지만 **그 결과를 초기 정책으로 옮기는 방법을 밝히지 않는다.** 회귀로 맞추는 것이 가장 직접적이다. prior를 입력 특징으로도 넣었으므로, 캠페인의 실제 관측이 prior와 어긋나면 신경망이 prior를 깎아낼 수 있다.

#### 영향도 순위는 PCA가 아니다

[`subset.py`](autotuner/subset.py)의 `rank_from_trace()`는 **일원 효과 크기**를 쓴다. 파라미터 값별로 평균 성능을 내고 그 평균들의 표준편차를 효과로 본다.

**논문의 PCA와 다르다.** §III-C는 파라미터에 대해 perf 기준 PCA를 한다고 하지만 성분 로딩을 파라미터별 순위로 바꾸는 방법을 밝히지 않고, 입력만의 PCA는 목표에 대한 정보를 담지 않는다. 일원 효과는 "어느 knob이 목적함수를 움직이는가"라는 질문에 직접 답하고 의존성이 없다.

**한계:** 상호작용을 못 본다. 둘이 함께일 때만 의미 있는 파라미터 쌍은 둘 다 무력해 보인다.

**온전성 검사는 통과한다.** 합성 모델이 쓰지 않는 `alignment_threshold`가 효과 0.000으로 마지막에 온다.

### 5.2 조기 종료기 — [`rl/stopper.py`](autotuner/rl/stopper.py)

#### 구조: 신경망 하나

**논문의 State Observer와 Action Decider를 한 신경망으로 합쳐 end-to-end 학습한다.** 은닉층이 상태 표현이고 출력층이 결정이다. 표준 DQN이다.

논문은 두 컴포넌트를 분리해 서술하지만 층 개수·폭·상태 인코딩·밴딧 변형을 밝히지 않는다. 분리해서 만들려면 Observer의 학습 목표를 우리가 또 정해야 하므로 **충실하려다 추측을 하나 더 하는 결과**가 된다. 이 규모(상태 8차원, 행동 2개)에서는 구조가 정책을 바꾸지 않는다.

#### 학습 데이터는 논문이 상세히 준다

`early_stopping.py`의 `LogCurve`와 `sample_curves`가 전부 논문 서술이다.

성능이 로그 곡선을 따른다(Fig 2). 가상 앱마다 초기값과 성장률이 다르다. 노이즈는 곡선 아래로 랜덤하게 내려가는 형태다 — 잘못된 파라미터를 잠깐 골랐다 회복하는 상황이다. 5회차 지연 보상. 평균 보상 증가가 5회차에 5% 이하로 정체하면 학습 종료.

**정체 구간을 만드는 기능(`plateau_at`)도 넣었다.** 논문 Fig 10(a)에서 휴리스틱 정지기가 걸려 넘어지는 그 구간이다.

**곡선을 총 이득 비율로 파라미터화했다.** 로그 스케일을 직접 주면 곡선의 급함이 임의가 되고, 너무 급하면 에이전트가 "비용을 치를 만하지 않은 회차"를 한 번도 보지 못해 영원히 계속하는 정책을 배운다. 논문의 기준점(HACC가 0.55 → 2.2 GB/s, 즉 3배)을 중심으로 0.4~4.0배를 샘플링한다.

#### 보상 — 여기가 알려진 한계다

논문 §III-D는 목표만 주고 함수를 주지 않는다. "최근 회차에 측정 가능한 개선이 없을 때 멈춘다", "성능 이득과 튜닝에 쓴 **시간**의 균형을 맞춘다".

**우리 보상:**

```
계속하기 = (현재 최적 대비 상대 개선) − step_cost
멈추기   = 0, 에피소드 종료
```

`step_cost` 기본값은 `0.05 / 5 = 0.01`이다. **휴리스틱 베이스라인의 문턱값에 맞춘 의도적 calibration이었다** — 5회차에 5%는 회차당 1%다. 그러면 두 정지기가 같은 비용/편익 문턱 위에 놓이고, 차이가 하나로 좁혀진다. **휴리스틱은 이미 실현된 개선을 뒤로 보고, 에이전트는 예측한 개선을 앞으로 본다.**

**RoTI를 직접 최대화하지 않는 이유.** RoTI는 이득을 누적 비용으로 나눈 값이다. 이득은 로그로 포화하고 비용은 선형으로 커지므로 **RoTI는 초반 몇 회차에 최대가 되고 이후 계속 떨어진다.** 최대화하는 에이전트는 거의 즉시 멈춘다. 논문 자신의 숫자도 그렇지 않다 — §IV-C는 50세대 중 35에서 멈추고 최적 RoTI의 90.5%를 얻는다고 한다. **RoTI는 논문이 보고하는 방식이고 에이전트가 최적화하는 것이 아니다.**

`5회차 지연`은 Q 목표의 5-step return으로 구현했다. 겹치는 창을 이중 계산하지 않고 "다음 다섯 회차에 무슨 일이 있었는가"로 행동을 평가한다.

#### 측정 결과 — 휴리스틱이 이긴다

합성 곡선 200개에서 비교했다. 점수는 "최적 정지 시점 대비 달성 return 비율"이다.

| 곡선 종류 | 강화학습 | 휴리스틱 | 안 멈춤 |
|---|---|---|---|
| 매끄러운 로그 곡선 | 0.939 | **0.987** | — |
| 중간에 정체 구간 | 0.871 | **0.920** | — |
| 혼합 (기본 분포) | 0.909 | **0.953** | 0.839 |

**학습은 정상이다.** 평균 정지 시점이 39/40에서 25/40으로 내려가고, 안 멈추는 정책보다 낫다. 논문의 정지 지점(50세대 중 35, 70%)과 비슷한 62% 지점에서 멈춘다.

**원인은 벤치마크 설계다.** `step_cost`를 휴리스틱의 문턱값에 맞추면 **휴리스틱이 이 보상의 거의 최적 정책이 된다.** 휴리스틱이 검사하는 것이 정확히 그 문턱이고, 합성 곡선의 best-so-far는 단조 증가라 과거 개선이 미래 개선을 잘 예측한다.

즉 **베이스라인이 최적인 시험장을 만들어 놓고 강화학습을 붙였다.** 여기서 계속 손보면 "강화학습이 이길 때까지 벤치마크를 조정하는" 함정에 빠진다.

**보류 결정했다(2026-08-12).** 지금 환경이 합성이고, 실기에서는 다르게 나올 개연성이 있다.

#### 다시 볼 때의 해법 — 근거가 논문에 있다

**회차당 비용이 일정하지 않다.** 튜닝 초반 후보들은 설정이 나빠서 실행이 느리다. 초반 회차가 비싸면 RoTI의 초반값이 눌리고 최대점이 뒤로 밀린다. **논문이 "완벽한 RoTI는 35회차"라고 쓰는 것과 맞아떨어진다.**

그리고 우리 코드는 이미 실제 소요 시간을 누적한다(`evaluator.tuning_seconds`). dry-run 합성 앱도 `elapsed = 연산시간 + 바이트/대역폭`이라 나쁜 설정이 더 오래 걸린다. **균등 비용을 가정한 곳은 조기 종료기의 합성 곡선뿐이다.**

수정 내용은 두 가지다. `step_cost`를 그 회차의 실제 소요 시간에 비례하게 바꾼다. 그리고 합성 곡선 대신 **실기 트레이스에서 관측된 곡선·비용 분포로 학습한다.**

§III-D의 "성능 이득과 튜닝에 쓴 **시간**의 균형"이 이 방향을 논문 문장으로 뒷받침한다.

#### 남는 위험 하나

**합성 곡선은 평가장이 아니라 학습 데이터다.** 정지기는 이 곡선으로 학습한 정책을 그대로 들고 실기에 나간다. "합성 환경이라 괜찮다"는 판단은 *평가*에는 맞지만 *학습*에는 그대로 적용되지 않는다.

**예상 방향:** 실기에서는 초반 회차가 비싸므로 계속하는 것의 실제 이득이 학습 때 본 것보다 크다. **정지기가 필요 이상으로 일찍 멈추는 쪽으로 편향될 것으로 예상한다.**

### 5.3 폴백 — [`rl/nets.py`](autotuner/rl/nets.py)

**PyTorch가 있으면 MLP, 없으면 random feature + 선형 헤드를 쓴다.**

개발 머신에 torch도 numpy도 없어서 **폴백이 실제로 돌아가는 경로다.** 형식적 stub이 아니라 같은 데이터·같은 보상·같은 루프로 학습한다. 은닉층은 무작위로 고정하고 선형 헤드만 학습한다 — 비선형성은 있고 학습되지 않을 뿐이다.

**MLP보다 약한 근사기이므로 `BACKEND`를 보고에 넣었다.** 폴백으로 나온 결과를 발표할 때는 그렇게 밝혀야 한다.

**학습률이 백엔드마다 다르다.** Adam+MLP는 1e-3, 선형 헤드+SGD는 5e-2다. 1e-3으로 폴백을 돌리면 수백 라운드가 필요하다 — 측정으로 확인했다.

### 5.4 구현하다 넘어진 것 세 개

기록해 두는 이유는, 다시 만들면 같은 곳에서 넘어질 것이기 때문이다.

**타깃 네트워크가 없어서 발산했다.** 손실이 단조 증가하고 정책이 "항상 계속"으로 붕괴했다. 계속의 가치가 자기 목표로 되먹임되기 때문이다. 이제 매 라운드 신경망을 복사해 얼려두고 그 사본으로 목표를 계산한다. 교과서적 처방이지만 **안 넣으면 조용히 실패하는 종류다.**

**개선폭의 기준을 잘못 잡았다.** baseline 대비로 재는데 휴리스틱은 **현재 최적 대비**로 잰다. 그러면 후반 비용이 과소평가되고 에이전트가 영원히 계속한다. 기준을 통일했다.

**정체 판정이 워밍업 구간에서 먼저 발동했다.** 논문의 조건(평균 보상이 5회차에 5% 이하 증가)을 문자 그대로 적용하면 처음 열 라운드 정도는 정책이 균일하게 "항상 계속"이라 점수가 아예 안 움직인다 — 수렴과 무관한 이유로 평평하다. 최소 라운드 하한과 **정책 안정성 조건**(평균 정지 시점이 반 회차 이내로 안정)을 추가했다.

---

## 6. C shim — [`src/autotuner_hdf5.c`](src/autotuner_hdf5.c)

### 6.1 원리

**애플리케이션을 다시 컴파일하지 않고 HDF5 설정을 바꾼다.**

`LD_PRELOAD`로 라이브러리를 지정하면 그 라이브러리가 원래 함수보다 먼저 호출된다. shim은 이 방식으로 `H5Fcreate`, `H5Dwrite`, `H5Dcreate1`, `H5Dcreate2`를 가로챈다.

가로챈 뒤 `config.xml`을 읽고 파라미터를 주입하고 원래 함수를 호출한다. 애플리케이션은 자기 설정이 바뀐 것을 모른다.

**TunIO도 이 계층을 그대로 쓴다.** 논문 §III-A가 명시한다. 바뀐 것은 그 위의 탐색 드라이버다.

### 6.2 추가한 파라미터 5개 (2026-08-13)

**HDF5 1.14.1-2 기준이다.** 대상 클러스터에 설치된 버전이다(parallel ON, Fortran/C++ 바인딩 OFF).

| 파라미터 | 호출 | 구현 |
|---|---|---|
| `meta_block_size` | `H5Pset_meta_block_size` | `strtoull` 파싱 |
| `chunk_cache` | `H5Pget_cache` → `H5Pset_cache` | `rdcc_nbytes`만 바꾸고 슬롯 수·축출 정책은 기존 값 유지 |
| `coll_metadata_write` | `H5Pset_coll_metadata_write` | **1.10+.** 미만이면 `#if`로 빠지고 런타임 경고 |
| `col_meta_ops` | `H5Pset_all_coll_metadata_ops` | 동일 |
| `mdc_conf` | `H5Pget_mdc_config` → 수정 → `H5Pset_mdc_config` | 프리셋 3종 |

**`mdc_conf`는 get 후 크기 필드만 덮는다.** `H5AC_cache_config_t`는 필드가 20개 넘고 `min ≤ initial ≤ max` 같은 상호 제약이 있어서, 안 건드린 필드는 HDF5가 이미 유효하다고 본 값을 유지하게 했다.

### 6.3 고친 버그 7건 (2026-08-13)

계획서 §6에 4건이 있었고, 고치는 과정에서 3건을 더 찾았다.

**1. `fopen` 결과를 검사하지 않았다.** 스트림이 그대로 XML 파서로 갔다. `config.xml`이 없으면 앱이 죽었고, **튜너가 다시 쓰는 중인 파일을 읽어도 죽었다.** 이제 실패 시 경고 후 튜닝 없이 파일을 만든다. **앱을 깨뜨리는 shim이 아무것도 튜닝하지 않는 shim보다 나쁘다.**

**2. `AT_CONFIG_FILE`을 되살렸다.** 경로가 프로세스 작업 디렉터리에 하드코딩되어 있었다. 랭크마다 자기 cwd에 사본이 필요했고 **후보 동시 평가가 원천 차단**되어 있었다.

**3. 비-MPIO 드라이버에서 원본을 호출하지 않았다.** 메시지를 찍고 `-1`을 반환했다. **직렬 HDF5 앱에 `LD_PRELOAD`를 걸면 파일 생성이 실패했다.** 슬라이서가 `H5Pset_fapl_mpio`를 떨어뜨렸을 때 I/O 커널이 걸리는 함정도 이것이다.

이제 **HDF5 계층 파라미터는 양쪽 다 적용한다.** property list 설정이라 MPI가 필요 없다. MPI-IO 힌트·파일시스템 힌트·collective 메타데이터만 건너뛴다.

> **행동 변화다.** 이전엔 비-MPIO 앱에서 파일 생성이 실패했고 이제는 성공한다. 기존 동작에 의존한 스크립트가 있다면 영향받는다.

**4. `bglockless:` 프리픽스가 무효였다 — 무효보다 나빴다.** 프리픽스된 이름을 지역 버퍼에 만든 뒤 `strcpy(filename, new_filename)`으로 **호출자의 문자열에 썼다.** 그 문자열은 `const`이고 쓰려는 내용보다 11바이트 짧다. **버퍼 오버플로다.** 게다가 결과가 반영되지도 않았고 malloc한 버퍼는 누수됐다. `IBM_lockless_io`가 거의 `true`가 아니라서 터지지 않았을 뿐이다.

**5. `H5Dcreate1`과 `H5Dcreate2`에 1·2번 버그가 그대로 있었다.** **데이터셋마다 호출되므로 누수가 `H5Fcreate`보다 심하다.** 세 훅이 각자 복사본을 갖고 있어서 `at_load_config()` 헬퍼로 뽑았다.

**6. `MPI_Info`가 매 `H5Fcreate`마다 누수됐다.** `H5Pget_fapl_mpio`는 호출자 소유 사본을 돌려주고 `H5Pset_fapl_mpio`는 자기 사본을 만드는데 어느 쪽도 해제되지 않았다.

**7. `sieve_buf_size`와 `alignment`가 `H5Pset_*` 반환값을 버렸다.** 컴파일러 경고로 드러났다. **주입 실패가 조용히 지나가는 것은 이 실험에서 정확히 피해야 하는 실패 모드다.** 파싱도 `atoi` → `strtoull`로 바꿨다.

### 6.4 함께 고친 것

**빈 엘리먼트 가드.** `<sieve_buf_size/>`처럼 텍스트 자식이 없는 노드는 모든 분기에서 `node->child->value.text.string`을 역참조해 앱을 죽였다. 정적 shim에는 체크가 있었고 동적 shim에만 없었다.

**`.misc/config.sh`.** HDF5를 1.8.12 → 1.14.1-2로 올렸다. 다운로드 URL이 죽은 경로여서 GitHub 릴리스를 1순위로 두고 **검증 필요 주석**을 달았다.

그리고 이 스크립트는 **원래부터 `bash -n`을 통과하지 못했다.** `if` 7개 `fi` 6개로 NetCDF4 블록의 `fi`가 빠져 있었다. 건드린 파일이므로 함께 고쳤다.

### 6.5 테스트

[`test/test_hdf5_metadata_params.c`](test/test_hdf5_metadata_params.c), 약 200줄.

**기존 방식을 따르지 않았다.** 저장소의 14개 테스트는 `ph5example.c`를 복제한 것이고 16,800줄에 고유 로직이 600줄이다. 그런데 **5개 파라미터가 모두 `H5Fcreate` 한 지점에서 주입되므로 한 프로그램이 전부 검증할 수 있고, 실제 병렬 쓰기 워크로드가 필요하지 않다.**

`test/config.xml.in`의 `mdc_conf`는 일부러 `aggressive`로 뒀다 — `default`는 아무것도 안 하는 분기라 프리셋 경로가 실행되지 않는다.

**한계는 기존 테스트와 같다.** property list를 되읽으므로 "shim이 썼다"는 확인되지만 "파일시스템이 적용했다"는 아니다. `03-원본-코드-분석.md` §7.3.

---

## 7. 논문과 다른 점 — 전체 목록

**"TunIO를 재현했다"고 말할 때 근거가 되어야 하므로 전부 모았다.** 뒤집을 수 있다.

| # | 항목 | 논문 | 우리 | 왜 |
|---|---|---|---|---|
| 1 | 파라미터 개수 | §III-C 11개, §IV 12개 | alignment를 둘로 세어 12개 | 불일치 해소를 위한 **해석**. 부수적으로 독립 변이 가능 |
| 2 | 후보 값 목록 | 없음 | 전부 우리 것 | `evolve.py` 것을 계승, 결함 셋 수정 |
| 3 | 공간 크기 | 21.8억 | 4.48억 | 억지로 맞추면 가짜 정밀도 |
| 4 | 부분집합 표현 | 미명시 | 파라미터별 점수 + 상위 k | 4,096개를 40번에 못 배움 |
| 5 | 부분집합 크기·일정 | 미명시 | 자유 파라미터의 1/3 고정 | 우리 선택 |
| 6 | 빠진 파라미터의 값 | 미명시 | 개체의 현재값 유지 | 엘리티즘 서술과 정합 |
| 7 | picker 보상의 성능 항 | perf **수준** | baseline 대비 **개선폭** | 수준은 나중 회차를 부당하게 우대 |
| 8 | 두 보상 항의 가중치 | 미명시 | 1:1 (논문의 단순 차이) | 우리 선택 |
| 9 | 영향도 순위 | PCA | 일원 효과 크기 | PCA→순위 매핑 미명시, 입력만의 PCA는 목표 정보 없음 |
| 10 | 사전학습→정책 전이 | 미명시 | 입력 특징 + warm-start 회귀 | 가장 직접적 |
| 11 | 신경망 구조 | 2단계 서술, 세부 미명시 | 단일 신경망 end-to-end | 분리하면 추측을 하나 더 함 |
| 12 | stopper 보상 | 미명시 | 개선 − step_cost | §III-D의 "시간과의 균형"에서 읽음 |
| 13 | 회차 비용 | 미명시 | 균등 가정 (합성 곡선) | **알려진 한계.** §5.2 |
| 14 | 반복 축약 | 3회 평균 | 기본 `best`, TunIO는 `mean` | `evolve.py` 계승. 논문 값도 선택 가능 |
| 15 | 변이율 | 유전자당 0.15 | 동일 (기본) | `--scale-mutation`은 꺼짐 |
| 16 | 세대 수 | §IV-C는 50 | **40** | H5Tuner와 같은 예산. 50이면 TunIO에 25% 더 줌 |
| 17 | `chunk_cache` | 미명시 | `rdcc_nbytes` 1차원 | 4인자 중 하나 |
| 18 | `mdc_conf` | 미명시 | 프리셋 3종 | 구조체 20+ 필드 |
| 19 | RL 프레임워크 | Keras + Gym | PyTorch, 없으면 폴백 | 알고리즘 동일 |
| 20 | I/O Discovery | 있음 | **구현됨** | 2026-08-25. §13 |
| 21 | 대역폭 측정 | Darshan | **Darshan + 정규식 폴백** | 2026-08-25. §13 |
| 22 | 반복 실행의 시간 | 튜닝 비용에 누적하지 않는다 | 동일 (기본) | 재독으로 발견, 같은 날 맞췄다. `--repetition-cost all`로 되돌릴 수 있다 |

**22번을 따로 설명한다.** §IV Methodology가 "The time cost of running the application is not accumulated across runs"라고 명시한다. 후보를 3회 실행하지만 그 시간을 튜닝 비용에 넣지 않는다는 뜻이다. 근거는 "플랫폼마다 변동성이 다르고, 추가 실행은 그 플랫폼에 필요한 비용으로 볼 수 있다"는 것이다.

**우리는 반복 실행 시간을 전부 누적한다.** `evaluate.py`의 `cost = time.time() - started`가 모든 회차의 벽시계다.

**이게 RoTI를 직접 바꾼다.** TunIO는 반복이 3회이므로 우리 RoTI가 논문 회계 대비 대략 1/3로 나온다.

**그리고 프레임워크 비교를 기울인다.** H5Tuner의 기본 반복은 5회이고 TunIO는 3회다. 우리 회계에서는 H5Tuner가 후보당 5배, TunIO가 3배의 비용을 낸다. 논문 회계에서는 둘 다 1배다. 즉 우리 쪽 비교가 반복 횟수 차이만으로 H5Tuner에게 불리하다.

**논문에 맞췄다(2026-08-25).** 기본값이 `--repetition-cost one`이고, 후보 하나의 비용은 반복의 평균에 공유된 실행 오버헤드를 나눠 더한 값이다. 실제로 쓴 벽시계는 `wall_seconds`에 따로 쌓이고 요약에 함께 나온다.

**효과를 확인했다.** 같은 캠페인에서 최적값은 2,459 MB/s로 같고 RoTI가 31,182에서 85,807로 올라간다. 반복 5회에 비례하는 차이다.

**`--repetition-cost all`로 되돌릴 수 있다.** 실제로 지불한 시간을 목적함수에 넣고 싶을 때 쓴다.

**재현하지 않은 원본 버그 둘.** `h5tuner-original`에서도 일부러 재현하지 않았다. `config.xml` 경로 결합 누락(구분자가 없어 홈 디렉터리 옆에 파일이 떨어졌다)과 정리가 실행되지 않던 버그(`Popen(리스트, shell=True)`가 첫 원소만 실행). 설계가 아니라 결함이므로 재현 대상이 아니다.

---

## 8. 검증 상태

**무엇이 확인됐고 무엇이 안 됐는지 구분하는 것이 중요하다.**

### 실행으로 확인

유전 알고리즘이 수렴한다. 합성 지형에서 8세대에 299 → 1013 MB/s.

원본 재현판이 정확히 13,440 조합을 탐색한다. 원본의 14개 alignment 쌍을 그대로 쓴다.

`config.xml`이 `xml/config_lustre.xml`과 같은 구조로 나온다.

휴리스틱 정지기가 21세대에서 "5세대 동안 5% 개선 없음"으로 멈춘다.

기록 → 순위 → 재생 왕복이 된다. 2,187점 격자를 쓸고 순위를 뽑고 앱 실행 0회로 탐색한다.

영향도 순위가 온전성 검사를 통과한다.

정적 부분집합이 논문 방향대로 동작한다. 상위 3개만 튜닝하면 6세대에 1639 MB/s, 전부 튜닝하면 10세대에 1265 MB/s.

RL picker가 트레이스 순위로 warm-start되고 세대별로 집합을 바꾼다.

RL stopper가 학습된다. 평균 정지 39/40 → 25/40, 폴백 백엔드로 약 30초.

트레이스에 없는 설정에서 명확한 오류. 보간하지 않는다.

C 코드가 스텁 헤더로 **HDF5 1.14.1과 1.8.12 양쪽 경로 모두 `-Wall` 경고 없이** 문법 검증 통과.

**HDF5 1.14.1-2와 mxml 2.9의 진짜 헤더로 API 시그니처를 전부 대조했다(2026-08-25).** 함수 열, 구조체 필드 다섯, 타입과 매크로가 모두 일치한다. 상세는 아래 "확인되지 않음"의 실기 컴파일 항목.

PBS Pro 런처와 h5bench 어댑터가 가짜 스케줄러·가짜 벤치마크로 전 경로 통과. 세 프레임워크 모두. 상세는 §12.

I/O Discovery가 합성 예제를 논문 Figure 5대로 축약한다. 54줄 중 25줄을 남기고, 산출 커널이 `-Wall` 경고 없이 컴파일되며, H5 호출 7개를 하나도 잃지 않는다. 루프 축약과 경로 전환을 적용한 커널도 컴파일된다. 상세는 §13.

Darshan 경로가 가짜 `darshan-parser`로 통과. 잡 스크립트가 두 라이브러리를 `LD_PRELOAD`에 넣고, 후보마다 로그가 남고, 읽은 값이 출력 스크레이핑 값과 일치한다.

**반복 측정이 대역폭에 실제로 반영되도록 고쳤다.** 그 전에는 `Bandwidth.score`가 `measurement.bw_write` 한 값만 봤고 그건 마지막 회차의 값이었다. `--reduction mean`이 벽시계에만 걸리고 대역폭에는 안 걸렸다는 뜻이다. TunIO의 "후보당 3회 측정"이 사실상 1회였다. 이제 평가기가 회차마다 대역폭을 긁어 목적함수의 축약 규칙으로 접는다.

XML 5개 파싱, `.misc/config.sh` `bash -n` 통과.

**TunIO 모듈을 실행 테스트 14건으로 검증했다 (2026-08-31). 버그를 찾지 못했다.**

유전 연산자가 정확하다. 변이율을 0.9로 올려도 부분집합 마스크 밖 유전자에 새 값이 생기지 않는다. 토너먼트는 논문대로 3명 중 2명이고 엘리트가 보존된다.

RoTI 수식이 맞다. 이득 1,000 MB/s를 60초로 나누면 1,000이고 120초면 500이다.

휴리스틱 정지기가 10세대부터 정체하는 곡선에서 정확히 15세대에 멈춘다. 5세대 창이 의도대로 동작한다. RL 정지기는 학습되고 로그 곡선에서 17/20에 멈춘다.

부분집합 선택기의 warm start가 prior를 재현한다. 탐험을 배제한 모델 순위가 8개 시드 전부에서 일치하고, 점수가 prior 값을 거의 그대로 낸다(1.0078 대 1.0).

세대 단위 배치 경로가 정확하다. 순서 보존, 캐시 히트 혼재, 일부 후보 실패 격리, 큐 대기 분리가 모두 맞다.

정지기를 baseline 없이 부르면 조용히 잘못된 값을 쓰지 않고 `RuntimeError`를 낸다.

**설계 논점 하나를 남긴다.** picker의 탐험률이 0세대에서 최대다(`epsilon=0.3`). sweep으로 얻은 순위를 첫 세대에서 30% 확률로 흔든다. 논문도 Figure 9에서 초기의 잘못된 부분집합 선택을 인정하므로 모순은 아니지만, sweep에 자원을 쓴 뒤 그 결과를 바로 흔드는 것은 아깝다. 0세대만 탐험을 끄는 것은 한 줄 변경이고, 논문 미명시 지점이다.

**shim이 누리온에서 실제로 빌드된다 (2026-08-28).** 진짜 HDF5 1.14.1.2 헤더로 `-Wall` 경고 없이 컴파일되고 `libautotuner.so`가 나온다. GCC 10.2.0 + OpenMPI 3.1.0 + `hdf5-gcc` 툴체인이다.

**세대 단위 잡 배치가 동작한다.** 4세대·개체 6에서 잡이 16개에서 5개로 준다. 측정값은 그대로다.

---

### 확인되지 않음

**실기 컴파일은 2026-08-28에 통과했다.** 아래 기록은 그 전에 API를 어떻게 검증했는지 남긴 것이다. 누리온에서 실제 빌드가 되며, 예측대로 시그니처 문제는 하나도 없었다.

**남은 실기 미확인 항목은 셋이다.** 힌트가 Lustre에 반영되는지(확인 잡 대기 중), `test/` 프로그램 링크(§TODO 4.2), 그리고 튜닝 캠페인 자체다.

**아래는 2026-08-25의 API 검증 기록이다.**

**API 시그니처는 진짜 헤더로 전부 대조했다.** HDF5 1.14.1-2 태그의 `H5ACpublic.h`, `H5Ppublic.h`, `H5FDmpio.h`, `H5public.h`와 mxml 2.9의 `mxml.h`를 받아 우리 호출과 하나씩 맞춰봤다. 결과는 전부 일치다.

`H5AC_cache_config_t`의 필드 다섯(`version`, `set_initial_size`, `initial_size`, `min_size`, `max_size`)이 이름과 타입까지 맞고, `H5AC__CURR_CACHE_CONFIG_VERSION`은 1이다. `H5Pset_all_coll_metadata_ops`와 `H5Pset_coll_metadata_write`는 둘 다 `(hid_t, hbool_t)`이고, 1.14.1의 `hbool_t`는 `bool`이라 우리 `at_parse_bool`의 반환 타입이 맞다. `H5Pget_cache`의 네 출력 인자 타입(`int*`, `size_t*`, `size_t*`, `double*`)도, `H5Pset_alignment`의 `hsize_t` 둘도, `H5Pset_meta_block_size`의 `hsize_t`도, `H5Pset_sieve_buf_size`의 `size_t`도 우리 캐스팅과 같다. `H5_VERSION_GE` 매크로도 1.14.1에 실재한다.

**MANUAL이 이전에 `H5Pget_all_coll_metadata_ops`를 불확실 항목으로 적었는데 우리 코드는 그 함수를 쓰지 않는다.** `H5Pset_` 쪽만 쓴다.

**mxml도 확인했다.** `.misc/config.sh`가 2.9로 고정하고 있어 4.x의 API 변경과 무관하다. 우리가 쓰는 심볼 일곱이 전부 2.9에 있고, `mxmlLoadFile(mxml_node_t*, FILE*, mxml_type_t (*)(mxml_node_t*))` 시그니처가 우리 호출과 맞으며, `node->child->value.text.string` 접근 경로가 `mxml_node_s`, `mxml_value_u`, `mxml_text_s` 정의와 일치한다.

**그래서 남은 위험은 시그니처가 아니라 빌드 환경이다.** 헤더 포함 순서, 매크로 충돌, autotools 설정, MPI 링크, 그리고 실행이다. 훨씬 작지만 0은 아니다.

**실기 실행 전체.** MPI + parallel HDF5 + Lustre에서 아무것도 돌려보지 못했다.

**힌트가 파일시스템에 반영되는지.** 테스트는 property list를 되읽을 뿐이다.

**`.misc/config.sh`의 다운로드 URL.** GitHub 릴리스 경로를 넣었지만 검증하지 못했다.

**PyTorch 백엔드.** 폴백만 실행해봤다. torch 경로는 코드 리뷰만 했다.

**단위 테스트가 없다.** CLI를 직접 돌려 확인했다.

---

## 9. 사용법 모음

```bash
# 목록
python3 -m autotuner info

# 합성 모델로 탐색 확인 (MPI/HDF5 불필요)
python3 -m autotuner run --framework h5tuner --dry-run --seed 1 \
    --generations 8 --population 10

# 원본 재현판 (벽시계 시간 최소화, 4차원, 13,440 조합)
python3 -m autotuner run --framework h5tuner-original --dry-run --seed 1

# 격자를 쓸어 트레이스 기록
python3 -m autotuner sweep --space minimal --levels 3 --dry-run \
    --record traces/grid3.jsonl

# 영향도 순위
python3 -m autotuner rank traces/grid3.jsonl --space minimal --out ranking.txt

# 재생 (앱 실행 0회)
python3 -m autotuner run --framework h5tuner --space minimal --levels 3 \
    --replay traces/grid3.jsonl --seed 1 --generations 10

# TunIO 전체
python3 -m autotuner run --framework tunio --space minimal --levels 3 \
    --replay traces/grid3.jsonl --ranking-from-trace traces/grid3.jsonl \
    --subset-size 3 --seed 1 --rl-seed 7

# ablation: 정지기만 휴리스틱 (강화학습 정지기의 기여 분리)
... --stopper heuristic --subset rl

# ablation: 부분집합만 정적 순위 (학습의 기여 분리)
... --stopper rl --subset static

# ablation: H5Tuner + 토너먼트 선택 (선택 연산자만 분리)
... --stopper never --subset all

# 실기 -- allocation 안에서 (qsub -I 또는 배치 스크립트)
python3 -m autotuner run --framework h5tuner --hdf5-version 1.14.1 \
    --ld-preload /path/to/H5Tuner/lib/libautotuner.so \
    --app-cmd 'mpirun -n 32 ./my_app' \
    --perf-regex 'Write bandwidth: ([0-9.]+) MB/s'

# 실기 -- h5bench를 PBS 잡으로. 상세는 §12
python3 -m autotuner run --framework h5tuner --hdf5-version 1.14.1 \
    --app h5bench-write --h5bench-bin $H5BENCH/h5bench_write \
    --ld-preload /path/to/H5Tuner/lib/libautotuner.so \
    --launcher pbs --pbs-account etc --pbs-select 4 \
    --pbs-module craype-mic-knl --pbs-module intel/18.0.3 --pbs-module impi/18.0.3 \
    --workdir /scratch/$USER/tune
```

`AT_CONFIG_FILE`은 튜너가 후보마다 절대 경로로 설정하므로 손댈 필요가 없다. `LD_PRELOAD`는 셸에서 export하지 말고 `--ld-preload`로 준다. 셸에 걸면 python과 qsub도 shim을 로드한다.

---

## 10. 다음에 할 일

```
[먼저] 실기 컴파일 확인               ← C 955줄을 고쳤고 스텁 검증만 했다
        ↓
[C] 실기 스윕 → 트레이스 기록          ← 클러스터 필요. 병목을 푼다
     PBS 런처와 h5bench 어댑터는 준비됐다(§12). 남은 것은 실기 확인뿐이다
        ↓
[D] picker prior 교체(무료) + stopper 재학습(보상 비용 항)
        ↓
[E] H5Tuner vs TunIO 비교 실험

독립: [F] ~~I/O Discovery~~ 완료   [G] ~~Darshan~~ 완료   [H] 단위 테스트
      [I] 논문 충실도 남은 셋: PCA(#9), 2단 신경망(#11), 세대 50(#16)
```

**실기 컴파일이 먼저다.**

```bash
export HDF5ROOT=/path/to/00-dependency/installation-home/hdf5
export MXMLROOT=... MPIROOT=...
./autogen.sh && ./configure && make && make check
```

**[C] 실기 스윕.** `--levels 2`면 `paper12`가 4,096점이다. 후보당 1분이면 약 68시간이므로 배치 잡으로 쪼개거나 `--samples`로 표본을 쓴다.

**[D]** picker는 `--ranking-from-trace`로 코드 변경 0. stopper는 §5.2의 수정이 필요하다(반나절).

**[F] I/O Discovery — 범위 외로 둘지 판단이 필요하다.** TunIO 세 컴포넌트 중 유일한 미착수 항목이다. 파이썬 800~1500줄로 남은 작업 중 가장 크다.

**빼고 가는 것도 정당하다.** 탐색을 개선하는 기법이 아니라 평가를 싸게 만드는 기법이고, 세 프레임워크 모두 전체 앱으로 평가하면 탐색 비교는 오히려 깔끔하다. "TunIO의 평가 비용 절감은 범위 외"라고 명시하면 된다.

**하기로 하면 루프 축약을 먼저 떼어낸다.** 논문 수치로 RoTI 2.47 → 23.30이 축약에서 나오고 AST 슬라이싱 단독은 2.87뿐이다. 축약은 앱에 반복 인자를 노출하는 것으로 시작할 수 있다. 계획서 §4.4.

**신규 프레임워크는 아직 논의하지 않았다.** 방법론(GA·RL·Bayesian Optimization 등)을 교체 가능하게 만들고 싶다는 요구가 있었고, 그건 베이스라인이 아니라 신규 프레임워크의 설계 문제로 미뤄 뒀다.

---

## 11. 커밋 상태

**아직 커밋하지 않았다.** 실기 컴파일이 확인된 뒤가 안전하다.

```
 M .gitignore
 M .misc/config.sh
 M src/autotuner_hdf5.c        955줄 변경
 M configure.ac                 MPI 라이브러리 자동 감지 (-lmpich 하드코딩 수정)
 M autotuner/{evaluate,objective,cli}.py   PBS·h5bench·Darshan 통합, 반복 집계 수정
 M autotuner/frameworks/base.py           세대 단위 배치 평가
?? autotuner/launchers.py      신규
?? autotuner/apps.py           신규 (h5bench, MACSio)
?? autotuner/darshan.py        신규
?? autotuner/discovery/        신규 (I/O Discovery)
?? test/stubs/hdf5.h           신규 (파싱 전용)
 M test/Makefile.am
 M test/config.xml.in
 M xml/config.xml, config_chunking.xml, config_chunking_IBM.xml, config_lustre.xml
?? autotuner/                  신규 4,238줄
?? test/test_hdf5_metadata_params.c
```

문서 01~07 과 `traces/` 는 2026-09-14 에 공개하기로 결정하고 `.gitignore` 에서 뺐다. 그 전까지는 원격이 public 이라 로컬에만 두었다.

브랜치 주의: 로컬은 `main`, 원격은 양쪽 다 `master`다. `main`의 upstream이 `origin/master`(upstream 원본)로 잡혀 있어 인수 없는 `git push`는 **upstream을 향한다.** `git push h5tuner-ext main:master`처럼 명시할 것.

---

## 12. h5bench를 PBS Pro에서 튜닝하기

**결론부터.** `--launcher pbs --app h5bench-write` 두 플래그로 후보마다 PBS 잡을 제출하며 튜닝한다. 누리온(KISTI) 기준으로 썼고, 세 프레임워크 모두 같은 경로를 쓴다.

### 실행 순서

**세 단계다.** 먼저 격자를 쓸어 트레이스를 만들고, 거기서 파라미터 영향도 순위를 뽑고, 그 순위를 TunIO에 먹인다.

TunIO에 순위를 안 주면 탐색이 개선되지 않는다. 실제로 확인했다. 부분집합 선택기가 사전 지식 없이 시작하면 영향 없는 파라미터만 고르다가 끝난다. 논문도 파라미터 스윕으로 사전학습한다고 쓴다.

```bash
# 1단계. 격자를 쓸어 트레이스 기록
python3 -m autotuner sweep --space minimal --levels 2 \
    --app h5bench-write --h5bench-bin $H5BENCH/h5bench_write \
    --ld-preload $HOME/H5Tuner/lib/libautotuner.so \
    --launcher pbs --pbs-account etc --pbs-select 4 \
    --pbs-module craype-mic-knl --pbs-module intel/18.0.3 --pbs-module impi/18.0.3 \
    --workdir /scratch/$USER/tune --record traces/grid.jsonl

# 2단계. 영향도 순위 (앱 실행 0회)
python3 -m autotuner rank traces/grid.jsonl --space minimal --out ranking.txt

# 3단계. 세 프레임워크 실행
python3 -m autotuner run --framework h5tuner --space minimal --levels 2 \
    --app h5bench-write --h5bench-bin $H5BENCH/h5bench_write \
    --ld-preload $HOME/H5Tuner/lib/libautotuner.so \
    --launcher pbs --pbs-account etc --pbs-select 4 \
    --pbs-module craype-mic-knl --pbs-module intel/18.0.3 --pbs-module impi/18.0.3 \
    --workdir /scratch/$USER/tune --seed 1

python3 -m autotuner run --framework tunio --space minimal --levels 2 \
    --ranking-from-trace traces/grid.jsonl --subset-size 4 \
    ... 나머지 동일 ...
```

### 두 가지 운용 모델

**`--launcher pbs`는 후보마다 잡을 제출한다.** 튜너가 로그인 노드에 살고, 잡을 내고, 완료를 기다리고, 출력을 읽는다. allocation 시간 제한을 안 받는 대신 후보마다 큐 대기를 낸다.

**`--launcher local`은 지금 이 프로세스에서 실행한다.** `qsub -I`로 잡은 노드 안이나 배치 스크립트 안에서 튜너를 돌릴 때 쓴다. 큐 대기는 처음 한 번뿐이지만 캠페인 전체가 한 allocation에 들어가야 한다. 기본값이고, 로그인 노드에서 이걸 쓰면 경고가 나온다.

### 완료 감지는 qstat이 아니라 sentinel 파일이다

**잡 스크립트가 마지막에 `.autotuner-done`을 쓴다.** 거기에 반복 회차별 종료 코드와 소요 시간이 한 줄씩 들어간다. 튜너는 이 파일을 폴링한다.

qstat을 안 쓰는 이유가 둘이다. PBS는 잡이 끝난 뒤에야 stdout을 제출 디렉터리로 복사하므로, qstat이 완료라고 말하는 시점에 출력이 아직 디스크에 없을 수 있다. 그리고 시작하지 못한 잡, 보류된 잡, 관리자가 지운 잡에 대해 qstat이 사이트마다 다르게 답한다.

qstat은 한 가지에만 쓴다. 잡이 sentinel을 안 남기고 큐에서 사라졌는지 확인하는 것이다.

**출력도 잡 스크립트가 직접 리다이렉트한다.** `#PBS -o`에 맡기면 복사 시점이 sentinel과 경합한다.

**`#PBS -m n`을 항상 넣는다.** 사이트 기본값이 잡마다 메일을 보내므로, 넣지 않으면 캠페인 하나가 메일 수십 통을 만든다.

### 한 세대가 잡 하나다

**후보마다 잡을 내지 않는다. 세대 전체를 한 잡에서 평가한다.**

누리온의 `normal` 큐는 대기 1,000개, 실행 700개를 허용하므로 개수는 제약이 아니었다. 제약은 대기 시간이다. 2026-08-28 확인 시점에 큐가 88% 사용 중이고 380개가 대기하고 있었다.

40세대에 개체 15면 후보가 600개다. 후보마다 잡을 내면 큐 대기를 600번 낸다. 세대로 묶으면 40번이다.

**측정은 달라지지 않는다.** 세대 안의 후보는 어차피 순차로 평가한다. 같은 allocation 안에서 도는 것뿐이다.

가짜 스케줄러로 확인했다. 4세대에 개체 6이면 잡이 16개에서 5개로 준다(baseline 1 + 세대 4).

**큐 대기는 배치당 한 번만 기록한다.** 첫 후보에 붙이고 나머지는 0으로 둔다. 어차피 튜닝 예산에는 넣지 않으므로 총계만 맞으면 된다.

### 반복 측정은 한 잡 안에서 돈다

**H5Tuner는 후보당 5회, TunIO는 3회 측정한다.** 논문 설정이고 프레임워크 기본값이다.

이걸 잡 5개로 쪼개면 후보 하나가 큐 대기를 다섯 번 낸다. 그래서 한 잡 안에서 반복한다. 후보 16개짜리 캠페인이 잡 80개가 아니라 16개가 된다.

각 회차마다 어댑터의 preamble이 다시 돈다. 출력 HDF5 파일을 지우는 일이다. Lustre는 파일을 만들 때 스트라이핑을 적용하므로, 기존 파일을 재사용하면 앞 후보의 스트라이프 배치를 물려받고 파라미터 두 개가 효과를 잃는다.

### 큐 대기는 튜닝 예산에 넣지 않는다

**RoTI는 대역폭 이득을 튜닝 시간으로 나눈 값이다.** 큐 대기를 넣으면 RoTI가 그날 기계가 얼마나 붐볐는지의 함수가 된다.

같은 프레임워크를 두 번 돌려도 값이 달라지고, H5Tuner와 TunIO를 비교하는 일 자체가 의미를 잃는다. 이 코드베이스의 목적이 그 비교이므로 넣지 않기로 했다.

**대신 따로 재서 보고한다.** 캠페인이 실제로 얼마나 걸렸는지는 여전히 알 수 있다.

### h5bench 특유의 함정 셋

**h5bench 러너(`h5bench config.json`)를 쓰면 안 된다.** 러너는 벤치마크를 `env=self.vol_environment`로 띄운다. 우리가 넣은 `LD_PRELOAD`가 사라질 수 있다. 그리고 러너가 JSON의 `file-system` 절에서 Lustre 스트라이핑을 직접 설정하는데, 그건 우리가 튜닝하는 파라미터 둘이다. 개별 바이너리를 직접 `mpirun`한다.

**대역폭 단위가 실행마다 바뀐다.** h5bench는 모든 속도를 `format_human_readable()`에 통과시킨다. 1024로 나누어 떨어질 때까지 나누고 맞는 접두어를 붙인다. 같은 벤치마크가 느린 기계에서는 MB/s, 빠른 기계에서는 GB/s를 찍는다.

고정 단위를 가정하면 후보가 빨라지는 순간 1024배 낮게 기록된다. 탐색이 그걸 "이 후보는 최악"으로 읽으므로 오차가 탐색이 성공할수록 커진다. 그래서 스크레이퍼가 접두어를 같이 캡처해서 변환한다.

**설정 파일에 빈 줄을 넣으면 파싱이 실패한다.** h5bench의 `read_config()`는 `=`가 없는 줄을 만나면 -1을 반환하고 멈춘다. 값 뒤의 공백도 위험하다. `MEM_PATTERN` 같은 키는 원본 문자열을 그대로 `strcmp`하므로 `CONTIG `가 조용히 `PATTERN_INVALID`가 된다. 어댑터가 셋 다 지킨다.

### 크기를 지정하는 방법이 함정이다

**`NUM_PARTICLES`를 쓰면 안 된다. `NUM_DIMS`와 `DIM_1`을 써야 한다.**

`read_config()`가 쓰기 연산에서 `num_particles`를 `dim_1 * dim_2 * dim_3`로 덮어쓴다. 기본값이 전부 1이므로 `NUM_PARTICLES`만 설정한 설정 파일은 랭크당 입자 하나를 쓴다.

**크기 접미사에는 앞 공백이 필요하다.** `parse_unit()`이 숫자와 단위를 공백으로 나눈다. `512K`는 512가 되고 `512 K`라야 524,288이 된다.

**어댑터는 접미사를 정수로 전개해서 이 규칙을 피한다.** `--h5bench-particles 512K`를 주면 `DIM_1=524288`을 쓴다.

**둘 다 실기에서만 드러났다.** 에러가 나지 않고 조용히 4KB짜리 실험이 된다.

### h5bench write에서 죽는 파라미터가 둘 있다

**12개 중 2개는 이 워크로드에서 효과가 없고, 4개는 약하다.** 소스를 읽고 확인했다(2026-08-25).

`chunk_cache`는 무효다. h5bench는 `COMPRESS=YES`일 때만 `H5Pset_layout(H5D_CHUNKED)`와 `H5Pset_chunk`를 부른다. 압축을 끄면 데이터셋이 contiguous이고 raw data chunk cache를 아예 쓰지 않는다.

**압축을 켜서 살리는 것은 권하지 않는다.** h5bench는 chunking과 함께 `H5Pset_deflate(9)`를 강제한다. 둘을 분리할 수 없다. deflate 9는 워크로드를 CPU 바운드로 만들어 I/O 파라미터의 효과를 전부 묻는다.

`sieve_buf_size`도 거의 무효다. sieve buffer는 부분 쓰기의 read-modify-write를 줄이는 장치인데, `CONTIG`/`CONTIG` collective write에는 부분 쓰기가 없다. **`FILE_PATTERN=STRIDED`로 바꾸면 살아난다.**

약한 넷은 `meta_block_size`, `coll_metadata_write`, `col_meta_ops`, `mdc_conf`다. 전부 메타데이터 쪽이고, h5bench write는 데이터셋을 몇 개만 만든다.

강한 여섯은 `striping_factor`, `striping_unit`, `cb_nodes`, `cb_buffer_size`, `alignment`, `alignment_threshold`다.

**이 예측은 2026-09-01 실측으로 정정됐다.** 누리온에서 171점을 쓸어 영향도를 재보니 `chunk_cache` 가 4위(25.9 MB/s), `sieve_buf_size` 가 10위(11.9 MB/s)로 **둘 다 살아 있다.** 청킹을 쓰지 않아도 chunk cache 설정이 HDF5 내부 버퍼링에 영향을 주는 것으로 보인다.

**소스만 읽고 파라미터가 죽었다고 단정하면 안 된다는 사례다.** 위 분석은 코드 경로로는 맞지만 실제 효과를 예측하지 못했다. 실측 순위는 `04-누리온-환경.md` §7d 에 있다.

논문도 §IV-B에서 "최종 설정은 12개 중 7개를 기본값에서 바꾸고 나머지 5개는 유의미한 영향이 없었다"고 쓰면서 어느 5개인지는 밝히지 않는다. **우리 데이터에는 죽은 파라미터가 하나도 없다.** 가장 낮은 `coll_metadata_write` 도 2.167 MB/s다. 워크로드가 다르니 당연한 차이다.

**메타데이터 파라미터를 살리려면 MACSio가 낫다.** h5bench에 `h5bench_macsio`로 포함돼 있고, 논문이 I/O Discovery 평가에 쓴 바로 그 응용이다. 데이터셋과 그룹을 많이 만들어 메타데이터 경로가 실제로 눌린다.

### 왜 Raw write rate인가

**`EMULATED_COMPUTE_TIME_PER_TIMESTEP`을 0으로 두고 `Raw write rate`를 읽는다.**

h5bench는 두 가지 속도를 찍는다. Raw는 쓰기 호출에 쓴 시간으로 나눈 값이고, Observed는 흉내낸 계산 시간까지 포함한 전체 시간으로 나눈 값이다. 계산 시간을 0으로 두면 둘이 거의 같아지므로 어느 쪽을 골랐는지가 실험 서술에서 문제되지 않는다. 그리고 분모에 계산 시간이 섞이면 튜닝하려는 효과가 그만큼 희석된다.

### collective metadata는 튜너가 가진다

**h5bench cfg의 `COLLECTIVE_METADATA`는 `NO`로 고정한다.** 이 값은 `H5Pset_coll_metadata_write`로 가는데, 그게 우리가 탐색하는 12개 파라미터 중 하나다.

shim은 `H5Fcreate` 훅 안에서 fapl을 고쳐 쓴다. h5bench가 fapl 설정을 끝낸 뒤다. 그러니 여기서 뭘 적어도 실행 결과는 안 바뀌고, 설정 파일만 사실과 다르게 남는다.

**`COLLECTIVE_DATA`는 반대다.** 그건 데이터셋 전송 property라 shim이 건드리지 않는다. 워크로드의 성격이므로 캠페인 내내 고정한다.

### LD_PRELOAD는 셸이 아니라 플래그로 준다

**`--ld-preload`로 준다.** 셸에서 `export LD_PRELOAD=...`을 하면 python도 qsub도 shim을 로드한다. 이 플래그는 애플리케이션 프로세스에만 넣는다.

`AT_CONFIG_FILE`은 튜너가 절대 경로로 자동 설정한다. shim은 이 변수가 없으면 작업 디렉터리의 `config.xml`을 찾는데, 스케줄러가 잡을 어디서 시작할지에 의존하게 되므로 절대 경로가 안전하다.

### 남는 디렉터리

후보마다 `<workdir>/.autotuner-jobs/cand-NNNNNN/`이 남는다. 안에 그 후보가 받은 `config.xml`, 생성된 `h5bench.cfg`, 제출된 `job.sh`, 회차별 출력 `run.N.out`, 그리고 sentinel이 있다.

**재현할 수 없는 실행을 나중에 읽을 수 있어야 하므로 남긴다.** 실기 캠페인에서 이게 유일한 증거다.

### 검증 상태

**가짜 qsub·qstat·qdel과 가짜 h5bench_write로 전 경로를 확인했다.** 가짜 h5bench는 실제 `read_config()`와 같은 규칙으로 cfg를 파싱하고, 실제 `config.xml`을 읽고, `format_human_readable()`과 같은 방식으로 단위를 스케일해서 출력한다.

세 프레임워크가 모두 돌았다. 8세대·개체 6으로 H5Tuner는 993 → 1546 MB/s에 잡 20개를 썼고, TunIO는 같은 1546 MB/s에 잡 7개를 쓰고 7세대에서 멈췄다. RoTI가 7,840 대 34,910이다.

**확인되지 않은 것은 실기다.** 실제 PBS, 실제 h5bench, 실제 Lustre에서는 아무것도 돌려보지 못했다. 그리고 C shim의 실기 컴파일이 여전히 미확인이다(§8).

---

## 13. I/O Discovery와 Darshan

**결론부터.** TunIO의 세 컴포넌트가 모두 구현됐다. I/O Discovery는 `python3 -m autotuner discover`로 쓰고, Darshan은 `--darshan-lib`로 켠다. 둘 다 실기 검증은 안 됐다.

### I/O Discovery는 무엇을 하나

**애플리케이션 소스에서 I/O에 필요한 라인만 남긴다.** 남은 것을 컴파일하면 I/O 커널이고, 튜너는 전체 앱 대신 그것을 평가한다.

이게 논문의 가장 큰 수치가 나오는 곳이다. peak RoTI가 전체 앱 2.47에서 커널 2.87로 오르고, 루프를 1%로 줄이면 23.30이 된다. **거의 전부가 루프 축약에서 온다.** AST 작업 자체의 기여는 2.47에서 2.87뿐이다.

### 쓰는 법

```bash
# 커널 만들기
python3 -m autotuner discover vpic.c \
    --include-dir $HDF5ROOT/include \
    --loop-keep 1 \
    --out vpic_kernel.c

# 직접 컴파일한 뒤 튜닝 대상으로 쓴다
mpicc -o vpic_kernel vpic_kernel.c -lhdf5
python3 -m autotuner run --framework tunio --app-cmd 'mpirun ./vpic_kernel' ...
```

**컴파일은 사용자가 한다.** 논문도 그렇다. III-E가 "provides a CLI tool ... which the user can compile using their preferred method and use as a substitute for the application"이라고 쓴다.

### 반드시 알아야 할 함정 하나

**include 경로를 안 주면 I/O를 못 찾는다. 그리고 조용히 못 찾는다.**

clang은 타입을 모르는 호출의 AST 노드를 만들지 않는다. `hid_t f = H5Fcreate(...)`에서 `hid_t`가 선언되지 않았으면 그 문장이 트리에서 **아예 사라진다.** 미해결 노드로 남는 게 아니라 없어진다.

그래서 `hdf5.h`를 못 찾으면 H5 호출로 가득한 파일이 "I/O 호출 없음"으로 보고된다. 더 나쁜 경우는 일부만 찾고 나머지를 놓치는 것이다.

**그래서 파싱 오류를 치명적으로 다룬다.** `--allow-parse-errors`로 넘길 수 있지만, 그때 나온 커널은 신뢰할 수 없다.

개발 머신에는 HDF5가 없으므로 `test/stubs/hdf5.h`를 뒀다. 파싱 전용 선언이고 링크되지 않는다. `--include-dir test/stubs`로 쓴다.

### 마킹 규칙은 논문 그대로다

**의존자 규칙 네 가지를 논문 문장대로 구현했다.** 함수 호출은 인자와 할당 좌변, 조건문은 불리언 식, 루프는 초기화·갱신·조건, 할당은 좌변이다.

거기에 닫힘 규칙 둘이 붙는다. 변수가 표시되면 역방향으로 그 변수의 모든 할당을 표시한다. 그리고 표시된 것의 문맥적 부모를 최상위까지 재귀로 표시한다.

**라인 단위로 작업하는 것도 논문 선택이다.** "Clang provides a large amount of nuance which makes it difficult to isolate statements, and therefore TunIO uses lines as a substitute."

### 우리가 논문에 없는 것을 두 가지 추가했다

**블록은 자기 안의 문장을 의존자로 갖지 않는다.** 이게 커널과 앱 복사본을 가르는 지점이다. 부모를 표시하는 것은 루프 헤더와 중괄호를 살리기 위한 것인데, 만약 블록을 표시할 때 그 안의 모든 문장이 딸려오면 `main` 깊은 곳의 H5Dwrite 하나가 함수 전체를, 이어서 파일 전체를 끌어온다. 처음 구현했을 때 실제로 그렇게 됐다. 54줄 중 38줄이 남았다.

논문의 규칙 넷은 모두 특정 자식(인자, 조건, 좌변)을 지목한다. 블록이 자기 문장에 의존한다는 말은 없다.

**전처리기 지시문과 피호출 함수 정의를 항상 유지한다.** `#include`는 AST에 커서가 없어서 어떤 마킹으로도 도달하지 못한다. 커널이 컴파일되려면 필요하고, 지시문은 명령을 실행하지 않으므로 유지 비용이 0이다.

I/O 경로에서 호출되는 헬퍼 함수는 본문을 통째로 남긴다. 재귀적으로 줄이면 커널이 작아지지만 그 함수가 계산하는 값이 바뀐다. 압축을 켜면 쓰는 데이터가 대역폭을 좌우한다. **논문은 이 경우를 다루지 않는다.** 자신의 future work에 "simulating necessary compute"를 열린 문제로 남겼다.

### 축약 둘

**루프 축약이 논문과 다른 점이 하나 있다.** 논문은 반복의 백분율을 줄인다. 우리는 절대 횟수 상한을 건다.

백분율을 적용할 수 없기 때문이다. 상한이 거의 항상 런타임 변수다. h5bench는 `TIMESTEPS`를 설정 파일에서 읽으므로 `t < timesteps`에는 컴파일 시점에 1%를 취할 값이 없다.

**대신 루프 본문 맨 앞에 상한 검사를 삽입한다.** 루프 조건을 고쳐 쓰는 것보다 안전하다. 조건을 고치려면 임의의 C 식을 파싱해야 한다.

논문이 신경 쓰는 경우에는 효과가 같다. 100 타임스텝 루프를 1회로 제한하는 것이 논문이 말하는 1% 축약이다. 차이는 원래 반복이 상한보다 적은 루프인데, 그건 손대지 않는다. **논문도 같은 한계를 인정한다.** "whenever the loop iterations are too small to reduce ... loop reduction will not be able to do anything."

**배수 보정은 하지 않는다.** 논문은 확장 가능한 지표에 축약 배수를 곱해 원래를 예측한다. 우리 목적함수는 대역폭이고 그건 비율이다. 분자와 분모가 함께 줄어든다. 논문 자신이 1% 축약에서 보고된 대역폭이 97.10% 정확하다고 측정했다. 쓰기 비중 알파도 비율이다. 배수가 필요한 것은 절대 바이트 수인데 목적함수가 그것을 읽지 않는다.

**I/O 경로 전환은 파일 경로만 바꾼다.** HDF5 파일 안의 데이터셋 이름이나 그룹 이름은 건드리지 않는다. 둘 다 문자열 인자라서 구분이 필요하고, 마지막 경로 요소에 확장자가 있는 것만 파일 경로로 본다.

### Darshan

**`--darshan-lib`로 켜면 대역폭이 Darshan 로그에서 온다.** 정규식 스크레이핑을 대체한다. 논문 III-E가 측정하는 방식이다.

정규식보다 나은 이유가 충실도 말고도 있다. 스크레이핑한 숫자는 애플리케이션이 출력하기로 한 값이다. 아무것도 출력하지 않는 앱은 튜닝할 수 없다. **특히 I/O Discovery로 만든 커널은 print 문이 제거된다.** Darshan은 프로세스가 실제로 수행한 I/O를 POSIX와 MPI-IO 계층에서 잰다.

### LD_PRELOAD 순서를 정했다

**우리 shim이 먼저다.** `LD_PRELOAD=libautotuner.so:libdarshan.so`.

shim이 file access property list를 고쳐 쓰고 나서 HDF5를 호출하므로, 뒤에 있는 Darshan이 설정이 적용된 상태의 I/O를 본다. Darshan이 앞에 오면 일부 경로에서 파라미터가 자리잡기 전의 호출이 계측된다.

**`--darshan-first`로 뒤집을 수 있다.** 사이트에서 이 판단이 틀린 것으로 밝혀질 경우를 위해 남겼다.

### 로그는 후보별 디렉터리에 남는다

`DARSHAN_LOGPATH`를 후보의 실행 디렉터리 아래 `darshan/`으로 설정한다. 찾기 쉽고, 한 후보의 로그를 다른 후보 것으로 오인할 수 없다.

**읽기 실패는 캠페인을 끝내지 않는다.** 세지고 요약에 보고된다. 로그 하나를 못 읽는 것이 캠페인을 끝낼 이유는 아니지만, 대부분을 못 읽은 캠페인은 아무것도 측정하지 않은 것이다.

### 쓰기 전용 워크로드에서는 정확하고 혼합에서는 근사다

**h5bench의 모든 패턴은 순수 쓰기이거나 순수 읽기다.** 그때 알파가 1 또는 0이고 TunIO의 perf는 집계 대역폭과 정확히 같다. `darshan-parser --perf`의 `agg_perf_by_slowest`를 그대로 쓴다.

혼합 워크로드는 읽기와 쓰기가 시간상 어떻게 겹쳤는지 가정하지 않고는 집계값을 쪼갤 수 없다. 그때는 방향별 바이트 수와 시간으로 유도하고, 그 경로는 근사라고 표시한다. 랭크별 시간이 합산되는데 실제 I/O는 겹치므로 대역폭이 과소평가된다.

### 검증 상태

**합성 예제로 전 경로를 확인했다.** 논문 Figure 5와 같은 구조의 C 파일에서 54줄 중 25줄을 남긴다. 남은 것은 H5 호출 7개 전부, 그것들의 인자로 쓰이는 변수 선언과 할당, 감싸는 루프 헤더, 그리고 `#include`다. 버린 것은 계산, 히스토그램, printf, 그리고 I/O와 무관한 루프다.

**산출 커널이 `-Wall` 경고 없이 컴파일된다.** 루프 축약과 경로 전환을 적용한 것도 그렇다.

**Darshan은 가짜 `darshan-parser`로 확인했다.** 읽은 값이 같은 실행의 출력 스크레이핑 값과 1 MB/s 안에서 일치한다.

**실기는 아무것도 확인하지 못했다.** 진짜 clang으로 h5bench 소스를 줄여본 적이 없고, 진짜 `libdarshan.so`를 붙여본 적이 없다. h5bench는 HDF5 헤더가 필요하고 이 맥에는 없다.

### 남은 논문 충실도 셋

**PCA(#9).** 논문은 스윕 결과에 PCA를 적용해 영향도를 뽑는다. 우리는 일원 효과 크기다. PCA 성분에서 파라미터 순위로 가는 매핑이 논문에 없어서 그대로 두었다.

**2단 신경망(#11).** 논문의 부분집합 선택기는 State Observer와 Subset Picker 둘로 나뉜다. 우리는 단일 신경망이다.

**세대 수(#16).** 논문 §IV-C는 50세대다. 우리는 40이다. H5Tuner와 같은 예산을 주기 위한 선택이었다.

---

## 14. 누리온에서 실행하기

**결론부터.** 2026-08-28에 코드가 누리온에 올라갔고 shim이 빌드됐다. 아래 경로는 전부 확인된 실제 값이다. 환경 상세는 `04-누리온-환경.md`에 있다.

### 확정된 경로

```bash
module purge
module load gcc/10.2.0 openmpi/3.1.0

AT=/scratch/$USER/12-autotuner
H=$HOME/00-dependency/installation-home
B=$HOME/01-benchmark/h5bench/build

SHIM=$AT/lib/libautotuner.so
DARSHAN=$H/darshan-runtime-gcc/lib/libdarshan.so
PARSER=$H/darshan-util-gcc/bin/darshan-parser
MACSIO=$B/macsio
JSONLIB=$B/json-cwx/lib
```

**툴체인을 섞으면 안 된다.** h5bench와 MACSio가 GCC 10.2.0 + OpenMPI 3.1.0 + `hdf5-gcc`로 빌드되어 있으므로 shim도 같아야 한다. Intel 빌드 HDF5로 만들면 심볼이 맞지 않아 `LD_PRELOAD`가 조용히 아무 일도 하지 않는다.

**Darshan도 `-gcc` 접미사 쪽을 쓴다.** 같은 이유다.

### 빌드

```bash
cd $AT
export HDF5ROOT=$H/hdf5-gcc
export MXMLROOT=$AT/deps/mxml-install
export MPIROOT=/apps/compiler/gcc/10.2.0/openmpi/3.1.0
./autogen.sh
./configure --with-hdf5=$HDF5ROOT --with-mxml=$MXMLROOT --with-mpi=$MPIROOT CC=mpicc
make
```

**`make`는 `test/`에서 멈춘다.** libtool이 없는 `libpbs.la`를 찾는 문제이고 `06-진행-상황.md` §4.2에 있다. **`src/`는 그 전에 끝나므로 `lib/libautotuner.so`는 만들어진다.**

mxml 2.9는 `deps/mxml-install`에 직접 빌드해 두었다. 로그인 노드에서 GitHub 다운로드가 된다.

### MACSio 캠페인

```bash
cd $AT
python3 -m autotuner run --framework tunio \
    --app macsio --macsio-bin $MACSIO --macsio-json-lib $JSONLIB \
    --macsio-part-size 16Mi --macsio-num-dumps 5 \
    --ld-preload $SHIM \
    --darshan-lib $DARSHAN --darshan-parser $PARSER \
    --hdf5-version 1.14.1 \
    --launcher pbs --pbs-account etc --pbs-select 4 \
    --pbs-module gcc/10.2.0 --pbs-module openmpi/3.1.0 \
    --pbs-walltime 00:30:00 \
    --workdir /scratch/$USER/12-autotuner/run \
    --ranking-from-trace traces/sweep.jsonl \
    --seed 1 --rl-seed 7
```

**MACSio는 `--darshan-lib` 없이는 거부된다.** 우리가 파싱할 형식으로 대역폭을 찍지 않기 때문이고, 없이 돌리면 모든 후보가 벌점을 받아 탐색이 따라갈 것이 없어진다. CLI가 그 이유를 말하고 멈춘다.

**`--macsio-arg`로 `--sieve_buf_size`, `--meta_block_size`, `--alignment`를 주면 안 된다.** 셋 다 튜닝 대상이다.

### 왜 MACSio인가

**h5bench write보다 파라미터를 잘 커버한다.** 12개 중 죽는 것이 적다.

MACSio는 SIF 모드에서 데이터셋을 single chunk로 만든다. 그래서 `chunk_cache`가 산다. h5bench write는 `COMPRESS=NO`면 contiguous라 그 파라미터가 죽고, 압축을 켜면 h5bench가 `H5Pset_deflate(9)`를 강제해 워크로드가 CPU 바운드가 된다.

메시 메타데이터가 많아 `meta_block_size`, `coll_metadata_write`, `col_meta_ops`, `mdc_conf`도 실제로 눌린다. h5bench write는 데이터셋을 몇 개만 만들어 이 넷이 약하다.

**그리고 논문이 I/O Discovery 평가에 쓴 응용이다.** 재현 관점에서도 맞다.

### 잡 개수

**세대 하나가 잡 하나다.** 40세대에 개체 15면 41잡이다(baseline 1 + 세대 40). 후보마다 내면 601잡이었다.

누리온 `normal` 큐는 대기 1,000개, 실행 700개를 허용하므로 개수는 제약이 아니다. **제약은 혼잡도다.** 2026-08-28 기준 88% 사용에 380개가 대기 중이었다.

`debug` 큐는 2노드·2잡 제한이라 확인용으로만 쓴다.

### 첫 실행 전 반드시 확인할 것

**힌트가 Lustre에 실제로 반영되는지 봐야 한다.** shim이 로드되고 property list에 값이 들어가도, 파일시스템이 그것을 적용하지 않으면 튜닝은 돌아가되 아무 의미가 없다.

```bash
for SF in 4 32; do
  # config.xml에 striping_factor=$SF 를 쓰고
  LD_PRELOAD=$SHIM AT_CONFIG_FILE=$PWD/config.xml mpirun $MACSIO ...
  lfs getstripe macsio*.h5 | grep stripe_count
done
```

**stripe_count가 4와 32로 다르게 나와야 한다.** 같으면 거기서 멈추고 원인을 찾아야 한다.

원래 `make check`의 15개 테스트가 이 역할이었는데 링크가 깨져 쓸 수 없다. 다만 이 방법이 더 강한 증거다. 테스트는 property list를 되읽을 뿐이고, 이것은 파일시스템까지 확인한다.
