# Run artifacts written into the working directory by evo/evolve.py.
# Anchored to the repo root so the tracked examples/config.xml,
# test/config.xml and xml/config.xml are unaffected.
/config.xml
/config_feat.txt
/result_output.txt
/running_time.txt

# The numbered documents 01-07 and traces/ used to live here: the h5tuner-ext
# remote is public and they carry internal analysis.  Published deliberately on
# 2026-09-14, so they are tracked now.

# Derived ranking regenerated by `autotuner rank`; traces/ is tracked.
/ranking.txt

# Python build noise.
__pycache__/
*.pyc

# local development virtualenv (libclang for I/O discovery)
.venv/

# Claude Code tool permissions: machine-specific, names a home directory.
.claude/settings.local.json
